# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 20, 2026-07-22 15:16 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 20**; next session I run is **Session 21**.
- **Next regular director progress report: my Session 24.** (Phase-0-close, Phase-1-close, Session-8, Session-16 reports exist. S17–S20 closed no phase and wrote no Claim-Sheet amendment → no trigger. S21–S23 need NO progress report unless a phase closes or a Claim-Sheet amendment is approved.)
- **`config.json` is deliberately NOT frozen.** Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 183 tests green** (173 after Codex's S19 screen + correction; +10 from my S20 baseline-integrity regression).

## Review-cycle state (READ THIS FIRST next session)

- **My S19 loop is CLOSED** — Codex genuinely owner-re-reviewed my three edits to the bounded noisy information review and **explicitly approved my exact handed-back state** (2026-07-22 14:10 PDT). Both approvals name the same state. Done; do not reopen.
- **ONE REVIEW LOOP IS OPEN, AND IT IS MINE TO HAVE HANDED BACK.** In S20 I first-reviewed Codex's S19 **bounded structural-action family screen** (both its handoff turn *and* its correction turn, as one state) and **did not approve same-state. I edited and handed back.** Codex must now genuinely owner-re-review my edits and either approve that same state or edit and hand back.
  - **My edits (all in Codex's lane, all in review):**
    1. `scripts/screen_structural_recovery_action.py` — new `_baseline_comparison_sound(spec, rows, source)`, conjoined into **both** assessment gates. The defect: `decide_assessment` checked only the *acting* rows; the four `no_action_1p00` **baseline** rows — against which every reported reduction and the entire specificity margin are computed — were never gated. A baseline that itself acted, evaluated twice, moved pre-decision, saturated, or raised an A1 flag would still have supported `ADVANCE` with every visible gate green. `select_candidate`'s tuning gate already held its baseline to this standard; assessment was the weaker half. Folded in-place (Codex's own correction pattern) so **no dict key was added** → recorded decision identical key-for-key *and* value-for-value.
    2. Same file — `_scope_lines()`, a **generated** report section ("What the recorded decision does and does not establish"), every figure recomputed from the recorded rows so it regenerates deterministically. Four bullets: the measured structural deficit and its ratio to the gate; the margin-vs-spread problem; the unfaulted-joint contrast; the non-discriminating safety readouts + the margin the action consumes.
    3. `tests/test_structural_recovery_action.py` — `_advancing_assessment_rows()` helper + a 2 sources × 5 conditions parameterized regression (27 → 37 focused tests).
    4. `Reproducibility Packet/README.md` Step 13 — one added paragraph carrying the deficit number and the resulting ordering of the redesign.
  - **Verification of the edits:** full end-to-end re-run at 12 workers → `summary.json` `bb29ad05…`, `candidate_rows.csv` `824e6de8…`, `tuning_rows.csv` `acf4afc6…`, `assessment_rows.csv` `4f3acb81…` all **byte-identical to Codex's originals**; only the report moves (`3c4dc458…` → `06cb38cd…`) and it regenerates deterministically both from a fresh run and from the committed `summary.json`.
  - **If Codex accepts the diagnosis but not my implementation, that is a real disagreement** — per `Playbooks/review-cycle.md` it must say so. Escalate to Randy if a specific point hasn't converged in ~2 round-trips.
- **My S20 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-22 15:16 PDT; file 1397 → **1490 lines**; my header at line 1401, appears once, physically last). Verified by anchoring the append on Codex's exact multi-line tail and re-checking after the write.
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S20 — clean.)

## THE HEADLINE FINDING OF S20 — carry this into everything

**Nobody had measured how much tracking error the faults actually cause.** I did, with no recovery action running (my own 84-arm drive, 4 assessment seeds):

| fault | no-action `J_5s` | deficit vs healthy | vs seed spread | peak \|gauge\| |
|---|---:|---:|---:|---:|
| healthy | 0.859842 | — | — | 19.02 µε |
| structure | 0.860499 | **+0.076%** | 0.18× | 38.25 µε (**2.01×**) |
| actuator | 0.914482 | **+6.355%** | 15.0× | 17.18 µε (**0.90×**) |

- **The structural fault is loud in strain and silent in tracking; the actuator fault is the exact mirror.** Structure's deficit is 0.18× the seed spread — indistinguishable from zero — and I get the same 0.18× independently from Codex's committed rows (+0.0494% there). It is **~200× below the Claim Sheet's 10% bar.** There has never been anything for a structural action to recover on this condition.
- **Retuning the nominal controller is NOT the binding lever.** Under the ×2 multiplier applied to *both* arms (i.e. a retuned controller), the structural deficit rises 0.049% → **0.378%** (7.7×) — still **26× short** of the bar.
- **The actuator class is where the control layer is alive, and the existing code already works there.** The current inverse-gain action recovers **85.7% of the actuator fault's entire deficit**, lands within **0.909%** of healthy, and has a **+1.404 pp** specificity margin consistent across all four seeds (4.5× any structural margin, and unlike them it does not sign-flip).
- **But its ceiling is the deficit: a perfect actuator recovery is a 5.975% reduction, and the bar is 10%.** ⇒ **On this bounded condition no correct diagnosis of any class can meet Slot 7/11.** Structure has no deficit; actuator's deficit is smaller than the bar. This is a property of the fault settings and the task, not of the controller or the action family.
- **The uncomfortable, honest shape this implies:** S's information advantage sits entirely on the class with **no control headroom** (structure); the class with control headroom (actuator) is the one **C1 already detects at 100% recall**. If a deficit screen confirms it, that is a real, publishable Slot-13 **diagnostic-only** result about where distributed structural sensing does and does not pay — pre-declared, not a retreat.

**My proposed order of operations (in chat §6, awaiting Codex):**
1. **Run a per-class deficit screen BEFORE any further action screen.** Sweep fault severity (and task speed/amplitude if needed), record the **no-action** `J_5s` deficit per class, advance only a condition whose deficit clears 10% with margin. Cheap: 3 sources × a few seeds × a severity grid, **no action arms at all**. Turns "the condition must show a measurable deficit" from a hope into a gate. Severity grids are already on the freeze list.
2. **Then** screen actions on a condition that can discriminate them, with the specificity comparison moved into **selection** (not just assessment) and given an uncertainty estimate.
3. If no admissible severity inside the A1 limits produces a ≥10% structural deficit, **that is the answer** → Slot-13 diagnostic-only for the structure class.

## What I found reviewing Codex's screen (S20) — the other two findings

**1. The source-specificity gate cannot resolve the quantity it gates.** I re-ran the identical design — same seeds, same mechanics, same candidate — changing only the `pair_id` string that keys the sensor RNG (one equally legitimate noise realization):

| | Codex's run | my replicate |
|---|---:|---:|
| structural improvement | 19.88% | 20.33% |
| healthy improvement | 20.15% | 20.02% |
| **specificity margin** | **−0.263 pp** | **+0.311 pp** |
| **verdict** | **BLOCK** | **ADVANCE** |

The gated quantity is the difference of two **unpaired** 4-seed means with **no computed uncertainty**, while the per-seed reduction spreads it is built from are **1.002 pp** (structure) and **1.298 pp** (healthy) — 4–5× the margin. It also has no floor: in my replicate the **0.75 derate**, which makes tracking 18.5% *worse*, scores a **+0.040 pp "PASS"** on specificity. **A gate whose sign is set by seed labeling cannot carry a decision string.** I did NOT redesign it (that is a real design choice in Codex's lane and it is about to rebuild the screen); I measured the problem and handed it over.

**2. The BLOCK is still right — on two grounds far outside noise, which I moved the report onto:**
- **~70% of the selected action's benefit is produced at the joint carrying NO fault.** At the identical 2.00× multiplier, `localized_2p00` (applied only at joint 1, the joint the diagnosis localizes *and* the faulted link) recovers **6.16%** vs `global_2p00`'s **20.37%**. Same ~70% at 1.25× (72.9%) and 1.50× (69.3%). Within-role, ~14 pp effect vs ~0.1 pp spread — not noise-limited, and it was **already sitting in Codex's tuning rows**, unread by the selection rule.
- **Algebraically, the winning action IS a gain retune.** I instrumented a `global_2p00` rollout: peak nominal PD command **0.0180 / 0.0104 N·m against the (0.20, 0.10) task limit** (9% / 10.4%); peak applied **0.0361 / 0.0141 against the (1.0, 0.5) recovery limit** (<4%); joint-0 applied/nominal peak ratio exactly **2.000000**. **No clip binds anywhere**, so on a linear PD law `global_2p00` ≡ doubling `kp` and `kd`. Every family member is a constant scalar gain — `FixedSourceStandIn` pins `severity_out = 0.50`, so `1/severity` never varies and the "severity-conditioned" path was **never exercised**. That is why the healthy stress could not have failed to fire.
- **Design implication I flagged:** a scalar-conditioned constant gain is a retune *by construction*. A genuinely source-specific action almost certainly has to consume the **deflection/strain signal the diagnosis was made from**, not the 4-way class label + a scalar severity.

**3. The safety gates carried no information, and the action is not free.** Operating point vs A1 limits: joint angle ~9% of π, gauge ~11% of 500 µε, actuator command <4% of range, contact force 42%→61% of 5 N. None could discriminate any candidate. Meanwhile the selected action raises mean peak contact force **2.084 → 3.054 N (+48%)** and mean peak structural strain **37.9 → 56.9 µε (+50%)** *on the link the diagnosis says has lost stiffness*, and `safety_incident_rate` scores all of it as **zero** (threshold-crossing count). **Same blind spot as my S19 note, opposite sign:** the derate gave up 18.5% tracking to cut peak force 76%; this action buys 20% tracking at +48% peak force. **Two candidate actions now trade against each other on an axis the project has no metric that can see.** If step 1 above finds no admissible condition, the graded-safety-statistic amendment is the conversation to have — declared in advance, through the protocol.

## Reproduction results (S20, for the record)

- **Decision arithmetic: 50/50 checks, zero mismatches, first pass** (scratchpad `s20_decision_audit.py`) — my own paired reductions, all four candidate gates, the selection rule, both assessment gates, the margin, written from stated definitions not imported. Reproduced: all 9 candidate rows, eligible set `{global_1p50, global_2p00}`, selection of `global_2p00`, structural 19.883692 / 19.400156, healthy 20.146310, margin −0.262617, both verdicts, JSON↔CSV agreement, no NaN/Infinity, every report table cell.
- **Byte-determinism 5/5 SHA-256** identical to committed and to Codex's reported hashes, **at 10 workers vs Codex's 8**.
- **My own 84-arm drive** (`s20_probe.py`): my trapezoidal `J_5s` from the §G definition vs the packet's `j_5s()` → **max abs diff 0.000e+00** over all 84 arms. Zero lifecycle violations, zero A1/saturation.
- **Lifecycle audit, all 43 recorded rows:** exactly one classification evaluation each, no pre-decision command change, no-action arms untouched, acting arms acting, all seven A1 flags zero, zero saturation. All 9 candidates within a tuning seed share **one** pre-decision hash → candidates provably differ only after the held decision.
- `compileall` clean; CLI help clean.

## The single most important things to do next session (Session 21)

1. **Check Codex's S20 turn** for its owner re-review of my four edits. If it approved the same state → loop closed, note it. If it edited and handed back → genuinely re-review its edits *and* its diagnosis (do not wave them through), then approve or edit again. Escalate a specific non-converged point to Randy after ~2 round-trips.
2. **Watch what Codex builds next.** If it runs the **deficit screen**, review it against the numbers above — the test is whether any admissible severity produces a ≥10% per-class deficit inside the A1 limits. If it instead goes straight to retuning the nominal controller, say so: I showed retuning moves the structural deficit 0.049% → 0.378%, still 26× short.
3. **No progress report due S21** (next regular = S24; only a phase close or approved amendment adds one).
4. **When I next touch `estimator.py`** (the learned head): fold in the carried `null_std` floor consistency nit (`CoefficientReferenceDetector.update` floors at `_EPS` → make it `1e-3` to match `WindowNoveltyDetector`). Forward fix, not a reopen.
5. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.

**Do NOT freeze a partial config.** Open freeze items: **the per-class deficit screen (NEW, S20 — now the prerequisite to any further action screen)**; the structural recovery action / control-sensitivity redesign; validation-sized healthy threshold calibration (≥~100 values) **with per-suite probability calibration**; the ambiguous-case fault library that makes abstention testable at all (my S19 note); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; the reference-lifecycle choice (single held decision vs. a temporal model over the full post-probe trajectory); the **bounded task/contact/controller profile** (`z=0.200 m` + PD gains `(0.05,0.03)`/`(0.005,0.003)` + torque limits `(0.20,0.10)` + timing are *dev candidates*, NOT frozen margins); `W`/stride (**768/16** pilot-advanced, still not frozen); split/leakage/storage/hash audits.

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13; UNCHANGED in S14–S20**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`, suite-agnostic over the 18-col registry. Constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin as per-feature z-scores; score std floored at `1e-3`. Latches `detection_time_s` after `persistence` consecutive over-threshold windows.
- **`CoefficientReferenceDetector`**: second interpretable detection rung; scores joint healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic every pilot/screen imports). Detection-only, abstains on type. `fit_reference` atomic; on re-fit invalidates threshold + resets latch. `calibrate_threshold` FAILS LOUD below `ceil(min_tail/far)` (≥100 at far=0.05). **NIT to fold in next estimator touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` → float to `1e-3`.
- **`synchronous_coefficient_vector` drops masked channels physically** (C1's vector is genuinely shorter than S's, not zero-padded); each suite's reference is fit on its own dimension; `/√D` puts suites on one comparable scale — **empirically confirmed S19** (thresholds 1.281 vs 1.263).
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (diagnosis→control socket; runs estimator every `stride`, **ZOHs the estimator OUTPUT but calls `recovery_command` EVERY step**). Calls `estimator.reset()` at construction.
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal.
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze. Do NOT ship untrained shells.

`Reproducibility Packet/tests/test_recovery_seam.py` (**built my S14; jointly approved S15; UNCHANGED S16–S20**): shared end-to-end mechanism regression driving `CablePlant → OnlineSensorSession → EstimatorCommandPolicy → GainScheduledRecoveryController`. 4 tests. Asserts applied/delivered torque only; labeled NOT a `J_5s`/safety result.

`Reproducibility Packet/scripts/utils/metrics.py` + `utils/stats.py` (**jointly approved through S11; unchanged S12–S20**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` w/ matched-`[T,7]` guard). `j_5s` has a fail-loud guard requiring the full `[t_c, t_c+5 s]` window — **and S20 confirmed it independently: my from-definition trapezoid matches it to 0.000e+00 over 84 arms.** **`safety_incident_rate` is a threshold-crossing count and cannot score a graded margin change in either direction** (S19 + S20). `stats.py` = crossed pair×seed paired hierarchical bootstrap. Eval **driver** (argparse CLI) owns the exact slice — build once frozen data layout exists.

## Codex's lanes — current state

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig` (S12; my approval S13) + `command_from_nominal(output, nominal_command)` (S17; my approval S18) + **`structural_action ∈ {derate, inverse_stiffness}` with `structural_compensation_scope ∈ {global, localized}` (S19; under my review)**. `_confident_source` gate = not abstained AND unique argmax==source AND p≥0.5 AND **finite** uncertainty ≤0.25. Actions: structural → 0.75 global derate (default, approved floor) **or** probability-weighted capped `1/remaining_stiffness` multiplier, global or at the located joint, failing safe to nominal on bad location/severity; attributed actuator → prob-weighted inverse-gain at joint, capped 2×. Recovery `torque_abs_limit (1.0, 0.5)` = the plant's own `ctrlrange`, so **saturation can only fire if the plant would clip too**.

`Reproducibility Packet/scripts/screen_structural_recovery_action.py` + `tests/test_structural_recovery_action.py` + `results/structural_recovery_action_screen/` (**Codex S19 + its S19 correction; my S20 first review → EDITED AND HANDED BACK, loop open**). 9-candidate family (`current_derate_0p75`, `no_action_1p00`, `global_{1.10,1.25,1.50,2.00}`, `localized_{1.25,1.50,2.00}`); tuning seeds 15000–15002 (**structure only**), disjoint assessment seeds 15100–15103 (structure + healthy false-authorization). Selected `global_2p00` (tuning mean/min 20.37%/20.24%); assessment retained 19.88%/19.40% with zero A1/saturation, but healthy improved 20.15% → margin **−0.26 pp** → **`BLOCK_STRUCTURAL_RECOVERY_ACTION_FAMILY`**. Codex's stated next step: retune the common nominal controller, then require a task/fault condition with a measurable stiffness-loss deficit. **My S20 review says the second clause should come first — see the headline finding.**

`Reproducibility Packet/scripts/run_bounded_noisy_information_review.py` + tests + `results/bounded_noisy_information_review/` (**Codex S18; my S19 edits; Codex's S19 owner re-review approved same state → JOINTLY APPROVED, loop closed**). Real noisy instrument at the held decision on the bounded condition. **S PASSES information + action gates (macro-F1 0.995, 100% per-fault detection, 2.1% healthy FA); C1 BLOCKS (0.704, 8.3% structural recall, 4.2% FA); the recovery-control profile BLOCKS.**

`Reproducibility Packet/scripts/utils/task_control.py` (**Codex S17; my approval S18**) — `BoundedTaskProfile` (rest → `(0.30,0.30)` rad by 3.0 s → hold → back to rest by 5.0 s; movement_start 2.40, transition 0.60, hold_end 4.40, return_end 5.00), `ObservedJointControllerConfig` (kp `(0.05,0.03)`, kd `(0.005,0.003)`, torque limits `(0.20,0.10)`), `ObservedJointPDController` (reads ONLY delivered `q_obs`/`qd_obs`; clips to its limits — **S20: peak command is only ~10% of those limits, so the clip never binds**), `EstimatorRecoveryTaskPolicy` (task PD + diagnosis cadence + recovery on a live per-step nominal; records `nominal_commands` and `commands`).

`Reproducibility Packet/scripts/utils/cable_mechanics.py` + `utils/cable_plant.py` + `make_mujoco_plant_trace.py` — the plant lane + endpoint-contact extraction (S14; my approval S15). **A1 safety flags** = `|q|>π` (per joint), `|qd|>10` (per joint), 3-D tip radius > 0.82 from base `[0,0,0.5]`, `max|gauge|>500 µε`, `contact_state[0]>5 N`. Limits live on `CableModelConfig` (`tip_workspace_radius_limit_m=0.82`, `gauge_abs_limit_microstrain=500.0`, `tip_contact_force_limit_n=5.0`) and `cable_config()` does **not** override them. `saturation_flag = (command != control_effort)` where `control_effort = clip(command, ctrlrange)` and ctrlrange is `(±1, ±0.5)`. **The 3-D workspace flag is NOT reconstructable from the record's 2-D `true_task_output` — recompute the other 6 from raw truth + trust the plant column for the 7th (my S18 harness lesson; honored S19 and S20).**

`screen_bounded_task_contact.py` (S17; my approval S18) — `BoundedTaskContactSpec`, `cable_config`, `SingleDecisionHoldEstimator` (one evaluation at `first_decision_step`, held causally, healthy before), `FixedSourceStandIn` + `_output_for_source` (**structure → location 1, severity 0.50, one-hot, uncertainty 0; actuator → 1, 0.70; sensor → 0, 0.05**). `first_decision_step` is `ceil(after_probe/stride)*stride` → structurally always a stride multiple. `run_noisy_reference_pilot.fault_specs` = the fixed development fault library (structure: `link_stiffness_loss`, loc 1, sev 0.50; actuator: `actuator_gain_loss`, loc 1, sev 0.70; sensor: `encoder_bias`, loc 0, sev 0.05).

`run_noisy_reference_pilot.py` (S11), `screen_optional_contact_profile.py` (S15), `run_matched_contact_pilot.py` (S16, blocked all three gates, superseded) — all jointly approved. `fault_specs`, `project_observed_suite`, `count_contact_episodes` are reused by the newer screens.

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic**. `synchronous_coefficient_vector` + `coefficient_reference_distance` in `utils/estimator.py` are the one canonical definition every pilot, screen, and review imports.

The **recovery seam** has both ends jointly exercised (my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController`), pinned by `test_recovery_seam.py`. **`detection_time_s` semantics are consistent everywhere** (latch-once at first detection; NaN before). The bounded screen (S17) established that recovery can act *before* contact; the noisy review (S18/S19) established that a *real* estimator can supply the label; **S20 establishes that on this condition there is nothing for the structural label to buy.**

**Contact + the 7th safety flag:** contact reaches deployable suites only through motion/strain (privileged-only otherwise). In closed loop it is NOT suite-invariant. **Design constraint (my S15 note, honored by every screen since): apply the contact profile identically across each matched C1/S CRN pair.**

**Sensor RNG is keyed on `(sensor_seed, pair_id, channel, stream)`** — the `pair_id` string is load-bearing (my S17 harness bug proved it; **S20 turned it into a measurement tool** — changing only the `pair_id` gives a legitimate independent noise replicate of the same design, which is how I showed the specificity gate flips).

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε mechanics-gate floor) → Codex's 0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50% task torque → S11 noisy-deployable pilot → task 0.50 / probe 0.05 N / W=768 / stride=16 → S12 `CoefficientReferenceDetector` + recovery-controller floor → S13 residual/linear-sysID baseline → S14 detect→(attribute)→compensate loop pinned end-to-end + endpoint-contact extraction → S15 both loops approved + optional-contact screen → S16 matched contact-enabled pilot BLOCKS all three gates → S17 I reproduced + approved the block; Codex's bounded redesign clears the mechanics/lifecycle block → S18 I reproduced every gate and approved; Codex replaced the stand-ins with a real noisy instrument → S19 I reproduced every number (53/53), edited and handed back; Codex owner-re-reviewed and approved (loop closed) → Codex built the structural-action family screen, which honestly blocked → **S20 I reproduced it (50/50 + 84 arms), fixed the ungated baselines, showed the specificity gate flips under a replicate, and measured the per-class deficits that say the control bar is unreachable on this condition.**
- **The deployable S advantage IS the structural fault**, confirmed four times (S12, S16, S19, and S20's 2.01× strain ratio). S19 sharpened it: **C1 is blind, not weakly sighted.** **S20 adds the other half: the structural fault is dynamically silent.** Carry BOTH into the confirmatory write-up — together they are the honest finding.
- **The information question is answered on this bounded development condition. The control question is now answered too, negatively, for this condition** — and the next move is to find out whether any admissible condition changes that.
- **Honesty bounds (keep loud):** (1) the deployable floors are **detection**, NOT learned attribution; (2) all rates come from **one fixed fault setting per class**, held-out over sensor noise only; (3) **abstention is untestable on this fault library** (min margin 0.90); (4) full-horizon continuations are **one seed per source/suite**; (5) one-hot prototype probabilities are **not** calibrated probabilities; (6) reference conditioning assumes a scheduled, phase-reset probe + ONE held decision; **(7) NEW S20 — every control-layer number so far comes from a condition where the structural fault causes no measurable tracking deficit, so no structural control result of any sign is interpretable until a deficit screen runs.**

## The headline experiment design

The "does attribution improve control" comparison is wired at the diagnosis→control seam, its control semantics pinned by `test_recovery_seam.py`, its task/contact mechanics by the bounded screen, its *information* path demonstrated with a real noisy estimator. Post-freeze arms: **detection-only rungs → nominal** (control floor); **learned attribution head → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation**; **oracle → ceiling**. Scored by `J_5s` reduction + no-safety-regression on frozen confirmatory data. **S20 sharpening: do not build this comparison until a deficit screen shows the condition can produce a ≥10% per-class deficit. On the current condition the comparison would return "no arm helps" for reasons that have nothing to do with the hypothesis.**

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force. Safety computed from **privileged truth**. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N.
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`) — **the S19 gap stands: held-out axes are sensor-seed only, so the §A whole-fault-setting split is still unexercised.** **§B** privileged `PlantStepState`/`PrivilegedRecord` (`q_true[T,2]`, `qd_true`, `gauge_true[T,4]`, `contact_state[T,2]`, `true_task_output[T,2]` (planar tip), `safety_flag[T,7]`); `observable_sources()` expose only the measurable subset (**contact_state + true 3-D tip NOT in it**). **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative). **§D** labels/outputs/causality/leakage. **§E** storage. **§F** frozen constants. **§G** tracking metric `J_5s`.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95% excludes zero; every source-class recall diff lower-95% above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10%** (paired excludes zero; **no safety regression** — `safety_regression_delta` upper-95% ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes (Slot 13):** **diagnostic-only** (attribution improves, control does not — **S20 makes this the near-certain landing for the structure class**) · fault-specific/bounded · confound-fragile · excitation-dependent.
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics, virtual-gauge extraction, excitation design, interpretable residual/linear-sysID baseline, recovery controller + external-nominal composition, contact/safety extraction, the contact/bounded screens, the matched pilots, the bounded noisy held-decision information review, **the structural recovery action family screen (S19; my S20 edits pending its re-review)**, **the per-class deficit screen + retuned nominal controller (next)**, evaluation-sized closed-loop controller comparison (pending).
- **Claude (me):** fault-injection + sensor-realism model, two-layer evaluation harness + metrics + stats, diagnosis-estimator front + window contract + oracle + seam adapter, synchronous-detection floor analysis, shared recovery-seam regression, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (+A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), `utils/synchronous.py`. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam.

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). **Running a script: from `Reproducibility Packet/` as `..\venv\Scripts\python.exe scripts\<name>.py`** (NOT from `scripts/`). **Full packet: 183 tests green (S20).** Set `PYTHONIOENCODING=utf-8` when a script prints unicode. My scratchpad reproduction scripts also run **from `Reproducibility Packet/`**. **Timings:** `run_bounded_noisy_information_review.py` ~12–20 min (592 runs + 8 rollouts); `screen_structural_recovery_action.py` **~8 min** (43 rollouts) at 8–12 workers; a bare 84-rollout probe ~15 min at 10 workers. Run these in the background — piping to `tail` buffers all output until exit, so check the output file only at the end.
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked. Root `.gitignore` covers `venv/`, `tmp/`, `MUJOCO_LOG.TXT`, caches, LaTeX aux, OS/IDE noise — **verified accurate S20, no change needed.**
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules + `tests/` are the import-not-CLI exception.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py`; shared seam test `tests/test_recovery_seam.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control,residual_baseline,task_control}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot,screen_optional_contact_profile,run_matched_contact_pilot,screen_bounded_task_contact,run_bounded_noisy_information_review,screen_structural_recovery_action}.py` + matching tests + `results/` artifacts.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S20 turn = tail, 1490 lines; **one review loop OPEN — my edits await Codex's owner re-review**). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (**no S20 entry — reproduction/measurement session, no external sources read**) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8, Session 16). **Next regular: Session 24.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-22 (Codex's structural-action block). Untouched S20 — the log is append-only by playbook, the entry is Codex's, and my finding is mid-loop. **The next public entry should carry the per-class deficit result once the loop closes / the deficit screen lands.**
- Scratchpad (not committed, outside repo): `s20_decision_audit.py` (50-check arithmetic audit — reusable template for auditing a decision function against its own committed rows), `s20_probe.py` (84-arm probe driver: own case runner, own `J_5s`, full candidate × source × seed matrix — **reusable for the deficit screen review**), `s20_probe_rows.json`, `s20_regen/` + `s20_regen2/` (byte-determinism runs before and after my edits).
