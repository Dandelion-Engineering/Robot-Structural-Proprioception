# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 11, 2026-07-20 10:33 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 11**; next session I run is **Session 12**.
- **Next regular director progress report: my Session 16.** (Phase-0-close, Phase-1-close, and Session-8 reports exist; they don't reset the per-agent counter. Session 11 closed no phase and wrote no Claim-Sheet amendment, so no progress-report trigger.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 102 tests green** (unchanged this session; no code of mine changed).

## Review-cycle state (READ THIS FIRST next session)

- **THE COMBINED LOOP IS CLOSED.** At its S10 turn Codex reviewed the two increments I handed back in S10 and opened a loop on my side. In **S11 I closed it** with genuine owner re-review + independent reproduction, and **approved the exact same state** Codex approved:
  1. **`utils/estimator.py` synchronous feature** — Codex corrected it from **amplitude-only** to **cosine + sine + amplitude** per column (`[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`). Reason: amplitude discards phase, but the mechanics safe-probe screen measures `‖coeff(fault) − coeff(reference)‖` (the coefficient-vector distance), and `| ‖a‖−‖b‖ | ≤ ‖a−b‖`, so amplitude-only can hide a detectable change. Diagnosis + implementation both correct; approved.
  2. **`utils/metrics.py` `safety_regression_delta`** — Codex added a fail-loud guard requiring matched `[T,7]` C1/S shape before differencing rates. Sound; approved.
- **No open review loops on my side.** **My S11 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-20 10:33 PDT). **Codex has NOT replied yet — check next session.**
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S11 — clean, my turn appears once and is last.)

## What I did this session (Session 11) — verification-only, no code changed

- **Genuine owner re-review of Codex's S10 edits**, reproduced independently (not a re-run of Codex's tests):
  1. **Math (from scratch):** triangle inequality never inverts over 2×10⁵ random pairs (max residual −1.7e-11); amplitude-only under-reports the coefficient distance in 100% of them; a *pure phase rotation* is invisible to amplitude-only but the screen counts it.
  2. **Real feature path:** on a genuine suite-S observed record (gauge-0, 696/700 valid, real dropout), `WindowFeatureExtractor.window_features` cos/sin is **bit-identical** to my independent normal-equations solve; injected 50 µε/0.8 Hz tone shifts coefficients exactly `[50,0]` (phase 0) / `[0,−50]` (phase 90); two equal-magnitude changes are 0 apart in amplitude, 50√2 apart in coefficient space.
  3. **Codex's real-MuJoCo headline (reproduced):** actuator-vs-healthy best coefficient distance **0.8977 µε (2.22×)** on gauge 1, best amplitude-only **0.7160 µε (1.77×)** on gauge 2, gauge-1 retention **29.6%** — matches Codex's 0.898/0.716/29.6% exactly. Insight: the gauge with the *largest* screened separation is the most phase-like, so amplitude-only loses margin precisely on the most informative channel.
- **Closed the loop** (approved same state) → **this unblocks Codex's pilot** (Codex had declined to start it on the amplitude-only contract).
- **Forward design proposal (in chat, §3 of my turn):** the feature now *retains* the coefficient vector, but `WindowNoveltyDetector` consumes cos/sin only as generic per-feature z-scores — not the *joint* coefficient-space distance-to-reference the screen uses. Proposed the deployable analog (`‖coeff_live − coeff_ref‖` standardized against the healthy null, phase/onset/stride alignment = pilot variables). **Offered to build the interpretable rung once the pilot settles the reference/alignment convention, OR prototype now against the dev convention if Codex wants the instrument for the sweep. Awaiting Codex's answer.**

## The single most important things to do next session (Session 12)

1. **Check Codex's reply to my S11 turn** in the active Phase-2 chat. Likely content: (a) acceptance of my loop-close; (b) an answer on the coefficient-distance rung (build-after-convention vs prototype-now); (c) possibly the pilot sweep starting/started. Respond accordingly — if Codex asks me to prototype the coefficient-distance rung, that's my next build; if Codex edits any of my files, close the loop with genuine re-review.
2. **If Codex ran/started the pilot sweep** (its lane): help interpret coefficient-space detector-margin vs safety vs tracking tradeoffs; align the synchronous feature / W=640 / stride to whatever the sweep settles.
3. **Deferred, still waiting on config freeze + confirmatory data** (do NOT build early): the learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; and (post-freeze, needs real multi-run storage) the §D deployable-loader leakage test + whole-trajectory/fault-setting split audit.

**Do NOT freeze a partial config.** Open freeze items (all still open): Codex's excitation-redesign pilot outcome; joint sanity-check of non-load-bearing sensor constants; shared severity/onset grids; validation-frozen class/abstention/selective/OOD thresholds; contact-enabled cases; `W`/stride pilot sweep (W now 640-centred).

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**jointly approved through S11**): `EstimatorOutput`/`EstimatorTrace` (§D contract, validated, causal); `WindowFeatureExtractor(window_steps=W, probe_frequency_hz=0.8)` — fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, **sync_cos, sync_sin, sync_amplitude**, valid_fraction]` (per-channel-time slopes AND per-channel synchronous **cos/sin/amplitude**, ≥1-cycle gated, via shared `utils.synchronous.harmonic_coefficients`), suite-agnostic over the 18-col registry; `WindowNoveltyDetector` — interpretable detect + calibrated-abstention (consumes the sync features generically); `OracleInterface(onset_time_s)` — allowlisted privileged ceiling, causal; `EstimatorCommandPolicy` — the `run_online_rollout` `CommandPolicy` adapter. `RECOMMENDED_WINDOW = (W=640, stride=8)` (pilot-sweep proposal). Feature layout constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline; shared temporal encoder over `[W,D]` → class/unknown/location/severity heads) + `RMALatentEncoder` (adapt-without-attribution control comparison). Post-freeze; need PyTorch + confirmatory data. Do NOT ship untrained shells or report performance before frozen data.
- **Open estimator-side design question (my proposal, awaiting Codex):** a coefficient-space distance-to-reference detector rung (deployable analog of `screen_synchronous_safe_probe.py`) that *uses* the retained cos/sin jointly. Build after pilot reference/alignment convention settles (or prototype earlier if Codex asks).

`Reproducibility Packet/scripts/utils/metrics.py` (**jointly approved through S11**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` — the last now with a matched-`[T,7]`-shape fail-loud guard). `utils/stats.py` = crossed pair×seed paired hierarchical bootstrap. The eval **driver** (argparse CLI) still owns the exact `[t_c, t_c+5 s]` control-grid slice before calling the safety point metrics — to build once the frozen data layout exists.

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — now `harmonic_coefficients` (returns `[cos,sin]`) + `harmonic_amplitude` (= their norm) + `harmonic_design_condition_number` — used by (a) Codex's mechanics safe-probe screen `screen_synchronous_safe_probe.py`, (b) the detector-floor analysis `analyze_synchronous_detection_floor.py`, and (c) my estimator's synchronous feature. The screen measures the fault-minus-reference **coefficient-vector distance** `‖coeff(fault)−coeff(reference)‖` (linear regression ⇒ `coeff(a−b)=coeff(a)−coeff(b)`); the estimator now retains the full `[cos,sin]` so that distance is reconstructible. The cos/sin are anchored to **absolute time** (`phase = 2π f t`, raw t), so two runs' coefficients share a phase reference and are differenceable — but deployable cross-run comparison still needs the pilot's explicit phase/onset alignment.

## The excitation blocker — current status (the project's dominant thread)

- **The reframing (S9, mine):** a synchronous detector at 0.8 Hz has NES ~0.11 µε — ~90× below the per-sample 10 µε mechanics-gate floor. So the excitation target moved from "reach 10 µε broadband" (which forced unsafe amplitudes) to "produce a clean ≥1-cycle differential at the known frequency," recoverable ~90× lower.
- **Codex's safe-probe candidate (S9):** on the *actual* four-gauge MuJoCo fault-minus-healthy traces, a **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50% ordinary task torque** clears the corrected detector threshold by **2.22×** (structural 1.015, actuator 0.898, separation 1.090 µε) AND stays inside the safety envelope across all 4 scenarios. **Advances to the pilot sweep ONLY — not frozen, not attribution, not an S-vs-C1 result.** Still fails the legacy 10 µε per-sample screen (preserved as the mechanics-selection record).
- **The 2.22× is a CLEAN PRIVILEGED margin, NOT a deployable-estimator margin** (settled S10–S11, both agents). The pilot must measure coefficient-space detectability on **noisy per-suite observations against a healthy reference**, with phase/onset/stride alignment — it may not promote the clean 2.22× ratio.
- **Honesty bounds (keep loud):** detection floor, NOT attribution (classifying structure-vs-actuator needs the trained head reading differential shape/phase across the 4 stations); the linear thermal ramp is a trend-rejection check, not a bound on nonlinear/probe-band thermal.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force (`saturation_flag[2]` separate). Safety computed from **privileged truth** across every scenario, never a corrupted observed channel. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10 rad/s`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. `CablePlant` emits `[0,0]` contact (collision disabled) and **fails loud** if `data.ncon != 0`.
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState` (lossless); `observable_sources()`/`observable_step_sources()` expose only the measurable subset. **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` = past-only, availability-masked, bounded window). **§D** labels/outputs/causality/leakage (automated leakage test fails build if a deployable loader reaches identity/privileged/label/other-suite arrays — to build post-freeze). **§E** storage. **§F** frozen constants (f_ctrl, dt, W, stride — values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95% excludes zero; every source-class recall diff lower-95% above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10%** (paired excludes zero; **no safety regression** — wired via `safety_regression_delta`, upper-95% ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (← the live risk; the bounded-burst BLOCK is exactly this; the synchronous finding + safe-probe candidate are the current way out, pending pilot).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop done; bounded-excitation envelope + sensitivity done S8; safe-probe co-design + A1 implementation done S9), virtual-gauge extraction, **excitation design (0.05 N one-cycle candidate → pilot sweep, now unblocked)**, interpretable residual/linear-sysID baseline, recovery controller, contact/safety implementation + real endpoint-contact extraction (pending, before optional-contact pilots).
- **Claude (me):** fault-injection + sensor-realism model (done S6, approved S7), two-layer evaluation harness + metrics + stats (done S7, corrected+approved S8, safety gate added S10, pairing guard approved S11), diagnosis-estimator front + window contract + oracle + seam adapter (done S8, approved S9; synchronous feature added S10, corrected to cos/sin/amplitude + approved S11), synchronous-detection floor analysis (done S9, review closed S10), **proposed coefficient-space detector rung (pending pilot convention)**, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py` shared harmonic statistic**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (my `EstimatorCommandPolicy` is the socket; Codex's recovery controller is the injected command).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/`. **Full packet: 102 tests green (S11).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (`results/synchronous_detection_floor/`, `results/synchronous_safe_probe/`, `results/bounded_burst_sensitivity/`).
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules are the import-not-CLI exception — no argparse there.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/`. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor}.py`.
- **Codex's plant lane:** `utils/{cable_mechanics,cable_plant,online_loop}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe}/`.
- **Shared:** `utils/synchronous.py` (Codex S9; now `harmonic_coefficients`/`harmonic_amplitude`/`harmonic_design_condition_number`) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S11 turn = tail; Codex reply pending). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (no S11 entry — verification-only session, no new sources) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8). **Next regular: Session 16.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entries 2026-07-19. Untouched S10 and S11 (heartbeat checks → no log-worthy event).
- Scratchpad (not committed, outside repo): `s11_reverify_math_feature.py` (math + real-feature-path checks), `s11_reverify_mujoco.py` (real-MuJoCo reproduction of the 2.22× vs 1.77× numbers).
