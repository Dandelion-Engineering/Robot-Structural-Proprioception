# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 13, 2026-07-20 14:24 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 13**; next session I run is **Session 14**.
- **Next regular director progress report: my Session 16.** (Phase-0-close, Phase-1-close, and Session-8 reports exist; they don't reset the per-agent counter. Session 13 closed no phase and wrote no Claim-Sheet amendment → no progress-report trigger.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 127 tests green** (was 113 end of S12; Codex's S12 added the recovery-controller suite + the re-fit/pilot-dedup regressions).

## Review-cycle state (READ THIS FIRST next session)

- **BOTH review loops are CLOSED. Nothing is open from my side.** In **S13**:
  1. I did the **genuine owner re-review of Codex's two corrections to my `CoefficientReferenceDetector`** (Codex edited `estimator.py`/`test_estimator.py` in its S12) and **approved the exact same edited state** → owner loop closed.
  2. I did the **first review of Codex's new recovery-controller floor** (`utils/recovery_control.py` + `test_recovery_control.py`, Codex's lane) and **approved the same state Codex approved** (no edits) → first-review loop closed.
- **My S13 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-20 14:19 PDT; file now 764 lines). Verified: my S13 header appears once and is last.
- **Codex has NOT replied to my S13 turn yet — check next session.** Likely Codex's next: the interpretable residual/linear-sysID baseline, the evaluation-sized closed-loop recovery comparison, or the validation-sized threshold calibration. If Codex opens a new loop, review it.
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S13 — clean.)

## What I did this session (Session 13 — a cross-review session, no new production code from me)

- **Reconciled a stale git snapshot:** the prompt showed HEAD at "Claude Session 11"; live repo was actually `a049f53 Codex Session 12`, working tree clean. Reviewed against that clean baseline.
- **Re-reviewed Codex's two estimator corrections, reproduced independently** (scratchpad `s13_reproduce.py`, 26 checks, NOT a re-run of Codex's tests):
  1. **Correction 1 (narrowed margin claim) is right.** I built two `CoefficientReferenceDetector`s sharing one extractor: `score(w)` is bit-identical on every window, but their **decisions disagree** once thresholds differ (far 0.5 vs 0.25), and persistence 1 vs 3 latch at different times. So "shared statistic ≠ shared margin" — the threshold/reference/persistence own the margin+rates. My S12 "deployed margin IS pilot margin" was the overclaim; Codex's narrowing (+ "matched-C1 **minimum per-fault** detection = 0%", localizing the 0% to the *structural* fault) is the honest level.
  2. **Correction 2 (re-fit lifecycle guard) is right.** Reproduced fit→calibrate→latch→re-fit → threshold cleared to None, latch reset to NaN, `update` raises until recalibrate, recalibrate restores scoring. Also verified **atomicity**: a re-fit that raises partway (oversized window) leaves the OLD reference+threshold fully intact (new values computed on locals before any `self.` assignment).
- **First-reviewed Codex's recovery controller, reproduced independently:** on the real `CablePlant`, actuator one-hot (loc 1, 50% remaining) → 2× at joint 1, plant's 0.5 gain delivers nominal bit-for-bit, no saturation; structure → 0.75 global derate; abstain/unlocalized/uncertain/**50-50 tie** all → nominal. **Cross-lane check:** drove my own detection-only `CoefficientReferenceDetector` through the controller → it abstains → controller holds nominal, so the "detection-only can't compensate" safety boundary holds **end-to-end through the shared seam**.
- **Verified all three of my S12 forward nits are closed** in committed artifacts: (a) pilot imports the coefficient functions from `utils.estimator` (confirmed by object identity); (b) both `summary.json`s carry `base_seed` (1000/5000) + seed ranges; (c) advancing report now carries the symmetric threshold caveat (32-seed threshold = LOO max, 1/48 resolution).
- **Two forward notes raised in chat §4 (non-blocking):**
  - (a) **Tiny robustness nit in MY rung:** `CoefficientReferenceDetector.update` floors `null_std` at `_EPS` for `unknown_score=z`, while `WindowNoveltyDetector` floors at `1e-3`. Never bites with a real ≥100-window null (genuine spread), but for sibling-rung consistency **float the coefficient rung's floor up to `1e-3` — fold into my next estimator increment, do NOT reopen the closed loop.**
  - (b) On the record: a constructor-supplied `detect_threshold` survives the *first* `fit_reference` but is invalidated on re-fit (correct by design — only the first fit gives the escape hatch a reference to score against).

## The single most important things to do next session (Session 14)

1. **Check Codex's reply to my S13 turn** in the active Phase-2 chat. Likely Codex's next plant-lane build (residual/linear-sysID baseline, eval-sized recovery comparison) or the validation-sized threshold calibration. If Codex opens a new loop, review it.
2. **When I next touch `estimator.py`:** fold in the `null_std` floor consistency nit (→ `1e-3`). Forward fix, not a reopen.
3. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.
   - A committed **end-to-end closed-loop seam test** (fixed-attribution stand-in driving active comp on the real plant through `run_online_rollout`, + a detection-only rung staying nominal) — I proposed this to Codex in S13 §4; build as a shared test next session if we agree it's worth having before the trained head exists (marginal over existing unit coverage).

**Do NOT freeze a partial config.** Open freeze items (all still open): validation-sized healthy threshold calibration (≥~100 values — now enforced by my rung's `calibrate_threshold` guard); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; contact-enabled cases; `W`/stride (**768/16** pilot-advanced, still not frozen).

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]` (per-channel-time slopes AND per-channel synchronous cos/sin/amplitude, ≥1-cycle + ≥8-sample gated, via shared `utils.synchronous.harmonic_coefficients`), suite-agnostic over the 18-col registry. Feature constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin generically as per-feature z-scores; score std floored at `1e-3`.
- **`CoefficientReferenceDetector`** (my S12 build; **Codex's S13-approved corrections in force**): the second interpretable detection rung, scoring the **joint** healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic the pilot now imports). Detection-only, abstains on fault type. `fit_reference` computes on locals then assigns (atomic); **on re-fit it invalidates the threshold + resets the latch** (Codex's Correction 2). `calibrate_threshold` freezes the (1−far)-quantile and **FAILS LOUD below `ceil(min_tail/far)` values** (≥100 at far=0.05); directly-set `detect_threshold` = labeled pilot escape hatch (survives first fit, invalidated on re-fit). Docstrings say it shares the pilot's **score statistic only**, NOT its margin/decision rates. **NIT to fix next touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` — float to `1e-3` for consistency with the novelty detector.
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal (reads `PlantStepState`, NOT a `DiagnosisEstimator` — does not go through `EstimatorCommandPolicy`). **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (the diagnosis→control socket; recovery command injected as `(EstimatorOutput, step, time)->command`).
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal (single-event-thin margins; validation owns the frozen value). Default `WindowFeatureExtractor` window = 768 (inert for existing tests).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze; need torch + confirmatory data. Do NOT ship untrained shells.

`Reproducibility Packet/scripts/utils/metrics.py` (**jointly approved through S11**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` — the last with a matched-`[T,7]`-shape fail-loud guard). `utils/stats.py` = crossed pair×seed paired hierarchical bootstrap. The eval **driver** (argparse CLI) still owns the exact `[t_c, t_c+5 s]` control-grid slice before calling the safety point metrics — build once the frozen data layout exists.

## Codex's recovery-controller floor (S12; my S13 first-review approval; Codex's lane)

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig`. Plugs into my `EstimatorCommandPolicy` as the injected `recovery_command`. Consumes only `EstimatorOutput` + time (no privileged state); reconstructs the nominal task command via `commanded_torque(t)`. Two auditable actions behind the `_confident_source` gate (not abstained AND **unique** argmax == source AND p ≥ 0.5 AND finite uncertainty ≤ 0.25): (1) structural diagnosis → bounded global derate 0.75; (2) attributed actuator → probability-weighted inverse-gain at the joint, capped 2×. Tests include a real-`CablePlant` compensation regression. **This is the control end of the shared "does attribution improve control" headline seam** — detection-only rungs can't trigger it (safety boundary), the trained head will.

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — `harmonic_coefficients` (`[cos,sin]`) + `harmonic_amplitude` (their norm) + `harmonic_design_condition_number` — used by (a) Codex's mechanics safe-probe screen, (b) the detector-floor analysis, (c) my estimator's synchronous feature, and (d) my `CoefficientReferenceDetector` via `synchronous_coefficient_vector`. The pilot (`run_noisy_reference_pilot.py`) now **imports** `synchronous_coefficient_vector` + `coefficient_reference_distance` from `utils.estimator` (one canonical definition; confirmed by object identity S13). The screen measures `‖coeff(fault)−coeff(reference)‖` (privileged differential); the deployable rung measures `‖coeff_live − coeff_ref‖` standardized against a healthy calibration null. **The rung shares this score STATISTIC with the pilot, but NOT the pilot's margin/decision rates** (threshold/reference/persistence own those — settled S13).

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε per-sample mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50% task torque** (clears the corrected detector threshold by 2.22× AND stays in the safety envelope) → S11 pilot on **noisy deployable observations** with a **healthy calibration reference** → advanced **task 0.50 / probe 0.05 N / W=768 / stride=16** (S worst per-fault detect 97.9%, prototype attribution 100%, healthy false alarms 0.7% pooled / 2.1% worst; matched C1 min detect 0%) → S12 built the deployable `CoefficientReferenceDetector` rung → S12 Codex built the recovery-controller floor → **S13 both review loops closed; the detect→(attribute)→compensate loop now exists end-to-end in prototype, with the *attribution* middle deliberately left to the post-freeze trained head.**
- **The 2.22× is a CLEAN PRIVILEGED margin, NOT a deployable-estimator margin** (settled S10–S11). **The deployed rung shares the pilot's score statistic, not its margin/rates** (settled S13). **The deployable S advantage IS the structural fault** (S12): S detects link-stiffness loss where C1 is blind; C1 sees actuator/sensor faults fine — so "matched-C1 minimum per-fault detection 0%" = the gauges add the structural-deformation observability C1 lacks. Carry this into the confirmatory S-vs-C1 framing.
- **Honesty bounds (keep loud):** (1) this is **detection**, NOT attribution (classifying structure-vs-actuator-vs-sensor needs the trained head; the pilot's nearest-centroid is a pilot instrument); (2) the follow-up's false-alarm rates are single-event-thin (1/48) and the 32-cal "99th percentile" == the max — **validation needs ≥~100 healthy cal values** (now enforced by the rung guard); (3) reference conditioning assumes a scheduled, phase-reset probe — unscheduled phase drift + probe-band thermal interference remain open; (4) the linear thermal ramp is a trend-rejection check, not a bound on nonlinear/probe-band thermal.

## The headline experiment design (both seam ends now exist in prototype — carry this)

The "does attribution improve control" comparison is now wireable at the diagnosis→control seam. Post-freeze arms: **detection-only rungs → nominal** (control floor, no compensation — verified S13); **learned attribution head (rung 2) → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation** (adapt-without-attribution arm); **oracle → ceiling** (drives active comp directly, off-seam, reads privileged state). Scored by `J_5s` (5-s post-change tracking-error integral) reduction + no-safety-regression on the frozen confirmatory data. The trained head lands into a socket whose control semantics are already fixed.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force (`saturation_flag[2]` separate). Safety computed from **privileged truth** across every scenario, never a corrupted observed channel. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10 rad/s`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. `CablePlant` emits `[0,0]` contact (collision disabled) and **fails loud** if `data.ncon != 0`.
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState` (lossless); `observable_sources()`/`observable_step_sources()` expose only the measurable subset. **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` = past-only, availability-masked, bounded window). **§D** labels/outputs/causality/leakage (automated leakage test fails build if a deployable loader reaches identity/privileged/label/other-suite arrays — to build post-freeze). **§E** storage. **§F** frozen constants (f_ctrl, dt, W, stride — values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95% excludes zero; every source-class recall diff lower-95% above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10%** (paired excludes zero; **no safety regression** — wired via `safety_regression_delta`, upper-95% ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (the live risk; the synchronous finding + safe-probe candidate + the noisy-reference pilot are the way out, pending validation-sized threshold calibration).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop; bounded-excitation envelope; safe-probe co-design + A1 implementation; noisy-reference pilot S11), virtual-gauge extraction, excitation design, interpretable residual/linear-sysID baseline (pending), **recovery controller (built S12 — `recovery_control.py`; my first-review approval S13)**, contact/safety implementation + real endpoint-contact extraction (pending, before optional-contact pilots).
- **Claude (me):** fault-injection + sensor-realism model (done S6, approved S7), two-layer evaluation harness + metrics + stats (done S7, corrected+approved S8, safety gate S10, pairing guard approved S11), diagnosis-estimator front + window contract + oracle + seam adapter (done S8, approved S9; synchronous feature S10, cos/sin/amplitude approved S11; **coefficient-distance rung + 768/16 window S12, Codex's corrections re-reviewed + approved S13**), synchronous-detection floor analysis (done S9, closed S10), matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py` shared harmonic statistic**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController` = both ends now in prototype).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/`. **Full packet: 127 tests green (S13).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (`results/synchronous_detection_floor/`, `results/synchronous_safe_probe/`, `results/bounded_burst_sensitivity/`, `results/noisy_reference_pilot/`, `results/noisy_reference_pilot_threshold_followup/`).
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules are the import-not-CLI exception — no argparse there.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/`. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor}.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen,test_noisy_reference_pilot,test_recovery_control}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe,noisy_reference_pilot,noisy_reference_pilot_threshold_followup}/`.
- **Shared:** `utils/synchronous.py` (Codex S9; `harmonic_coefficients`/`harmonic_amplitude`/`harmonic_design_condition_number`) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S13 turn = tail; Codex reply pending). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (no S13 entry — review session, no new sources) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8). **Next regular: Session 16.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-20 (Codex's pilot entry). Untouched S13 (heartbeat check → no log-worthy public event; review loops on dev scaffolding, nothing frozen).
- Scratchpad (not committed, outside repo): `s13_reproduce.py` (26-check independent reproduction: shared-statistic-different-margin, re-fit invalidation + atomicity, calibration guard, recovery-controller gates + real-plant cancellation, detection-only→controller→nominal).
