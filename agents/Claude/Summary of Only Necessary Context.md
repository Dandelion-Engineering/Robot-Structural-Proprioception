# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 18, 2026-07-21 21:17 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 18**; next session I run is **Session 19**.
- **Next regular director progress report: my Session 24.** (Phase-0-close, Phase-1-close, Session-8, and Session-16 regular reports all exist. Session 18 closed no phase and wrote no Claim-Sheet amendment → no trigger. So S19–S23 need NO progress report unless a phase closes or a Claim-Sheet amendment is approved in that session.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 155 tests green** (was 148 end of my S17; Codex's S17 bounded redesign added 7 tests: 5 in `test_bounded_task_contact.py` + 2 in `test_recovery_control.py`).

## Review-cycle state (READ THIS FIRST next session)

- **NO OPEN REVIEW LOOPS BETWEEN THE AGENTS as of end of my S18.** I closed the one loop Codex opened in its S17 turn:
  - **CLOSED (first review):** Codex's development-only **bounded task/contact/controller redesign** (`scripts/utils/task_control.py`, the `command_from_nominal` extension in `utils/recovery_control.py`, `scripts/screen_bounded_task_contact.py`, `tests/test_bounded_task_contact.py` + 2 tests in `tests/test_recovery_control.py`, `utils/__init__.py`, `results/bounded_task_contact_screen/` = summary.json + rows CSV + report, packet runbook new Step 11 + renumbered Steps 12–15, root Live-Run entry). Reproduced independently (scratchpad `s18_bounded_reproduce.py`, my own plant drive of the negative-control + selected planes across all four sources, my own metric + A1-flag recompute) → **approved same state, no edits.** The screen **ADVANCES z=0.200 m** to matched-noisy-information review; nothing frozen.
- **My S18 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-21 21:17 PDT; file now 1227 lines; my header at line 1190, appears once, is physically last). Verified via the transcript hard-gate (pre-write 1186 lines, Codex's handoff at 1186, my header once at 1190, physically last).
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S18 — clean.)
- **Codex's likely next turn (check for a NEW loop it may open in its S18):** the **matched noisy held-decision C1-vs-S information / reference-lifecycle review** on this exact bounded-mechanics condition — replacing the fixed source-correct stand-ins with a real noisy estimator making ONE declared held decision on matched CRN C1/S pairs, disjoint calibration/evaluation roles, no source-correct stand-ins. If Codex opened a new loop → review it (reproduce independently, per my pattern).

## What I did this session (Session 18 — pure cross-review; no new build in my lane)

- **First-reviewed + approved Codex's bounded task/contact/controller redesign.** Independent reproduction (`s18_bounded_reproduce.py`) driving `CablePlant → OnlineSensorSession(C1) → EstimatorRecoveryTaskPolicy` myself across z ∈ {0.100 (neg control), 0.200 (selected)} × {healthy, structure, actuator, sensor}, recomputing every gate with my own logic vs the committed CSV → **zero mismatches**:
  1. **Causal arithmetic (recomputed from scratch):** onset index 500, one-cycle probe 625 steps, first decision step **1136 = 2.272 s** (exact multiple of stride 16 → the single evaluation lands precisely on the scheduled step), n_steps 3000, decision precedes movement start (2.272 < 2.400).
  2. **Negative control z=0.100:** all four sources → 0 contact steps / 0 episodes / 0 force / 0 A1 flags (genuine no-contact baseline).
  3. **Selected z=0.200:** exactly one contact episode per source; peak forces 0.476/2.125/1.946/1.585 N (all < 5 N); first contact 4.618–5.194 s, strictly after decision (2.272) AND movement start (2.400). **All A1 flags zero** — I recomputed 6 of 7 directly from raw truth (`|q|>π`, `|qd|>10`, `max|gauge|>500 µε`, `force>5 N` — the joint-angle flag being exactly S16's blocker, now `|q|max ≈ 0.29–0.36 rad ≪ π`) + confirmed the 7th (tip-workspace) via the plant's own flag column; the plant's full 7-flag audit = 0 in every arm.
  4. **Recovery mechanism (my own nominal-vs-applied recompute):** healthy/sensor leave nominal untouched (0 changed steps); structure/actuator apply from the held decision onward with changed-step counts **1864/1863** (bit-matched) and `first_change_time (2.272) < first_contact_time` → recovery has authority before the safety-relevant window in every action arm.
  5. **Decision + bracket (independent `select_candidate` on committed rows + my own per-plane contact count):** ADVANCE, selected z=0.200, eligible=[0.200], neg-control passes. Bracket opens monotonically: 0.100 none → 0.125 healthy → 0.150 +actuator → 0.175 +sensor → 0.200 all four.
- **Byte-determinism:** regenerated the whole 20-arm screen (default command, 4 workers) to scratch → all 3 artifacts SHA-256 identical to committed AND equal to Codex's reported hashes (summary `82a388c7…`, rows `c2db2393…`, report `ccc1ef5d…`). **Full packet 155 green** on my machine. **No NaN/Infinity** in the JSON. Packet runbook Steps 1–15 consistent (cross-ref to Step 14 updated).
- **Harness-bug honesty (my side, not Codex's):** my first repro pass tripped an assertion because I reconstructed the 3-D workspace-radius flag from the record's 2-D `true_task_output` in the wrong frame (read a false 1.12 m vs the 0.82 m limit; the record doesn't store the 3-D tip). Fixed → recompute the other 6 flags from raw truth + confirm the 7th via the plant column. (Same lesson as my S17 `pair_id` bug: when an independent repro disagrees, suspect my harness first; the decisive checks are raw-truth recompute + byte-identical regeneration.)
- **Two non-blocking forward notes raised (chat §4)** for the matched-noisy-information gate — both continuing my S17 note #1:
  1. **Recovery now *precedes* contact, but this screen doesn't yet let it *change* the outcome.** Every arm (healthy no-action, structure/actuator action) lands at one bounded episode / forces < 5 N / zero flags → the seam can *express* source-specific action early (the prerequisite), but a correct diagnosis isn't yet *needed* for the outcome. For the eval-sized C1-vs-S comparison to be *able* to show a control/safety advantage, the task/contact condition must carry enough dynamic range that a wrong/absent diagnosis produces a measurably worse `J_5s`/safety outcome than the correct one. Design that sensitivity in deliberately.
  2. **The "recovery precedes contact" property depends on the held decision being confident *and* correct — which a real noisy estimator won't always be.** The action gate (`_confident_source`: not abstained, unique argmax, p≥0.5, finite severity-uncertainty ≤0.25) will sometimes withhold or fire on a wrong location once the fixed stand-in is replaced. The next gate should report the *action-gate firing rate* (correct-confident / false-confident / abstain) at the single held decision, not just detection/attribution accuracy — that firing behavior is what determines whether the safe-mechanics property survives real information.
- Live-Run README heartbeat → left unchanged (Codex already logged the bounded-redesign advancement in its S17 public entry; a confirming internal review is not a new public event — same lean-log call as my S15/S16/S17). No new references (review/reproduction session, no external sources read).

## The single most important things to do next session (Session 19)

1. **Check Codex's S18 turn** in the active Phase-2 chat for a NEW review loop (the matched noisy held-decision C1-vs-S information/lifecycle review on the bounded condition). If Codex handed something for review → review it, reproduce independently, approve same-state or edit-and-hand-back.
2. **No progress report due S19** (next regular = S24; only a phase close or approved amendment would add one).
3. **When I next touch `estimator.py`** (the learned head): fold in the carried `null_std` floor consistency nit (`CoefficientReferenceDetector.update` floors at `_EPS` → make it `1e-3` to match `WindowNoveltyDetector`). Forward fix, not a reopen.
4. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.

**Do NOT freeze a partial config.** Open freeze items (all still open): the **matched noisy held-decision C1-vs-S information/lifecycle review** (Codex's next, on the bounded condition); validation-sized healthy threshold calibration (≥~100 values) **with per-suite probability calibration** (S14 forward point); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; **the reference-lifecycle choice** (single held decision — now prototyped safely by the bounded screen — vs. a temporal model over the full post-probe trajectory); the **bounded task/contact/controller profile** (`z=0.200 m` + PD gains `(0.05,0.03)`/`(0.005,0.003)` + torque limits `(0.20,0.10)` + timing are *dev candidates* from the S17 screen, NOT frozen margins); `W`/stride (**768/16** pilot-advanced, still not frozen); split/leakage/storage/hash audits.

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13; UNCHANGED in S14–S18**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`, suite-agnostic over the 18-col registry. Constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin as per-feature z-scores; score std floored at `1e-3`. Latches `detection_time_s` after `persistence` consecutive over-threshold windows.
- **`CoefficientReferenceDetector`** (my S12 build; Codex's S13-approved corrections in force): second interpretable detection rung; scores joint healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic the pilots import). Detection-only, abstains on type. `fit_reference` atomic; on re-fit invalidates threshold + resets latch. `calibrate_threshold` FAILS LOUD below `ceil(min_tail/far)` (≥100 at far=0.05). **NIT to fold in next estimator touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` → float to `1e-3`.
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (diagnosis→control socket; recovery command injected as `(EstimatorOutput, step, time)->command`; runs estimator every `stride`, **ZOHs the estimator OUTPUT but calls `recovery_command` EVERY step** — so a per-step nominal can be recovered every step; verified S18). Calls `estimator.reset()` at construction.
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal (validation owns the frozen value).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze; need torch + confirmatory data. Do NOT ship untrained shells.

`Reproducibility Packet/tests/test_recovery_seam.py` (**built my S14; Codex's S14 detection-time-latch fix; jointly approved S15; UNCHANGED S16–S18**): shared end-to-end mechanism regression driving `CablePlant → OnlineSensorSession → EstimatorCommandPolicy → GainScheduledRecoveryController` over multiple steps via `run_online_rollout`. 4 tests. Asserts applied/delivered torque only; labeled NOT a `J_5s`/safety result.

`Reproducibility Packet/scripts/utils/metrics.py` + `utils/stats.py` (**jointly approved through S11; unchanged S12–S18**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` w/ matched-`[T,7]` guard — the 7th flag `tip_contact_force_exceeded` becomes live once contact scenarios exist; the S17 bounded screen confirmed the 7th flag stays 0 at z=0.200 because peaks < 5 N, and now ALSO that the joint-angle flag stays 0 because the bounded task keeps `|q|max ≈ 0.36 rad ≪ π`). `stats.py` = crossed pair×seed paired hierarchical bootstrap. Eval **driver** (argparse CLI) owns the exact `[t_c, t_c+5 s]` slice — build once frozen data layout exists.

## Codex's lanes — current state (jointly approved)

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig` (S12; my approval S13). **S17: added `command_from_nominal(output, nominal_command)`** (my approval S18) — applies diagnosis-conditioned recovery to an *externally supplied deployable nominal command* (copies it, so it can't mutate the caller's input); the legacy `__call__` now generates the old time-indexed `commanded_torque` nominal and delegates to it (backward-compatible). `_confident_source` gate = not abstained AND unique argmax==source AND p≥0.5 AND **finite** uncertainty ≤0.25. Actions: structural → 0.75 global derate; attributed actuator → prob-weighted inverse-gain at joint, capped 2×. Detection-only rungs can't trigger it. Recovery torque_abs_limit `(1.0, 0.5)` (larger than the task controller's `(0.20,0.10)` so compensation has headroom).

`Reproducibility Packet/scripts/utils/task_control.py` (**Codex S17; my approval S18**) — the bounded closed-loop task lane:
- **`BoundedTaskProfile`**: smooth finite joint-reference excursion (rest → target `(0.30,0.30)` rad by 3.0 s → hold → back to rest by 5.0 s → stays 0 to 6.0 s); `joint_reference(t)` cubic smoothstep; `task_reference(t)` = rigid planar 2-link FK image of the joint reference (matches the plant's rest reference `[0.8,0]` at t=0). Timing: movement_start 2.40, transition 0.60, hold_end 4.40, return_end 5.00.
- **`ObservedJointControllerConfig`** (kp `(0.05,0.03)`, kd `(0.005,0.003)`, torque limits `(0.20,0.10)`) + **`ObservedJointPDController`**: reads ONLY delivered `q_obs`/`qd_obs` (ZOH newest valid sample per joint), never privileged state → deployable. Low-authority by necessity (a high-gain PD destabilized the flexible cable plant; those scratch trials not recorded as artifacts).
- **`EstimatorRecoveryTaskPolicy`**: composes the task PD controller + `EstimatorCommandPolicy` (diagnosis cadence) + recovery. Each step: compute nominal PD command → store as `_current_nominal` → call the diagnosis policy (whose `recovery_command` = `_recover` applies `command_from_nominal(output, _current_nominal)` every step) → records nominal + applied. This is the FIRST closed-loop task driving *active* recovery onto a live per-step nominal.

`Reproducibility Packet/scripts/utils/cable_mechanics.py` + `utils/cable_plant.py` + `make_mujoco_plant_trace.py` — the plant lane, **+ endpoint-contact extraction (Codex S14; my approval S15).** `CableModelConfig` has `endpoint_contact_enabled` (default **False**) + `endpoint_contact_plane_z_m` + `tip_contact_force_limit_n` (5.0). When enabled: adds one plane + explicit `<pair>` between distal geom + the plane; sums `‖mj_contactForce‖` → `tip_contact_force_n`, sets `tip_contact_active`, drives the 7th safety flag. **A1 safety flags** (`_safety_flags`) = `|q|>π` (angle, per joint), `|qd|>10` (speed, per joint), 3-D tip radius > 0.82 from base `[0,0,0.5]` (workspace), `max|gauge|>500 µε`, `contact_state[0]>5 N` (contact) — all from privileged truth. **The 3-D workspace flag is NOT reconstructable from the record's 2-D `true_task_output` — recompute the other 6 from raw truth + trust the plant column for the 7th (my S18 harness lesson).**

`Reproducibility Packet/scripts/screen_bounded_task_contact.py` + `tests/test_bounded_task_contact.py` + `results/bounded_task_contact_screen/` (**Codex S17; my first-review approval S18 → jointly approved**). The bounded redesign screen; runs `z={0.100,0.125,0.150,0.175,0.200} × {healthy,structure,actuator,sensor}` for 6.0 s. `plant task_torque_scale=0.0` (task now comes from the PD policy, not the plant's `commanded_torque`). Key classes: **`SingleDecisionHoldEstimator`** (evaluates the inner estimator exactly once at `first_decision_step`, holds causally, healthy before; `classification_evaluations==1`) + **`FixedSourceStandIn`** (fixed source-correct one-hot diagnosis — mechanism instrument, NOT a trained estimator: structure loc1/sev0.50, actuator loc1/sev0.70, sensor loc0/sev0.05). Selection contract: lowest plane above an all-source no-contact control where every source gives exactly 1 post-decision contact episode, ≥5 active steps, ≤5% duty, force <5 N, zero A1 flags, correct held source, 1 classification eval, recovery-before-contact for action sources, no pre-decision recovery. **Decision: `ADVANCE_BOUNDED_TASK_CONTACT_PROFILE_TO_MATCHED_INFORMATION_REVIEW`, selected z=0.200 m, z=0.100 negative control.** Development evidence only, byte-deterministic; fixed stand-ins → NO attribution / recovery-advantage / suite result / frozen config.

`Reproducibility Packet/scripts/run_noisy_reference_pilot.py` (**Codex S11; jointly approved**). Provides `fit_reference`, `evaluate_reference`, `predict_fault`, `causal_window`, `project_observed_suite` (S→C1), `generate_plant_records`, `fault_specs`, `CoefficientReference`. **`fault_specs` reused by the bounded screen** (physical faults plant-side, sensor fault observation-side).

`Reproducibility Packet/scripts/screen_optional_contact_profile.py` + tests + results (**Codex S15; my approval S16**). Open-loop ascending-plane screen (advanced z=0.100 m as a dev candidate over the 2.274 s horizon). Its `count_contact_episodes` is **reused by the bounded screen**. Superseded on the long horizon by the matched pilot (S16) → bounded redesign (S17).

`Reproducibility Packet/scripts/run_matched_contact_pilot.py` + tests + results (**Codex S16; my approval S17**). The first matched contact-enabled C1/S pilot; **BLOCKED all three gates** (8.3% healthy false alarms; single-decision prototype drift to actuator; z=0.100 open-loop joint-angle violations over onset+5 s). The block scoped Codex's S17 bounded redesign. **Now superseded** by the bounded redesign for the mechanics/lifecycle question (runbook reads it "blocked").

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — used by Codex's mechanics safe-probe screen, the detector-floor analysis, my estimator's synchronous feature, and my `CoefficientReferenceDetector`. The noisy-reference + matched-contact pilots import `synchronous_coefficient_vector` + `coefficient_reference_distance` from `utils.estimator` (one canonical definition). The rung shares the pilots' **score statistic**, NOT their margin/decision rates (threshold/reference/persistence own those — settled S13).

The **recovery seam** has both ends jointly exercised: my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController`, control boundary pinned end-to-end by `test_recovery_seam.py`. Detection-only floors (residual_baseline, CoefficientReferenceDetector, WindowNoveltyDetector) correctly stay nominal through the seam. **`detection_time_s` semantics are consistent everywhere** (latch-once at first detection; NaN before). **The bounded screen (S17) is the first to drive active recovery onto a live per-step closed-loop nominal** — and it establishes the *mechanism* that the matched pilot lacked: recovery has authority before contact. What it does NOT yet establish (my S18 forward notes): that a correct diagnosis *changes* the outcome, or that a real estimator can supply it.

**Contact + the 7th safety flag:** contact reaches deployable suites only through motion/strain (contact_state is privileged-only, confirmed S15). In closed loop it is NOT suite-invariant (S vs C1 issue different recovery → different endpoint trajectory → different contact). **Design constraint (my S15 note, honored by the matched pilot AND the bounded screen): apply the contact profile identically across each matched C1/S CRN pair**, so any 7th-flag diff is attributable to recovery. In the bounded screen the 7th flag stayed 0 (forces < 5 N) AND the joint-angle flag stayed 0 (bounded task keeps `|q|max ≈ 0.36 ≪ π`) → the S16 blockers are both cleared by the redesign.

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50 % task torque** → S11 noisy-deployable pilot with healthy calibration reference → advanced **task 0.50 / probe 0.05 N / W=768 / stride=16** → S12 built `CoefficientReferenceDetector` + recovery-controller floor → S13 residual/linear-sysID baseline → S14 detect→(attribute)→compensate loop pinned end-to-end in prototype + endpoint-contact extraction → S15 both S14 loops approved + optional-contact profile screen (z=0.100 m dev candidate) → S16 matched contact-enabled pilot → **BLOCK all three gates** → S17 I reproduced + approved the block → **Codex's S17 bounded redesign clears the mechanics/lifecycle block (bounded PD task + single held decision + contact after decision) → ADVANCE z=0.200 m → S18 I reproduced all gates independently + approved (jointly approved).** The *attribution* middle (trained head) stays deliberately post-freeze.
- **The deployable S advantage IS the structural fault** (S12, re-confirmed by the matched pilot): S detects link-stiffness loss at ~100 % where C1 is structurally blind (~21 %); C1 handles actuator/sensor fine. The gauges add the structural-deformation observability C1 lacks. Carry into the confirmatory S-vs-C1 framing.
- **Why the bounded redesign is a mechanics/lifecycle ADVANCE, not a research result:** it uses *fixed source-correct stand-ins*, so it proves only that the controller/contact/lifecycle *seam* can safely express one source-specific action before contact for every source. It does NOT show a real estimator supplies those labels, that S beats C1, or that recovery improves control. The next gate (Codex's) is the matched *noisy* held-decision C1-vs-S review that replaces the stand-ins.
- **Honesty bounds (keep loud):** (1) the deployable floors are **detection**, NOT attribution; (2) thresholds are max-LOO on too-few seeds — validation needs ≥~100 healthy cal values, calibrated per-suite; (3) reference conditioning assumes a scheduled, phase-reset probe + ONE held decision — unscheduled phase drift + probe-band thermal + faults developing after the held decision remain open; (4) **recovery now precedes contact (S17 fix), but the screen doesn't yet let recovery *change* the safety outcome** (my S18 forward note #1 — the eval condition must carry dynamic range so a correct diagnosis measurably helps); (5) the "recovery precedes contact" property depends on a *confident + correct* held decision, which a real noisy estimator won't always give (my S18 forward note #2 — report the action-gate firing rate).

## The headline experiment design (both seam ends pinned in prototype — carry this)

The "does attribution improve control" comparison is wired at the diagnosis→control seam, its control semantics pinned by `test_recovery_seam.py`, and its *task/contact mechanics* now pinned by the bounded screen (bounded PD task, one held decision, contact after decision). Post-freeze arms: **detection-only rungs → nominal** (control floor); **learned attribution head → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation** (adapt-without-attribution arm); **oracle → ceiling**. Scored by `J_5s` reduction + no-safety-regression on frozen confirmatory data. **S18 sharpening (forward notes): the eval condition must (a) give a correct diagnosis measurable leverage over the outcome — not just let it act early — and (b) report the action-gate firing rate under real noisy information, since the safe-mechanics property depends on a confident+correct held decision.** The trained head lands into a socket whose control + task/contact mechanics are already fixed.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force. Safety computed from **privileged truth**. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. **`CablePlant`'s optional endpoint-contact profile (disabled by default → `[0,0]`, fails loud if `ncon≠0`) populates `contact_state` from real `mj_contactForce` truth + drives the 7th flag when enabled; z=0.200 m is a bounded-screen dev candidate (S17) that clears all seven flags over onset+5 s.**
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState`/`PrivilegedRecord` (lossless; fields incl. `q_true[T,2]`, `qd_true`, `gauge_true[T,4]`, `contact_state[T,2]`, `true_task_output[T,2]` (planar tip), `safety_flag[T,7]`); `observable_sources()` expose only the measurable subset (**contact_state + true 3-D tip NOT in it — privileged-only**). **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` past-only/masked/bounded). **§D** labels/outputs/causality/leakage (`detection_time_s` = time change first flagged, NaN before; automated leakage test — build post-freeze). **§E** storage. **§F** frozen constants (values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.
- **Sensor RNG is keyed on `(sensor_seed, pair_id, channel, stream)`** — the `pair_id` string is load-bearing for reproducibility (my S17 harness bug proved it). The bounded screen's `pair_id = f"bounded-task-contact-{plane_z:.3f}-{source}"`. Match pair_ids exactly when reproducing.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95 % excludes zero; every source-class recall diff lower-95 % above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10 %** (paired excludes zero; **no safety regression** — `safety_regression_delta` upper-95 % ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (the synchronous finding + safe-probe + noisy-reference pilot are the way out, pending validation-sized calibration + the now-cleared contact/lifecycle mechanics).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop; bounded-excitation envelope; safe-probe co-design + A1; noisy-reference pilot S11), virtual-gauge extraction, excitation design, **interpretable residual/linear-sysID baseline (S13)**, **recovery controller (S12) + external-nominal composition (S17)**, **contact/safety + real endpoint-contact extraction (S14)**, **optional-contact profile screen (S15)**, **matched contact-enabled C1/S pilot (S16)**, **bounded task/contact/controller redesign + single-decision lifecycle (S17; my approval S18)**, **the matched noisy held-decision C1-vs-S information/lifecycle review (next)**, evaluation-sized closed-loop controller comparison (pending).
- **Claude (me):** fault-injection + sensor-realism model (S6), two-layer evaluation harness + metrics + stats (S7, safety gate S10, pairing guard S11), diagnosis-estimator front + window contract + oracle + seam adapter (S8; synchronous feature S10; cos/sin/amplitude S11; coefficient-distance rung + 768/16 window S12), synchronous-detection floor analysis (S9–S10), **shared recovery-seam regression `test_recovery_seam.py` (S14; approved S15)**, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py`**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (both ends pinned end-to-end in prototype by the seam test; task/contact mechanics pinned by the bounded screen).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/` (e.g. `..\..\venv\Scripts\python.exe screen_bounded_task_contact.py`). **Full packet: 155 tests green (S18).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise). When running my scratchpad reproduction scripts, run them **from `Reproducibility Packet/`** (they anchor the packet root via cwd). The bounded screen's default run is ~187 s (20 arms, 4 workers).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (incl. `results/optional_contact_profile_screen/`, `results/matched_contact_enabled_pilot/`, **`results/bounded_task_contact_screen/`**). Root has a `tmp/` (ignored) and MuJoCo writes `MUJOCO_LOG.TXT` at the root — check it's ignored before committing.
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules + `tests/` are the import-not-CLI exception.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py` + results; **shared seam test `tests/test_recovery_seam.py`**. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor,recovery_seam}.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control,residual_baseline,task_control}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot,screen_optional_contact_profile,run_matched_contact_pilot,screen_bounded_task_contact}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen,test_noisy_reference_pilot,test_recovery_control,test_residual_baseline,test_optional_contact_profile,test_matched_contact_pilot,test_bounded_task_contact}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe,noisy_reference_pilot,noisy_reference_pilot_threshold_followup,optional_contact_profile_screen,matched_contact_enabled_pilot,bounded_task_contact_screen}/`.
- **Shared:** `utils/synchronous.py` (Codex S9) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S18 turn = tail, 1227 lines; **no open review loops between agents**). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (**no S18 entry — review/reproduction session, no new external sources read**) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8, Session 16). **Next regular: Session 24.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-21 (Codex's bounded-redesign advancement). Untouched S18 (heartbeat check → the advancement is already logged by Codex; a confirming review is not a new public event).
- Scratchpad (not committed, outside repo): `s18_bounded_reproduce.py` (independent gate reproduction; note the workspace-flag frame lesson — recompute 6 flags from raw truth, trust the plant column for the 3-D 7th).
