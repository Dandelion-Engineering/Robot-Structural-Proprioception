# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 12, 2026-07-20 12:08 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 12**; next session I run is **Session 13**.
- **Next regular director progress report: my Session 16.** (Phase-0-close, Phase-1-close, and Session-8 reports exist; they don't reset the per-agent counter. Session 12 closed no phase and wrote no Claim-Sheet amendment, so no progress-report trigger.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 113 tests green** (was 107; +6 estimator tests I added S12).

## Review-cycle state (READ THIS FIRST next session)

- **THE PILOT REVIEW LOOP IS CLOSED.** In **S12 I genuinely reviewed Codex's S11 noisy-reference pilot** (`scripts/run_noisy_reference_pilot.py` + tests + `results/noisy_reference_pilot/` + `results/noisy_reference_pilot_threshold_followup/` + runbook/public wording), reproduced its load-bearing properties independently, and **approved the exact state Codex approved.** Both agents approved the same state → closed.
- **I OPENED A NEW LOOP in S12:** I built the `CoefficientReferenceDetector` rung in `utils/estimator.py` (+ moved `RECOMMENDED_WINDOW` to 768/16) and **handed that increment to Codex with my explicit approval for genuine review.** **Codex has NOT replied yet — check next session.** If Codex edits my files, close the loop with a genuine owner re-review (both feedback AND edits) and approve the same state, or edit and hand back.
- **My S12 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-20 12:02 PDT). Verified: my S12 header appears once and is last.
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S12 — clean.)

## What I did this session (Session 12)

- **Reviewed + approved Codex's noisy-reference pilot**, with independent reproduction (scratchpad `s12_reproduce_pilot.py`, exercising Codex's pilot functions on the advancing task-0.50/probe-0.05 N cell — NOT a re-run of Codex's committed grid):
  1. **Projected C1 == native C1, bit-for-bit** across all 18 channels (values/masks/measurement+availability times/latency/suite masks). The efficient "generate S once, project C1 by deleting the gauge role" trick is a genuine matched C1 under CRN; carries no S-only value/timing across.
  2. **W=512 coefficient vectors identically zero** — inert negative control holds (sub-cycle → feature gate withholds).
  3. **S advantage is the STRUCTURAL fault specifically.** C1 detected actuator (motion) + sensor (encoder-bias) faults but detected **structure at 0.0%**; S detected it (~92% at 8 cal seeds; Codex's 32-seed follow-up = 97.9%). So "C1 min detection 0%" = the gauges add the structural-deformation observability the current/IMU suite lacks. **This is the project thesis in a number — carry it into the confirmatory S-vs-C1 framing: the S detection advantage is concentrated on the structural fault, not spread evenly.**
  4. **BLOCK pathology reproduced:** 8 cal seeds → S false alarm 0.333 (the undersized-tail failure Codex preserved as a BLOCK).
  Calibration/held-out seeds disjoint by construction; threshold fit on calibration LOO only, never retuned on held-out; follow-up used new non-overlapping seeds (32 cal / 48 held-out, base 5000).
- **Built the permanent `CoefficientReferenceDetector` rung** (my S11 §3 proposal, Codex's invitation) in `utils/estimator.py` — see My lanes below.
- **Two forward points raised in chat (§2 of my S12 turn), non-blocking:**
  - (a) The advancing (follow-up) report omits the threshold-resolution caveat the BLOCK report carries. At **32 cal seeds, `np.quantile(loo, 0.99, method="higher")` == the calibration MAX** (index 31), so the follow-up's 0.7%/2.1% false alarms rest on a max-based threshold with single-event (1/48) resolution. Validation needs ≥~100 healthy cal values so the (1−far) quantile is a genuine quantile. **I encoded this as a hard guard in my rung** (`calibrate_threshold` fails loud below `ceil(min_tail/far)` values).
  - (b) The pilot's `summary.json` grid doesn't record the base `--seed` (only counts) — a small packet self-containment nit (Codex's lane, forward fix).

## The single most important things to do next session (Session 13)

1. **Check Codex's reply to my S12 turn** in the active Phase-2 chat. Likely: (a) acceptance/review of my `CoefficientReferenceDetector` + 768/16 increment (if Codex edits my files → close the loop with genuine re-review); (b) response to the two forward points; (c) possibly Codex's next plant-lane build (interpretable residual/linear-sysID baseline, recovery controller) or the validation-sized threshold calibration.
2. **If Codex reviewed/edited my estimator increment:** genuine owner re-review of both the feedback and edits; approve same state or edit + hand back.
3. **Deferred, still waiting on config freeze + confirmatory data** (do NOT build early): the learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; and (post-freeze, needs real multi-run storage) the §D deployable-loader leakage test + whole-trajectory/fault-setting split audit.

**Do NOT freeze a partial config.** Open freeze items (all still open): validation-sized healthy threshold calibration (≥~100 values — now with a home: my rung's `calibrate_threshold` guard refuses smaller); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; contact-enabled cases; `W`/stride (**768/16 now proposed**, pilot-advanced, still not frozen).

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**my S12 increment — handed to Codex, loop open**):
- **Window front-end `WindowFeatureExtractor(window_steps=W, probe_frequency_hz=0.8)`** (jointly approved through S11): fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, **sync_cos, sync_sin, sync_amplitude**, valid_fraction]` (per-channel-time slopes AND per-channel synchronous **cos/sin/amplitude**, ≥1-cycle + ≥8-sample gated, via shared `utils.synchronous.harmonic_coefficients`), suite-agnostic over the 18-col registry. Feature layout constants unchanged: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`** (approved through S11): interpretable detect + calibrated-abstention; consumes cos/sin **generically** as per-feature z-scores.
- **NEW S12 — `CoefficientReferenceDetector`** (a `DiagnosisEstimator`): the second interpretable detection rung, scoring the **joint** healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` of a live window to a healthy calibration reference — the deployable analog of Codex's mechanics safe-probe screen (uses the coefficients *jointly*, the gap I flagged S11 §3). **Detection-only, abstains on fault type** (spreads non-healthy mass uniformly; NOT the pilot's nearest-centroid attribution, which is a pilot instrument I deliberately did not deploy). `fit_reference(healthy_windows)` → mean/scale + LOO null scores; `calibrate_threshold(false_alarm_rate=, min_tail_count=)` freezes the (1−far)-quantile threshold and **FAILS LOUD when the cal set is too small to resolve the tail** (needs ≥ `ceil(min_tail/far)`, e.g. ≥100 at far=0.05) — the pilot's BLOCK as a hard precondition; a directly-set `detect_threshold=` is a labeled pilot-proposal escape hatch. `p_class`/`unknown_score` rendered in sigma-above-healthy-null units; persistence latch for `detection_time_s`.
- **NEW S12 — module-level `synchronous_coefficient_vector(record, extractor)` + `coefficient_reference_distance(vector, mean, scale)`** — the canonical statistic. **Pinned bit-equal to the pilot's copies** by `test_coefficient_statistic_matches_pilot_definition`, so the deployed rung's margin IS the pilot's margin (amplitude → coefficient pair → joint coefficient distance, one statistic). The pilot script still has its own copies (Codex's lane); dedup by re-pointing the pilot at the estimator is a forward fix I flagged for Codex (I did NOT touch the pilot).
- **`OracleInterface(onset_time_s)`** (approved S9): allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (the diagnosis→control socket; Codex's recovery controller is the injected command).
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** as of S12 (was 640/8) — pilot-advanced (only S cell clearing the ≤5% worst-alignment false-alarm screen; 97.9% detect; 100% attribution; 0% C1). **Still a pilot proposal, single-event-thin margins, validation owns the frozen value.** Default `WindowFeatureExtractor` window is now 768 (inert for existing tests: per-column stats use valid samples only; `valid_fraction` constant across equal-length records → ~0 z-contribution).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze; need PyTorch + confirmatory data. Do NOT ship untrained shells or report performance before frozen data.

`Reproducibility Packet/scripts/utils/metrics.py` (**jointly approved through S11**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` — the last with a matched-`[T,7]`-shape fail-loud guard). `utils/stats.py` = crossed pair×seed paired hierarchical bootstrap. The eval **driver** (argparse CLI) still owns the exact `[t_c, t_c+5 s]` control-grid slice before calling the safety point metrics — to build once the frozen data layout exists.

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — `harmonic_coefficients` (returns `[cos,sin]`) + `harmonic_amplitude` (= their norm) + `harmonic_design_condition_number` — used by (a) Codex's mechanics safe-probe screen `screen_synchronous_safe_probe.py`, (b) the detector-floor analysis `analyze_synchronous_detection_floor.py`, (c) my estimator's synchronous feature, and now (d) **my `CoefficientReferenceDetector` via `synchronous_coefficient_vector`**. The screen measures the fault-minus-reference **coefficient-vector distance** `‖coeff(fault)−coeff(reference)‖`; the deployable rung measures `‖coeff_live − coeff_ref‖` standardized against a healthy calibration null (the privileged matched reference → a calibration model). Codex's **pilot** is the bridge that showed this deployable statistic works on noisy observations (S detects structure, C1 doesn't).

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε per-sample mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50% task torque**, clears the corrected detector threshold by 2.22× AND stays in the safety envelope → S11 pilot on **noisy deployable observations** with a **healthy calibration reference** → advanced **task 0.50 / probe 0.05 N / W=768 / stride=16** (S worst per-fault detect 97.9%, prototype attribution 100%, healthy false alarms 0.7% pooled / 2.1% worst; matched C1 min detect 0%).
- **The 2.22× is a CLEAN PRIVILEGED margin, NOT a deployable-estimator margin** (settled S10–S11). The pilot measured the deployable margin on noisy per-suite observations vs a healthy reference. **S12 sharpened what the deployable S advantage IS: the structural fault** (S detects link-stiffness loss where C1 is blind; C1 sees actuator/sensor faults fine).
- **Honesty bounds (keep loud):** (1) this is **detection**, NOT attribution (classifying structure-vs-actuator-vs-sensor needs the trained head reading differential shape/phase across the 4 stations; the pilot's nearest-centroid is a pilot instrument only); (2) the follow-up's false-alarm rates are single-event-thin (1/48) and the 32-cal "99th percentile" == the max — **validation needs ≥~100 healthy cal values**; (3) reference conditioning assumes a scheduled, phase-reset probe — unscheduled phase drift + probe-band thermal interference remain open; (4) the linear thermal ramp is a trend-rejection check, not a bound on nonlinear/probe-band thermal.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force (`saturation_flag[2]` separate). Safety computed from **privileged truth** across every scenario, never a corrupted observed channel. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10 rad/s`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. `CablePlant` emits `[0,0]` contact (collision disabled) and **fails loud** if `data.ncon != 0`.
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState` (lossless); `observable_sources()`/`observable_step_sources()` expose only the measurable subset. **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` = past-only, availability-masked, bounded window). **§D** labels/outputs/causality/leakage (automated leakage test fails build if a deployable loader reaches identity/privileged/label/other-suite arrays — to build post-freeze). **§E** storage. **§F** frozen constants (f_ctrl, dt, W, stride — values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95% excludes zero; every source-class recall diff lower-95% above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10%** (paired excludes zero; **no safety regression** — wired via `safety_regression_delta`, upper-95% ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (the live risk; the synchronous finding + safe-probe candidate + the noisy-reference pilot are the way out, pending validation-sized threshold calibration).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop; bounded-excitation envelope; safe-probe co-design + A1 implementation; **noisy-reference pilot instrument S11**), virtual-gauge extraction, excitation design, interpretable residual/linear-sysID baseline (pending), recovery controller (pending), contact/safety implementation + real endpoint-contact extraction (pending, before optional-contact pilots).
- **Claude (me):** fault-injection + sensor-realism model (done S6, approved S7), two-layer evaluation harness + metrics + stats (done S7, corrected+approved S8, safety gate S10, pairing guard approved S11), diagnosis-estimator front + window contract + oracle + seam adapter (done S8, approved S9; synchronous feature S10, corrected to cos/sin/amplitude + approved S11; **coefficient-distance rung + 768/16 window S12, handed to Codex**), synchronous-detection floor analysis (done S9, review closed S10), matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py` shared harmonic statistic**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (my `EstimatorCommandPolicy` is the socket; Codex's recovery controller is the injected command).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/`. **Full packet: 113 tests green (S12).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (`results/synchronous_detection_floor/`, `results/synchronous_safe_probe/`, `results/bounded_burst_sensitivity/`, `results/noisy_reference_pilot/`, `results/noisy_reference_pilot_threshold_followup/`).
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules are the import-not-CLI exception — no argparse there.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py` (**+ S12 coefficient-distance rung**); synchronous floor `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/`. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor}.py`.
- **Codex's plant lane:** `utils/{cable_mechanics,cable_plant,online_loop}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen,test_noisy_reference_pilot}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe,noisy_reference_pilot,noisy_reference_pilot_threshold_followup}/`.
- **Shared:** `utils/synchronous.py` (Codex S9; `harmonic_coefficients`/`harmonic_amplitude`/`harmonic_design_condition_number`) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S12 turn = tail; Codex reply pending). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (no S12 entry — review+implementation session, no new sources) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8). **Next regular: Session 16.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-20 (Codex's pilot entry). Untouched S12 (heartbeat check → no log-worthy public event; the pilot entry already covered the convention advance).
- Scratchpad (not committed, outside repo): `s12_reproduce_pilot.py` (independent pilot reproduction: bit-exact C1 projection, W512 inert, S-detects-structure-C1-blind, BLOCK pathology).
