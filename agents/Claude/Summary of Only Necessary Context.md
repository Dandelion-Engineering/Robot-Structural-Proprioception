# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 15, 2026-07-20 20:55 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 15**; next session I run is **Session 16**.
- **Next regular director progress report: my Session 16.** (Phase-0-close, Phase-1-close, and Session-8 reports exist; they don't reset the per-agent counter. Session 15 closed no phase and wrote no Claim-Sheet amendment → no progress-report trigger. **So my Session 16 must write a regular progress report in addition to normal session work** — `Playbooks/research-progress-report.md`, at the Accessible-Piece bar, into `agents/Claude/Progress Reports/`.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 139 tests green** (was 138 end of my S14; Codex's S14 added the 1-test endpoint-contact regression).

## Review-cycle state (READ THIS FIRST next session)

- **NO OPEN REVIEW LOOPS BETWEEN THE AGENTS as of end of my S15.** I closed both loops Codex opened in its S14 turn:
  1. **CLOSED (owner re-review):** Codex's detection-time-latch correction to my `tests/test_recovery_seam.py`. Codex found my `FixedDiagnosisStandIn` re-stamped `detection_time_s` with the current decision time every step instead of latching first-detection (schema §D: "time change was *first* flagged"); it added a reset-able first-detection latch + a regression. I owner-re-reviewed **both diagnosis and implementation**, reproduced independently (9-check script), and **approved the same state**. The latch idiom now matches the three real detectors; note the defect passed `validate()` because `detection==now` is a valid special case of `detection≤now`, so a fixture assertion was the only possible guard.
  2. **CLOSED (first review):** Codex's development-only **endpoint-contact extraction** (Codex's plant lane). Reproduced independently (13-check script) → approved same state, no edits.
- **My S15 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-20 20:53 PDT; file now 940 lines; my header at line 907, appears once, is last). Verified.
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S15 — clean.)
- **Codex's likely next turn (check for a NEW loop it may open in its S15):** the **evaluation-sized closed-loop controller comparison** (separating exact actuator-delivery compensation from `J_5s` tracking recovery + privileged safety), and/or the design + screen of an **actual optional-contact profile/grid** across healthy + fault scenarios (the endpoint-contact extraction it just built unblocks this). If Codex opened a new loop → review it (reproduce independently, per my pattern).

## What I did this session (Session 15 — a pure cross-review session, two loops closed, no new build)

- **Reconciled a stale git snapshot again:** prompt showed an old HEAD; live repo was `acba786 Codex Session 14`, working tree clean. Worked against the clean live baseline. (Recurring quirk; live tree + continuity files authoritative.)
- **Owner-re-reviewed Codex's detection-time fix** to `test_recovery_seam.py` (scratchpad `s15_seam_latch_reproduce.py`, 9 checks): reconstructed OLD (buggy) vs NEW (latched) stand-in from scratch, drove both through the real seam. Confirmed: OLD emits the current decision time every step (`[0.000,0.002,…]`), NEW latches 0.0 across all 8 steps; **both pass `validate()`** (so validation can't catch it — the fixture-level fix is the only guard); silent at run level (`EstimatorTrace.detection_time_s` = first-finite = 0.0 for both) but false in the per-step `stack()`/§E array; NEW uses the same latch-once idiom as `WindowNoveltyDetector`/`CoefficientReferenceDetector`; `EstimatorCommandPolicy.__init__` calls `estimator.reset()` so the reset latch-clear is load-bearing. Accepted diagnosis + implementation → **approved same state (loop closed).**
- **First-reviewed Codex's endpoint-contact extraction** (scratchpad `s15_contact_reproduce.py`, 13 checks): drove the enabled/disabled plant myself. Confirmed: enabled arm contacts (4 points at final step); my independent `mj_contactForce` sum reproduces recorded `contact_state[force]` to <1e-12 (0.844396 N — validates the multi-point sum-of-magnitudes since ncon=4>1); every contact is exactly the intended `{endpoint_geom, plane_geom}` pair; the 7th safety flag == `force>limit` elementwise over 100 steps; `tip_contact_active` binary and set exactly where force>0; disabled plant is **genuinely** collision-free (`data.ncon==0`, `[0,0]` every step, flag never trips); **privileged/deployable boundary airtight** — observed channels are only `{q_obs,qd_obs,tau_cmd,current_proxy_obs,imu_obs,gauge_obs}`, `observable_step_sources()` drops contact, `contact_state` is privileged-only (reaches deployable suites solely via motion/strain). Checked friction share of the `condim=3` force = ~0.0% at rest → no edit. **Approved same state (loop closed).**
- **One non-blocking forward point raised (chat §3):** contact truth now feeds the **7th safety flag**, a live input to my Slot-7 `safety_regression_delta` gate. Today all-zero (contact disabled); once contact-enabled confirmatory scenarios exist it becomes live and is **NOT suite-invariant in closed loop** (S vs C1 issue different recovery commands → different endpoint trajectory → different contact events). Legitimate (that's what the gate measures); my matched-`[T,7]` guard (S11) already handles it, no metric change. **Design constraint for Codex's eventual contact grid: apply the contact profile identically across each matched C1/S CRN pair**, so any 7th-flag diff is attributable to closed-loop recovery, not a mismatched contact setup.
- **Full packet 139 green** on my machine; Live-Run README heartbeat check → left unchanged (review session on dev scaffolding, nothing frozen, no public-worthy event — same call Codex made for its S14 contact work).

## The single most important things to do next session (Session 16)

1. **Write the regular progress report** (my Session 16 cadence) in addition to normal work → `agents/Claude/Progress Reports/Progress Report Session 16.md`, per `Playbooks/research-progress-report.md`.
2. **Check Codex's S15 turn** in the active Phase-2 chat for a NEW review loop (eval-sized controller comparison / contact profile+grid screen). If Codex handed something for review → review it, reproduce independently, approve same-state or edit-and-hand-back.
3. **When I next touch `estimator.py`** (the learned head): fold in the carried `null_std` floor consistency nit (`CoefficientReferenceDetector.update` floors at `_EPS` → make it `1e-3` to match `WindowNoveltyDetector`). Forward fix, not a reopen.
4. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.

**Do NOT freeze a partial config.** Open freeze items (all still open): validation-sized healthy threshold calibration (≥~100 values — enforced by both interpretable rungs' calibration guards) **with per-suite probability calibration** (S14 forward point); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; **contact profile/grid** (extraction now exists, but no profile/grid screened or frozen); `W`/stride (**768/16** pilot-advanced, still not frozen).

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13; UNCHANGED in S14–S15**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`, suite-agnostic over the 18-col registry. Constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin as per-feature z-scores; score std floored at `1e-3`. Latches `detection_time_s` after `persistence` consecutive over-threshold windows (the canonical latch idiom the S14 seam-fixture fix now mirrors).
- **`CoefficientReferenceDetector`** (my S12 build; Codex's S13-approved corrections in force): second interpretable detection rung; scores joint healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic the pilot imports). Detection-only, abstains on type. `fit_reference` atomic; on re-fit invalidates threshold + resets latch. `calibrate_threshold` FAILS LOUD below `ceil(min_tail/far)` (≥100 at far=0.05). **NIT to fold in next estimator touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` → float to `1e-3`.
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (diagnosis→control socket; recovery command injected as `(EstimatorOutput, step, time)->command`; runs estimator every `stride`, ZOH between; calls `estimator.reset()` at construction).
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal (validation owns the frozen value).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze; need torch + confirmatory data. Do NOT ship untrained shells.

`Reproducibility Packet/tests/test_recovery_seam.py` (**built my S14; Codex's S14 detection-time-latch fix; jointly approved S15**): shared end-to-end mechanism regression driving `CablePlant → OnlineSensorSession → EstimatorCommandPolicy → GainScheduledRecoveryController` over multiple steps via `run_online_rollout`. 4 tests: localizing-attribution-restores-delivery (+ asserts the latched `detection_time_s==0.0` across the rollout), detection-only-stays-nominal, the divergence, structural-global-derate. `FixedDiagnosisStandIn` now has a reset-able first-detection latch (matches the real detectors). Fixed deployable stand-ins; asserts applied/delivered torque only; labeled NOT a `J_5s`/safety result.

`Reproducibility Packet/scripts/utils/metrics.py` + `utils/stats.py` (**jointly approved through S11; unchanged S12–S15**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` w/ matched-`[T,7]` guard — the 7th flag `tip_contact_force_exceeded` becomes live once contact scenarios exist; see S15 forward point). `stats.py` = crossed pair×seed paired hierarchical bootstrap. Eval **driver** (argparse CLI) owns the exact `[t_c, t_c+5 s]` slice — build once frozen data layout exists.

## Codex's lanes — current state (jointly approved)

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig` (S12; my approval S13). Plugs into my `EstimatorCommandPolicy` as `recovery_command`. Consumes only `EstimatorOutput` + time. `_confident_source` gate = not abstained AND unique argmax==source AND p≥0.5 AND **finite** uncertainty ≤0.25. Actions: structural → 0.75 global derate; attributed actuator → prob-weighted inverse-gain at joint, capped 2×. Detection-only rungs can't trigger it (safety boundary; verified end-to-end by the seam test).

`Reproducibility Packet/scripts/utils/residual_baseline.py` — `LinearResidualAttributionEstimator` + `LinearResidualConfig` (Codex S13; my approval S14). The Claim Sheet Slot-5 interpretable residual/linear-sysID floor: one normalized affine ridge-ARX predictor per suite on healthy `ObservedRecord`s; residual features `[signed_mean, rms, valid_fraction]` per predicted scalar; nearest standardized centroid per class; off-prototype abstention threshold. Detection-oriented — no location/severity (location=-1, severity_uncertainty=inf), so can't trigger active recovery. Suite-generic. Three role-separated stages: `fit_dynamics` (healthy) → `fit_prototypes` (4-class labeled) → `calibrate_unknown_threshold` (4-class known). Re-fit invalidates downstream. Latches `detection_time_s` (same idiom).

`Reproducibility Packet/scripts/utils/cable_mechanics.py` + `utils/cable_plant.py` + `make_mujoco_plant_trace.py` — the plant lane, **+ endpoint-contact extraction (Codex S14; my first-review approval S15 → jointly approved).** `CableModelConfig` gained `endpoint_contact_enabled` (default **False**) + `endpoint_contact_plane_z_m` (default 0.498, a *fixture*, not frozen). When enabled: `model_xml` adds one plane (`contype=0/conaffinity=0`) + one explicit MJCF `<pair>` between the distal endpoint geom `L2_G{point_count-2}` and the plane; `_contact_state()` filters every contact to that exact geom pair (fails loud on any other pair / disabled-but-contact / non-finite / missing handle), sums `‖mj_contactForce(...)[:3]‖` across contact points → `tip_contact_force_n`, sets `tip_contact_active` if ≥1 contact; this drives the **7th privileged safety flag** `tip_contact_force_exceeded` (from privileged truth, no observed-channel path). Default (disabled) still emits `[0,0]` and fails loud if `data.ncon!=0`. CLI `--endpoint-contact-plane-z-m` (omit → disabled). Dev-only: **no contact profile/grid screened, piloted, or frozen.**

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — used by (a) Codex's mechanics safe-probe screen, (b) the detector-floor analysis, (c) my estimator's synchronous feature, (d) my `CoefficientReferenceDetector`. The pilot imports `synchronous_coefficient_vector` + `coefficient_reference_distance` from `utils.estimator` (one canonical definition). The rung shares the pilot's **score statistic**, NOT its margin/decision rates (threshold/reference/persistence own those — settled S13).

The **recovery seam** has both ends jointly exercised: my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController`, with the detection-vs-attribution control boundary pinned end-to-end by `test_recovery_seam.py` (detection-time latch corrected S14, jointly approved S15). Codex's `residual_baseline` and my `CoefficientReferenceDetector`/`WindowNoveltyDetector` are the deployable **detection** floors; all correctly stay nominal through the seam (no location/severity → no active recovery). **`detection_time_s` semantics are now consistent across every estimator and the seam fixture** (latch-once at first detection; NaN before).

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε per-sample mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50% task torque** (clears corrected detector threshold by 2.22× AND stays in safety envelope) → S11 pilot on **noisy deployable observations** with a **healthy calibration reference** → advanced **task 0.50 / probe 0.05 N / W=768 / stride=16** (S worst per-fault detect 97.9%, prototype attribution 100%, healthy false alarms 0.7% pooled / 2.1% worst; matched-C1 min detect 0%) → S12 built the deployable `CoefficientReferenceDetector` rung → S12 Codex built recovery-controller floor → S13 Codex built the residual/linear-sysID baseline → **S13/S14 all detection + recovery floors jointly approved; S14 the detect→(attribute)→compensate loop pinned end-to-end in prototype (mechanism); S15 both S14 loops (seam-fixture latch fix + endpoint-contact extraction) jointly approved.** The *attribution* middle stays deliberately post-freeze (the trained head).
- **The 2.22× is a CLEAN PRIVILEGED margin, NOT a deployable-estimator margin** (settled S10–S11). **The deployed rung shares the pilot's score statistic, not its margin/rates** (settled S13). **The deployable S advantage IS the structural fault** (S12): S detects link-stiffness loss where C1 is blind; C1 sees actuator/sensor faults fine — "matched-C1 minimum per-fault detection 0%" = the gauges add the structural-deformation observability C1 lacks. Carry into the confirmatory S-vs-C1 framing.
- **Honesty bounds (keep loud):** (1) the deployable floors are **detection**, NOT attribution (classifying structure-vs-actuator-vs-sensor needs the trained head; centroids/nearest-prototype are pilot/floor instruments); (2) follow-up false-alarm rates are single-event-thin (1/48) and 32-cal "99th percentile" == the max — **validation needs ≥~100 healthy cal values, calibrated per-suite** (S14 forward point); (3) reference conditioning assumes a scheduled, phase-reset probe — unscheduled phase drift + probe-band thermal interference remain open; (4) the linear thermal ramp is a trend-rejection check, not a bound on nonlinear/probe-band thermal.

## The headline experiment design (both seam ends pinned in prototype — carry this)

The "does attribution improve control" comparison is wired at the diagnosis→control seam, its control semantics pinned by `test_recovery_seam.py`. Post-freeze arms: **detection-only rungs → nominal** (control floor, no compensation — verified end-to-end); **learned attribution head (rung 2) → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation** (adapt-without-attribution arm); **oracle → ceiling** (drives active comp directly, off-seam, reads privileged state). Scored by `J_5s` (5-s post-change tracking-error integral) reduction + no-safety-regression on the frozen confirmatory data. The trained head lands into a socket whose control semantics are already fixed.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force (`saturation_flag[2]` separate). Safety computed from **privileged truth** across every scenario. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10 rad/s`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. **`CablePlant` now has an optional dev endpoint-contact profile (disabled by default → emits `[0,0]`, fails loud if `data.ncon!=0`); when enabled it populates `contact_state` from real `mj_contactForce` truth and drives the 7th safety flag (S14/S15).**
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState` (lossless); `observable_sources()`/`observable_step_sources()` expose only the measurable subset (**contact_state is NOT in it — privileged-only, confirmed in code S15**). **§C** observed record (fixed 18-col registry `{q_obs,qd_obs,tau_cmd,current_proxy_obs,imu_obs,gauge_obs}`, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` = past-only, availability-masked, bounded window). **§D** labels/outputs/causality/leakage (`detection_time_s` = time change first flagged, NaN before, `validate()` requires NaN or `∈[0,decision_time_s]`; automated leakage test fails build if a deployable loader reaches identity/privileged/label/other-suite arrays — to build post-freeze). **§E** storage. **§F** frozen constants (f_ctrl, dt, W, stride — values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95% excludes zero; every source-class recall diff lower-95% above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10%** (paired excludes zero; **no safety regression** — `safety_regression_delta` upper-95% ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (the live risk; the synchronous finding + safe-probe candidate + noisy-reference pilot are the way out, pending validation-sized threshold calibration).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop; bounded-excitation envelope; safe-probe co-design + A1 implementation; noisy-reference pilot S11), virtual-gauge extraction, excitation design, **interpretable residual/linear-sysID baseline (S13; approved S14)**, **recovery controller (S12; approved S13)**, **contact/safety implementation + real endpoint-contact extraction (S14; approved S15)**, contact profile/grid design + screen (pending), evaluation-sized closed-loop controller comparison (pending).
- **Claude (me):** fault-injection + sensor-realism model (done S6), two-layer evaluation harness + metrics + stats (done S7, safety gate S10, pairing guard S11), diagnosis-estimator front + window contract + oracle + seam adapter (done S8; synchronous feature S10; cos/sin/amplitude S11; coefficient-distance rung + 768/16 window S12; Codex's corrections re-reviewed S13), synchronous-detection floor analysis (S9–S10), **shared recovery-seam regression `test_recovery_seam.py` (built S14; jointly approved S15)**, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py`**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController` = both ends pinned end-to-end in prototype by the seam test).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/`. **Full packet: 139 tests green (S15).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (`results/synchronous_detection_floor/`, `results/synchronous_safe_probe/`, `results/bounded_burst_sensitivity/`, `results/noisy_reference_pilot/`, `results/noisy_reference_pilot_threshold_followup/`).
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules + `tests/` are the import-not-CLI exception — no argparse there.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/`; **shared seam test `tests/test_recovery_seam.py` (jointly approved S15)**. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor,recovery_seam}.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control,residual_baseline}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen,test_noisy_reference_pilot,test_recovery_control,test_residual_baseline}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe,noisy_reference_pilot,noisy_reference_pilot_threshold_followup}/`.
- **Shared:** `utils/synchronous.py` (Codex S9) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S15 turn = tail, 940 lines; **no open review loops between agents**). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (**no S15 entry — review/reproduction session, no new sources**) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8). **Next regular: Session 16 (write it).**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-20 (Codex's noisy-reference pilot). Untouched S15 (heartbeat check → no log-worthy public event; pure review of dev scaffolding, nothing frozen).
- Scratchpad (not committed, outside repo): `s15_seam_latch_reproduce.py` (9-check detection-time reproduction) + `s15_contact_reproduce.py` (13-check endpoint-contact reproduction).
