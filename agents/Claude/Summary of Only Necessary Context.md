# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 14, 2026-07-20 19:14 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 14**; next session I run is **Session 15**.
- **Next regular director progress report: my Session 16.** (Phase-0-close, Phase-1-close, and Session-8 reports exist; they don't reset the per-agent counter. Session 14 closed no phase and wrote no Claim-Sheet amendment → no progress-report trigger.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 138 tests green** (was 134 end of Codex S13; my S14 added the 4-test recovery-seam regression).

## Review-cycle state (READ THIS FIRST next session)

- **One loop I CLOSED in S14, one loop I OPENED in S14:**
  1. **CLOSED:** the first review of Codex's new **linear residual / system-ID baseline** (`utils/residual_baseline.py` + `test_residual_baseline.py`, Codex's lane). I reproduced it independently (22-check script) and **approved the same state Codex approved** (no edits) → first-review loop closed. **This baseline is now jointly approved.**
  2. **OPEN (Codex owes the first review):** I built the shared **end-to-end recovery-seam regression** `tests/test_recovery_seam.py` (my `EstimatorCommandPolicy` socket) and handed it to Codex for genuine first review. **Codex has NOT reviewed it yet — check next session.** If Codex approves same-state → loop closes. If Codex edits it → I must genuinely owner-re-review both feedback and edits before it can close.
- **My S14 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-20 19:12 PDT; file now 840 lines). Verified: my S14 header appears once and is last.
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S14 — clean.)
- **Codex's likely next turn:** the first-review of my seam regression, and/or a new lane build — the **evaluation-sized closed-loop controller comparison** (separating exact actuator-delivery compensation from `J_5s` tracking recovery + privileged safety) or **real MuJoCo endpoint-contact extraction** (before optional-contact pilots). If Codex opens a new loop, review it.

## What I did this session (Session 14 — a cross-review + one shared build)

- **Reconciled a stale git snapshot again:** prompt showed HEAD at "Claude Session 11" + dirty tree; live repo was `1260e92 Codex Session 13`, working tree clean. Worked against the clean baseline. (Recurring quirk; live tree + continuity files are authoritative.)
- **First-reviewed Codex's linear residual baseline, reproduced independently** (scratchpad `s14_reproduce.py`, 22 checks, NOT a re-run of Codex's tests):
  1. **ARX + residual math is exactly as claimed** — I reconstructed the normalized affine ARX fit (`[1, x[t-1], mask[t-1], u[t], u_mask[t]]` regressor → per-target ridge normal eqs, unpenalized intercept) and the `[signed_mean, rms, valid_fraction]` residual vector from my own records; coefficients + residual vector match to `max|Δ|<1e-9`. Fit transition count == `Σ(n-1)` (transitions never bridge record boundaries).
  2. **Matched-ablation / leakage boundary is structural** — C1 predicts 0 gauge scalars, S predicts exactly 4; `S.state_labels − C1.state_labels` = exactly the 4 `gauge_obs[i]` (clean nesting, the only difference is the structural channels). `tau_cmd` exogenous-only. C1-record-with-gauge-unmasked rejected; wrong-suite rejected; identity fields (`run_id`/`pair_id`/`config_hash`/`split`) never consumed (garbaged them → residual byte-identical).
  3. **Re-fit lifecycle atomic + self-invalidating** — re-fit replaces model, nulls centroids+threshold; a re-fit that raises leaves the old model intact + scoring; scoring refuses a re-fit reference until recalibrated; tail guard fails loud below `ceil(min_tail/far)` (100 at far=0.05).
  4. **Safety boundary end-to-end** — `update` always emits `location_out=-1`, `severity_uncertainty=inf`, valid §D output; a **confident** structure OR actuator output with `inf` uncertainty stays nominal through the real `GainScheduledRecoveryController` because `_confident_source` requires `np.isfinite(severity_uncertainty)`. (This is the property Codex's own seam smoke — a healthy-only rollout — does NOT exercise.)
  Approved same-state → loop closed.
- **Built the shared recovery-seam regression** `tests/test_recovery_seam.py` (fixed deployable stand-ins, real `CablePlant` multi-step rollout via `run_online_rollout`): (a) localizing actuator attribution → sustained active inverse-gain compensation restoring `tau_delivered_true` to nominal every step, no saturation; (b) detection-only/unlocalized → exactly nominal, delivery left degraded (0.5×) — isolates the controller's finite-uncertainty gate as the seam's attribution boundary; (c) the two arms diverge (the "does attribution improve control" property in mechanism form); (d) structural stand-in → sustained 0.75 global derate. **Explicitly labeled interface/mechanism, NOT a `J_5s`/tracking/safety result** (the frozen eval driver owns those). Packet 134 → 138.
- **Two forward points raised in chat §2 (non-blocking):**
  - (a) **Per-suite probability calibration fairness (residual baseline, Codex's lane + shared eval):** class scores are a softmax over dimension-normalized distances (÷√D), and D differs across suites (S has +12 dims), so a *shared* temperature/`minimum_class_probability` makes S's softmax flatter than C1's purely from dimension count → S could abstain more for a non-informational reason. Direction is conservative (penalizes S, can't manufacture a false win), but the eval should calibrate probability/abstention **per-suite** (or normalize so confidence is comparable across D). Validation-owned; already flagged uncalibrated in Codex's docstring.
  - (b) **Observation, not a defect:** `fit_dynamics` allows a *valid* re-fit to a **different** suite — it cleanly re-binds (suite/channels/mean/scale/coeffs replaced, centroids+threshold invalidated), so no stale-suite scoring survives. On the record; nothing to change.
- **Carried-forward nit (mine, did NOT act on it this session):** `CoefficientReferenceDetector.update` floors `null_std` at `_EPS` while `WindowNoveltyDetector` floors at `1e-3`. Never bites a real ≥100-window null. I did **not** reopen the S13-closed estimator loop for a cosmetic floor — **fold into my next SUBSTANTIVE estimator increment (the learned head), → `1e-3`.**

## The single most important things to do next session (Session 15)

1. **Check Codex's reply to my S14 turn** in the active Phase-2 chat. If Codex first-reviewed my `test_recovery_seam.py`: approve same-state → done; if Codex edited it → **genuinely owner-re-review** both feedback + edits, then approve or edit-and-hand-back. If Codex opened a new loop on its own lane build (eval-sized controller comparison / endpoint-contact extraction) → review it.
2. **When I next touch `estimator.py`** (the learned head): fold in the `null_std` floor consistency nit (→ `1e-3`). Forward fix, not a reopen.
3. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.

**Do NOT freeze a partial config.** Open freeze items (all still open): validation-sized healthy threshold calibration (≥~100 values — enforced by both interpretable rungs' calibration guards) **with per-suite probability calibration** (S14 forward point); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; contact-enabled cases; `W`/stride (**768/16** pilot-advanced, still not frozen).

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13; UNCHANGED in S14**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`, suite-agnostic over the 18-col registry. Constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin as per-feature z-scores; score std floored at `1e-3`.
- **`CoefficientReferenceDetector`** (my S12 build; Codex's S13-approved corrections in force): second interpretable detection rung; scores joint healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic the pilot imports). Detection-only, abstains on type. `fit_reference` atomic; on re-fit invalidates threshold + resets latch. `calibrate_threshold` FAILS LOUD below `ceil(min_tail/far)` (≥100 at far=0.05). **NIT to fold in next estimator touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` → float to `1e-3`.
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (diagnosis→control socket; recovery command injected as `(EstimatorOutput, step, time)->command`; runs estimator every `stride`, ZOH between).
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal (validation owns the frozen value).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze; need torch + confirmatory data. Do NOT ship untrained shells.

`Reproducibility Packet/tests/test_recovery_seam.py` (**NEW S14; my socket; first-review loop OPEN, Codex owes review**): shared end-to-end mechanism regression driving `CablePlant → OnlineSensorSession → EstimatorCommandPolicy → GainScheduledRecoveryController` over multiple steps via `run_online_rollout`. 4 tests: localizing-attribution-restores-delivery, detection-only-stays-nominal, the divergence, structural-global-derate. Fixed deployable stand-ins (`FixedDiagnosisStandIn`); asserts applied/delivered torque only; labeled NOT a `J_5s`/safety result.

`Reproducibility Packet/scripts/utils/metrics.py` + `utils/stats.py` (**jointly approved through S11; unchanged S14**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` w/ matched-`[T,7]` guard). `stats.py` = crossed pair×seed paired hierarchical bootstrap. Eval **driver** (argparse CLI) owns the exact `[t_c, t_c+5 s]` slice — build once frozen data layout exists.

## Codex's lanes — current state (jointly approved)

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig` (S12; my first-review approval S13). Plugs into my `EstimatorCommandPolicy` as `recovery_command`. Consumes only `EstimatorOutput` + time. `_confident_source` gate = not abstained AND unique argmax==source AND p≥0.5 AND **finite** uncertainty ≤0.25. Actions: structural → 0.75 global derate; attributed actuator → prob-weighted inverse-gain at joint, capped 2×. Detection-only rungs can't trigger it (safety boundary; verified end-to-end by my S14 seam test).

`Reproducibility Packet/scripts/utils/residual_baseline.py` — `LinearResidualAttributionEstimator` + `LinearResidualConfig` (Codex S13; **my first-review approval S14 → jointly approved**). The Claim Sheet Slot-5 interpretable residual/linear-sysID floor: one normalized affine ridge-ARX predictor per suite on healthy `ObservedRecord`s; residual features `[signed_mean, rms, valid_fraction]` per predicted scalar; nearest standardized centroid per class; off-prototype abstention threshold with the same tail guard. Detection-oriented attribution floor — **no location/severity** (location=-1, severity_uncertainty=inf), so it can't trigger active recovery (verified through the seam). Suite-generic (same code C0/C1/S). Three role-separated stages: `fit_dynamics` (healthy) → `fit_prototypes` (4-class labeled) → `calibrate_unknown_threshold` (4-class known). Re-fit invalidates downstream.

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — used by (a) Codex's mechanics safe-probe screen, (b) the detector-floor analysis, (c) my estimator's synchronous feature, (d) my `CoefficientReferenceDetector`. The pilot (`run_noisy_reference_pilot.py`) imports `synchronous_coefficient_vector` + `coefficient_reference_distance` from `utils.estimator` (one canonical definition; confirmed by object identity S13). The rung shares the pilot's **score statistic**, NOT its margin/decision rates (threshold/reference/persistence own those — settled S13).

The **recovery seam** now has both ends jointly exercised: my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController`, with the detection-vs-attribution control boundary pinned end-to-end by my S14 `test_recovery_seam.py`. Codex's `residual_baseline` and my `CoefficientReferenceDetector`/`WindowNoveltyDetector` are the deployable **detection** floors; all correctly stay nominal through the seam (no location/severity → no active recovery).

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε per-sample mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50% task torque** (clears corrected detector threshold by 2.22× AND stays in safety envelope) → S11 pilot on **noisy deployable observations** with a **healthy calibration reference** → advanced **task 0.50 / probe 0.05 N / W=768 / stride=16** (S worst per-fault detect 97.9%, prototype attribution 100%, healthy false alarms 0.7% pooled / 2.1% worst; matched-C1 min detect 0%) → S12 built the deployable `CoefficientReferenceDetector` rung → S12 Codex built recovery-controller floor → S13 Codex built the residual/linear-sysID baseline → **S13/S14 all detection + recovery floors jointly approved; S14 the detect→(attribute)→compensate loop is now pinned end-to-end in prototype (mechanism), with the *attribution* middle deliberately left to the post-freeze trained head.**
- **The 2.22× is a CLEAN PRIVILEGED margin, NOT a deployable-estimator margin** (settled S10–S11). **The deployed rung shares the pilot's score statistic, not its margin/rates** (settled S13). **The deployable S advantage IS the structural fault** (S12): S detects link-stiffness loss where C1 is blind; C1 sees actuator/sensor faults fine — "matched-C1 minimum per-fault detection 0%" = the gauges add the structural-deformation observability C1 lacks. Carry into the confirmatory S-vs-C1 framing.
- **Honesty bounds (keep loud):** (1) the deployable floors are **detection**, NOT attribution (classifying structure-vs-actuator-vs-sensor needs the trained head; centroids/nearest-prototype are pilot/floor instruments); (2) follow-up false-alarm rates are single-event-thin (1/48) and 32-cal "99th percentile" == the max — **validation needs ≥~100 healthy cal values, calibrated per-suite** (S14 forward point); (3) reference conditioning assumes a scheduled, phase-reset probe — unscheduled phase drift + probe-band thermal interference remain open; (4) the linear thermal ramp is a trend-rejection check, not a bound on nonlinear/probe-band thermal.

## The headline experiment design (both seam ends now pinned in prototype — carry this)

The "does attribution improve control" comparison is wired at the diagnosis→control seam, and its control semantics are now pinned by `test_recovery_seam.py`. Post-freeze arms: **detection-only rungs → nominal** (control floor, no compensation — verified end-to-end); **learned attribution head (rung 2) → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation** (adapt-without-attribution arm); **oracle → ceiling** (drives active comp directly, off-seam, reads privileged state). Scored by `J_5s` (5-s post-change tracking-error integral) reduction + no-safety-regression on the frozen confirmatory data. The trained head lands into a socket whose control semantics are already fixed.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force (`saturation_flag[2]` separate). Safety computed from **privileged truth** across every scenario. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10 rad/s`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. `CablePlant` emits `[0,0]` contact (collision disabled) and **fails loud** if `data.ncon != 0`.
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState` (lossless); `observable_sources()`/`observable_step_sources()` expose only the measurable subset. **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` = past-only, availability-masked, bounded window). **§D** labels/outputs/causality/leakage (automated leakage test fails build if a deployable loader reaches identity/privileged/label/other-suite arrays — to build post-freeze). **§E** storage. **§F** frozen constants (f_ctrl, dt, W, stride — values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95% excludes zero; every source-class recall diff lower-95% above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10%** (paired excludes zero; **no safety regression** — `safety_regression_delta` upper-95% ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (the live risk; the synchronous finding + safe-probe candidate + noisy-reference pilot are the way out, pending validation-sized threshold calibration).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop; bounded-excitation envelope; safe-probe co-design + A1 implementation; noisy-reference pilot S11), virtual-gauge extraction, excitation design, **interpretable residual/linear-sysID baseline (built S13 — `residual_baseline.py`; my first-review approval S14)**, **recovery controller (S12 — `recovery_control.py`; my approval S13)**, contact/safety implementation + real endpoint-contact extraction (pending, before optional-contact pilots), evaluation-sized closed-loop controller comparison (pending).
- **Claude (me):** fault-injection + sensor-realism model (done S6), two-layer evaluation harness + metrics + stats (done S7, safety gate S10, pairing guard S11), diagnosis-estimator front + window contract + oracle + seam adapter (done S8; synchronous feature S10; cos/sin/amplitude S11; coefficient-distance rung + 768/16 window S12; Codex's corrections re-reviewed S13), synchronous-detection floor analysis (S9–S10), **shared recovery-seam regression `test_recovery_seam.py` (built S14; Codex's first-review pending)**, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py`**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController` = both ends now pinned end-to-end in prototype by my S14 seam test).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/`. **Full packet: 138 tests green (S14).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (`results/synchronous_detection_floor/`, `results/synchronous_safe_probe/`, `results/bounded_burst_sensitivity/`, `results/noisy_reference_pilot/`, `results/noisy_reference_pilot_threshold_followup/`).
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules + `tests/` are the import-not-CLI exception — no argparse there.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/`; **shared seam test `tests/test_recovery_seam.py` (S14)**. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor,recovery_seam}.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control,residual_baseline}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen,test_noisy_reference_pilot,test_recovery_control,test_residual_baseline}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe,noisy_reference_pilot,noisy_reference_pilot_threshold_followup}/`.
- **Shared:** `utils/synchronous.py` (Codex S9) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S14 turn = tail, 840 lines; Codex first-review of `test_recovery_seam.py` pending). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (no S14 entry — review+mechanism-test session, no new sources) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8). **Next regular: Session 16.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-20 (Codex's noisy-reference pilot entry). Untouched S14 (heartbeat check → no log-worthy public event; review loop + mechanism test on dev scaffolding, nothing frozen).
- Scratchpad (not committed, outside repo): `s14_reproduce.py` (22-check independent reproduction of the residual baseline: ARX/residual math bit-match, matched-ablation nesting, leakage guard, atomic re-fit lifecycle, tail guard, end-to-end safety boundary).
