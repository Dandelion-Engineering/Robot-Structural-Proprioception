# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 19, 2026-07-22 13:28 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 19**; next session I run is **Session 20**.
- **Next regular director progress report: my Session 24.** (Phase-0-close, Phase-1-close, Session-8, and Session-16 reports all exist. S17/S18/S19 closed no phase and wrote no Claim-Sheet amendment → no trigger. S20–S23 need NO progress report unless a phase closes or a Claim-Sheet amendment is approved.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 160 tests green** (159 after Codex's S18 review + 4 new tests; my S19 split of its gate-separation test → 160).

## Review-cycle state (READ THIS FIRST next session)

- **ONE REVIEW LOOP IS OPEN, AND IT IS MINE TO HAVE HANDED BACK.** In S19 I first-reviewed Codex's S18 **bounded noisy held-decision information review** and — unlike S15–S18 — **did not approve same-state. I edited and handed back.** Codex must now genuinely owner-re-review my edits and either approve that same state or edit and hand back.
  - **My edits (all in Codex's lane, all in review):**
    1. `scripts/run_bounded_noisy_information_review.py` — `decide()` gate completeness. Added a `lifecycle_pass` conjunction (information + action + representative safety/lifecycle + matched CRN + `predecision_clean`); **only `control_sensitivity_pass` may now separate the two advancing labels.** Proven not to change the recorded result (decision dict key-for-key identical → `summary.json` + all 3 CSVs byte-unchanged).
    2. Same file — `write_report()` interpretation boundary gained one paragraph: the single-fault-setting bound, plus the quantified abstention limitation (smallest held-out prototype margin, computed from the rows = **0.90**, so no threshold below it binds). Report regenerated deterministically from the committed `summary.json`; diff purely additive.
    3. `tests/test_bounded_noisy_information_review.py` — the fourth test's docstring claimed "an information pass cannot silently override an unsafe action/mechanics path" while its assertion pinned the opposite. Rewrote it to assert what it says and **split into two** (`_online_rows` helper + a five-failure-mode block test). 4 → 5 tests in that file.
  - **If Codex accepts the diagnosis but not my implementation, that is a real disagreement** — per `Playbooks/review-cycle.md` it must say so rather than swallow the edit. Escalate to Randy if it hasn't converged in ~2 round-trips.
- **My S19 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-22 13:27 PDT; file now 1327 lines; my header at line 1265, appears once, physically last). Verified via the transcript hard-gate (pre-write 1261 lines, Codex's handoff intact and unmodified).
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S19 — clean.)

## What I did this session (Session 19 — cross-review with corrections; no new build in my lane)

**First-reviewed Codex's bounded noisy held-decision information review** (`scripts/run_bounded_noisy_information_review.py`, `tests/test_bounded_noisy_information_review.py`, 5 artifacts in `results/bounded_noisy_information_review/`, packet runbook Step 12 + renumbering to Step 16, root Live-Run entry). Its recorded decision: `ADVANCE_INFORMATION_REFERENCE_LIFECYCLE_ONLY_BLOCK_RECOVERY_CONTROL_PROFILE`.

- **Independent reproduction: 53/53 checks, zero mismatches, first pass** (scratchpad `s19_information_reproduce.py`). My own collection loop + capture policy for all 592 calibration/evaluation runs; my own reference fit (healthy mean/scale, LOO detection threshold, prototype centroids, LOO selective margin); distance/classification/action-gate written from the definitions rather than imported; my own macro-F1 (abstention-as-error), per-class recall, and `J_5s`. Reproduced: the causal schedule from scratch (1136 = 2.272 s, stride-aligned, before movement at 2.400); disjoint seed roles; all 592 pre-decision histories contact-free and flag-clean; **both thresholds to 1e-12** (C1 1.281200511392, S 1.263399593694); **all 384 held-out decision rows field-by-field**; macro-F1 0.703704 / 0.994791; FA 4.17% / 2.08%; min detection 8.33% / 100%; the three false-actionable healthy cases (C1/14137, C1/14141, S/14141); **all 8 representative rows** driven off *my* references (J_5s, forces, contact, changed-command counts, gate states, pre-decision hashes); one classification evaluation per arm; no pre-decision action; **all seven A1 flags zero** (six from raw truth, the 3-D workspace flag from the plant column per my S18 lesson); `|q|max = 0.3599 rad`.
- **Byte-determinism: 5/5 SHA-256 identical** to committed and to Codex's reported hashes — **at 10 workers vs Codex's 12**, so determinism is worker-count independent. Full packet 159 green before my edits, 160 after; `compileall` clean.
- **The BLOCK reproduces:** structure is the only suite-informative pair; the other three pairs are bit-identical across suites; `J_5s` 0.8589 → 1.0184 (**−18.6%**) while peak force 2.051 → 0.499 N with zero safety incidents in either arm.
- **Deliberately did NOT edit the root Live-Run entry** — `Playbooks/live-run-readme.md` makes that log append-only and names rewriting it as a failure mode, and the entry is Codex's. Recommended a seven-word qualifier in chat ("at one development fault setting per class"); the packet README's Current boundary already carries the equivalent.
- Live-Run README heartbeat → left unchanged (a confirming review with in-review corrections is not a new public event; Codex's split-decision entry still stands — same lean-log call as S15–S18). No new references (reproduction session, no external sources read).

## The three forward notes I raised (chat §4) — note 1 is the consequential one

1. **The structural action and the headline control metric point in opposite directions BY CONSTRUCTION.** The structural action is a bounded global 0.75 derate (honestly documented in `recovery_control.py` as the auditable response to a stiffness loss you cannot invert). The Claim Sheet's control layer (Slots 7/11) is a **≥10% reduction in the 5-s tracking-error integral**, with safety entering only as a non-regression side-constraint. A derate trades tracking for margin, so **a correct structural diagnosis's best case on the headline metric is break-even** — the 18.6% regression is the *direction* of the action, not a tuning miss. On the current design, Slot-13 **"diagnostic-only"** is the near-certain landing for the control layer. Two legitimate exits, and they are different kinds of decision:
   - **Re-aim the structural action at tracking** (stiffness-loss-aware feedforward / gain re-scheduling that compensates rather than retreats). Codex's lane; its own next-step note proposes this. It must be able to *reduce* `J_5s`, not merely avoid raising it.
   - **Or amend the contract** so a graded safety-margin win is first-class. The derate cut peak force **2.051 → 0.499 N (76%)** and `safety_incident_rate` scores it as *nothing* (threshold-crossing count; both arms at zero). Creditable only via a graded safety statistic (e.g. peak force as a fraction of the 5 N limit) **declared in advance** — a Claim Sheet amendment through the protocol, not a post-hoc metric.
   Pick one deliberately, before the evaluation-sized comparison. Do not quietly do both.
2. **On this fault library macro-F1 is close to a re-encoding of structural recall.** Actuator (median score 56–64) and sensor (24–27) sit 20–50× above threshold in *both* suites; structure is the only contested class. So 0.995 vs 0.704 is driven by one binary fact, and the ≥0.05 macro-F1 bar will be saturated or near-binary here. The validation fault library should decouple them: near-threshold severities, partial/compound faults, held-out locations.
3. **Scope the ambiguous-case library BEFORE validation-sized calibration.** Minimum held-out margin 0.90 → no threshold below 0.90 binds → abstention is structurally untestable on this library. Class/abstention/selective/OOD thresholds are on the freeze list, and a threshold that has never bound cannot be frozen. This is a prerequisite for that freeze item, not a follow-up.

## The single most important things to do next session (Session 20)

1. **Check Codex's S19 turn** for its owner re-review of my three edits. If it approved the same state → loop closed, note it. If it edited and handed back → genuinely re-review its edits *and* its diagnosis (do not wave them through), then approve or edit again. If a specific point hasn't converged after ~2 round-trips, escalate that point to Randy.
2. **Codex's likely next build after the loop closes:** the redesigned structural recovery action / control-sensitivity gate on the same bounded condition. When it lands, review it against forward note 1 — the test is whether the action can *reduce* `J_5s`, or whether the project instead brings a Claim-Sheet amendment.
3. **No progress report due S20** (next regular = S24; only a phase close or approved amendment adds one).
4. **When I next touch `estimator.py`** (the learned head): fold in the carried `null_std` floor consistency nit (`CoefficientReferenceDetector.update` floors at `_EPS` → make it `1e-3` to match `WindowNoveltyDetector`). Forward fix, not a reopen.
5. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.

**Do NOT freeze a partial config.** Open freeze items: **the structural recovery action / control-sensitivity redesign (new, Codex's next)**; validation-sized healthy threshold calibration (≥~100 values) **with per-suite probability calibration**; **the ambiguous-case fault library that makes abstention testable at all (my S19 note 3)**; severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; the reference-lifecycle choice (single held decision — now validated under real noisy information — vs. a temporal model over the full post-probe trajectory); the **bounded task/contact/controller profile** (`z=0.200 m` + PD gains `(0.05,0.03)`/`(0.005,0.003)` + torque limits `(0.20,0.10)` + timing are *dev candidates*, NOT frozen margins); `W`/stride (**768/16** pilot-advanced, still not frozen); split/leakage/storage/hash audits.

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13; UNCHANGED in S14–S19**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`, suite-agnostic over the 18-col registry. Constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin as per-feature z-scores; score std floored at `1e-3`. Latches `detection_time_s` after `persistence` consecutive over-threshold windows.
- **`CoefficientReferenceDetector`**: second interpretable detection rung; scores joint healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic the pilots and Codex's noisy review all import). Detection-only, abstains on type. `fit_reference` atomic; on re-fit invalidates threshold + resets latch. `calibrate_threshold` FAILS LOUD below `ceil(min_tail/far)` (≥100 at far=0.05). **NIT to fold in next estimator touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` → float to `1e-3`.
- **`synchronous_coefficient_vector` drops masked channels physically** (C1's vector is genuinely shorter than S's, not zero-padded), and each suite's reference is fit on its own dimension. The `/√D` normalization is what puts suites on one comparable scale — **empirically confirmed S19**: the two suites' healthy score distributions and thresholds nearly coincide (1.281 vs 1.263).
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (diagnosis→control socket; runs estimator every `stride`, **ZOHs the estimator OUTPUT but calls `recovery_command` EVERY step**). Calls `estimator.reset()` at construction.
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal (validation owns the frozen value).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze. Do NOT ship untrained shells.

`Reproducibility Packet/tests/test_recovery_seam.py` (**built my S14; jointly approved S15; UNCHANGED S16–S19**): shared end-to-end mechanism regression driving `CablePlant → OnlineSensorSession → EstimatorCommandPolicy → GainScheduledRecoveryController`. 4 tests. Asserts applied/delivered torque only; labeled NOT a `J_5s`/safety result.

`Reproducibility Packet/scripts/utils/metrics.py` + `utils/stats.py` (**jointly approved through S11; unchanged S12–S19**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` w/ matched-`[T,7]` guard). `j_5s` has a fail-loud guard requiring the full `[t_c, t_c+5 s]` window (both endpoints present) — the 3000-step bounded runs satisfy it exactly. **S19 finding: `safety_incident_rate` is a threshold-crossing count and therefore cannot score a graded margin improvement — see forward note 1.** `stats.py` = crossed pair×seed paired hierarchical bootstrap. Eval **driver** (argparse CLI) owns the exact slice — build once frozen data layout exists.

## Codex's lanes — current state

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig` (S12; my approval S13) + `command_from_nominal(output, nominal_command)` (S17; my approval S18). `_confident_source` gate = not abstained AND unique argmax==source AND p≥0.5 AND **finite** uncertainty ≤0.25. Actions: structural → **0.75 global derate** (`structural_command_derate`); attributed actuator → prob-weighted inverse-gain at joint, capped 2×. Detection-only rungs can't trigger it. Recovery torque_abs_limit `(1.0, 0.5)`.

`Reproducibility Packet/scripts/utils/task_control.py` (**Codex S17; my approval S18**) — `BoundedTaskProfile` (rest → `(0.30,0.30)` rad by 3.0 s → hold → back to rest by 5.0 s; movement_start 2.40, transition 0.60, hold_end 4.40, return_end 5.00), `ObservedJointControllerConfig` (kp `(0.05,0.03)`, kd `(0.005,0.003)`, torque limits `(0.20,0.10)`), `ObservedJointPDController` (reads ONLY delivered `q_obs`/`qd_obs` → deployable), `EstimatorRecoveryTaskPolicy` (task PD + diagnosis cadence + recovery on a live per-step nominal; records `nominal_commands` and `commands`).

`Reproducibility Packet/scripts/utils/cable_mechanics.py` + `utils/cable_plant.py` + `make_mujoco_plant_trace.py` — the plant lane + endpoint-contact extraction (S14; my approval S15). **A1 safety flags** = `|q|>π` (per joint), `|qd|>10` (per joint), 3-D tip radius > 0.82 from base `[0,0,0.5]`, `max|gauge|>500 µε`, `contact_state[0]>5 N`. **The 3-D workspace flag is NOT reconstructable from the record's 2-D `true_task_output` — recompute the other 6 from raw truth + trust the plant column for the 7th (my S18 harness lesson; honored again in S19).**

`Reproducibility Packet/scripts/screen_bounded_task_contact.py` + tests + `results/bounded_task_contact_screen/` (**Codex S17; my approval S18 → jointly approved**). Bounded redesign screen; `SingleDecisionHoldEstimator` (one evaluation at `first_decision_step`, held causally, healthy before) + `FixedSourceStandIn` (mechanism instrument). **Decision: ADVANCE, z=0.200 m selected, z=0.100 m negative control.** `BoundedTaskContactSpec.first_decision_step` is `ceil(after_probe/stride)*stride` — **structurally always a stride multiple**, so the single held decision can never be missed.

`Reproducibility Packet/scripts/run_bounded_noisy_information_review.py` + `tests/test_bounded_noisy_information_review.py` + `results/bounded_noisy_information_review/` (**Codex S18; my S19 first review → EDITED AND HANDED BACK, loop open**). Replaces the fixed stand-ins with a real noisy instrument at the same held decision on the same bounded condition. Roles: 100 calibration seeds (14000–14099) fit each suite's healthy reference + 95th-percentile LOO detection threshold + fault prototypes + LOO selective margin; 48 disjoint held-out seeds (14100–14147) supply every reported rate; representative full-horizon continuation on predeclared held-out seed 14100. Every case runs the real `CablePlant → OnlineSensorSession(S) → observed PD` closed loop; **C1 is projected from that exact S window** (verified equivalent by bit-identical pre-decision hashes, not assumed). `mechanics_spec()` pins the S18-approved condition exactly — only the plane list narrows to `(0.100, 0.200)`. **Result: S PASSES information + action gates (macro-F1 0.995, 100% per-fault detection, 2.1% healthy FA/false-actionable); C1 BLOCKS (0.704, 8.3% structural recall, 4.2% FA); the recovery-control profile BLOCKS.**

`run_noisy_reference_pilot.py` (S11), `screen_optional_contact_profile.py` (S15), `run_matched_contact_pilot.py` (S16, **blocked all three gates**, superseded by the bounded redesign) — all jointly approved. `fault_specs`, `project_observed_suite`, and `count_contact_episodes` are reused by the newer screens.

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic**. `synchronous_coefficient_vector` + `coefficient_reference_distance` in `utils/estimator.py` are the one canonical definition every pilot, screen, and the noisy review import.

The **recovery seam** has both ends jointly exercised (my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController`), pinned by `test_recovery_seam.py`. **`detection_time_s` semantics are consistent everywhere** (latch-once at first detection; NaN before). The bounded screen (S17) established that recovery can act *before* contact; the noisy review (S18/S19) establishes that a *real* estimator can supply the label — and that acting on it currently *hurts* the headline metric.

**Contact + the 7th safety flag:** contact reaches deployable suites only through motion/strain (privileged-only otherwise). In closed loop it is NOT suite-invariant. **Design constraint (my S15 note, honored by every screen since): apply the contact profile identically across each matched C1/S CRN pair.** In the noisy review the 7th flag stayed 0 (forces < 5 N) and the joint-angle flag stayed 0 (`|q|max ≈ 0.36 ≪ π`).

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50 % task torque** → S11 noisy-deployable pilot → task 0.50 / probe 0.05 N / W=768 / stride=16 → S12 `CoefficientReferenceDetector` + recovery-controller floor → S13 residual/linear-sysID baseline → S14 detect→(attribute)→compensate loop pinned end-to-end + endpoint-contact extraction → S15 both loops approved + optional-contact screen → S16 matched contact-enabled pilot **BLOCKS all three gates** → S17 I reproduced + approved the block; Codex's bounded redesign clears the mechanics/lifecycle block → S18 I reproduced every gate and approved (jointly approved); Codex replaced the stand-ins with a real noisy instrument → **S19 I reproduced every number (53/53) and edited-and-handed-back on gate logic + honesty wording.** The *learned* attribution head stays deliberately post-freeze.
- **The deployable S advantage IS the structural fault**, now confirmed three independent times (S12, S16, S19). S19 sharpens it: **C1 is blind, not weakly sighted** — its structural score distribution (median 0.955, max 1.560) sits on top of its healthy one (median 0.972, max 1.574), so its 8.3% "detections" are the same tail its false alarms come from. S separates completely with a real gap (healthy max 1.457 < structure min 1.724). Carry this framing into the confirmatory S-vs-C1 write-up; it is stronger and more honest than the macro-F1 headline.
- **The information question is now answered on this bounded development condition; the control question is the live one.** See forward note 1 — the structural action's direction, not its tuning, is what blocks.
- **Honesty bounds (keep loud):** (1) the deployable floors are **detection**, NOT learned attribution — the prototype instrument is not the learned head; (2) all rates come from **one fixed fault setting per class**, held-out over sensor noise only — no severity/onset/location generalization is demonstrated; (3) **abstention is untestable on this fault library** (min margin 0.90); (4) the full-horizon continuation is **one seed per source/suite** — a mechanism/safety sensitivity, not evaluation-sized control evidence; (5) one-hot prototype probabilities are **not** calibrated probabilities; (6) reference conditioning assumes a scheduled, phase-reset probe + ONE held decision — unscheduled phase drift, probe-band thermal, and faults developing after the held decision remain open.

## The headline experiment design (both seam ends pinned; task/contact mechanics pinned; information path now real)

The "does attribution improve control" comparison is wired at the diagnosis→control seam, its control semantics pinned by `test_recovery_seam.py`, its task/contact mechanics by the bounded screen, and its *information* path now demonstrated with a real noisy estimator. Post-freeze arms: **detection-only rungs → nominal** (control floor); **learned attribution head → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation**; **oracle → ceiling**. Scored by `J_5s` reduction + no-safety-regression on frozen confirmatory data. **S19 sharpening: the headline arm cannot win on `J_5s` while its structural action is a derate — resolve that direction (redesign the action, or amend Slots 7/11 to admit a graded safety-margin win) before the evaluation-sized comparison is built.**

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force. Safety computed from **privileged truth**. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N.
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`) — **note the S19 gap: the noisy review's held-out axis is sensor-seed only, so the §A whole-fault-setting split is still unexercised.** **§B** privileged `PlantStepState`/`PrivilegedRecord` (`q_true[T,2]`, `qd_true`, `gauge_true[T,4]`, `contact_state[T,2]`, `true_task_output[T,2]` (planar tip), `safety_flag[T,7]`); `observable_sources()` expose only the measurable subset (**contact_state + true 3-D tip NOT in it**). **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative). **§D** labels/outputs/causality/leakage. **§E** storage. **§F** frozen constants. **§G** tracking metric `J_5s`.
- **Sensor RNG is keyed on `(sensor_seed, pair_id, channel, stream)`** — the `pair_id` string is load-bearing for reproducibility (my S17 harness bug proved it). The noisy review's `pair_id = f"bounded-information-{split}-{source}-{sensor_seed}"` for collection and `f"bounded-information-evaluation-{source}-14100"` for the representative rows. **Match pair_ids exactly when reproducing.**

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95 % excludes zero; every source-class recall diff lower-95 % above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10 %** (paired excludes zero; **no safety regression** — `safety_regression_delta` upper-95 % ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes (Slot 13):** **diagnostic-only** (attribution improves, control does not — *this is the shape the current design is heading toward*) · fault-specific/bounded · confound-fragile · excitation-dependent.
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics, virtual-gauge extraction, excitation design, interpretable residual/linear-sysID baseline, recovery controller + external-nominal composition, contact/safety extraction, the contact/bounded screens, the matched pilots, **the bounded noisy held-decision information review (S18; my S19 edits pending its re-review)**, **the structural recovery action / control-sensitivity redesign (next)**, evaluation-sized closed-loop controller comparison (pending).
- **Claude (me):** fault-injection + sensor-realism model, two-layer evaluation harness + metrics + stats, diagnosis-estimator front + window contract + oracle + seam adapter, synchronous-detection floor analysis, shared recovery-seam regression, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (+A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), `utils/synchronous.py`. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam.

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). **Running a script: from `Reproducibility Packet/` as `..\venv\Scripts\python.exe scripts\<name>.py`** (NOT from `scripts/` — the default `--output-dir` is packet-relative and the intra-`scripts` imports need `scripts/` as `sys.path[0]`, which `python scripts\x.py` provides). **Full packet: 160 tests green (S19).** Set `PYTHONIOENCODING=utf-8` when a script prints unicode (Windows cp1252 default errors otherwise). My scratchpad reproduction scripts also run **from `Reproducibility Packet/`** (they anchor the packet root via cwd). **`run_bounded_noisy_information_review.py` takes ~12–20 min** (592 runs + 8 rollouts); use `--workers 10–12` and run it in the background.
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked. Root `.gitignore` covers `venv/`, `tmp/`, `MUJOCO_LOG.TXT`, caches, LaTeX aux, OS/IDE noise — **verified accurate S19, no change needed.**
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules + `tests/` are the import-not-CLI exception.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py`; shared seam test `tests/test_recovery_seam.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control,residual_baseline,task_control}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot,screen_optional_contact_profile,run_matched_contact_pilot,screen_bounded_task_contact,run_bounded_noisy_information_review}.py` + matching tests + `results/` artifacts.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S19 turn = tail, 1327 lines; **one review loop OPEN — my edits await Codex's owner re-review**). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (**no S19 entry — reproduction session, no new external sources**) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8, Session 16). **Next regular: Session 24.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-21 (Codex's information/control split). Untouched S19. **That log is append-only by playbook — do not rewrite an entry, including another agent's.**
- Scratchpad (not committed, outside repo): `s19_information_reproduce.py` (53-check independent reproduction — reusable template: own collection loop, own reference fit, own metrics, diff against committed CSVs), `regen/` (byte-determinism run).
