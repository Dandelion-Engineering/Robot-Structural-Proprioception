# Summary of Only Necessary Context — Claude

*Rewritten every session. Restores my working context for the next session. Excludes anything already in `Project Details/Project Details.md` and `AgentPrompt.md` (I re-read those in full at session start). Last rewritten: end of Session 17, 2026-07-21 18:05 PDT.*

## Where the project is

- **Phase 2 (Execution) is OPEN.** All Phase-1 gates closed and in force. **Schema v1.0 + Amendment A1 (contact/safety roles) are jointly in force.** Contract changes run through the **amendment protocol**, not casual editing.
- I am **Claude**; last session was **Session 17**; next session I run is **Session 18**.
- **Next regular director progress report: my Session 24.** (Phase-0-close, Phase-1-close, Session-8, and Session-16 regular reports all exist. Session 17 closed no phase and wrote no Claim-Sheet amendment → no trigger. So S18–S23 need NO progress report unless a phase closes or a Claim-Sheet amendment is approved in that session.)
- **`config.json` is deliberately NOT frozen** and cannot be until the open freeze items resolve. Current role hashes are `dev-`; no `dev-` trace may enter confirmatory analysis.
- **Packet: 148 tests green** (was 143 end of my S16; Codex's S16 added the 5-test matched-contact-pilot suite).

## Review-cycle state (READ THIS FIRST next session)

- **NO OPEN REVIEW LOOPS BETWEEN THE AGENTS as of end of my S17.** I closed the one loop Codex opened in its S16 turn:
  - **CLOSED (first review):** Codex's development-only **matched contact-enabled C1/S pilot** (`scripts/run_matched_contact_pilot.py`, `tests/test_matched_contact_pilot.py`, `results/matched_contact_enabled_pilot/` = summary.json + 3 CSVs + report, packet runbook Steps 10–14, Live-Run blocker entry). Reproduced independently (scratchpad `s17_matched_contact_reproduce.py`, my own plant drive + metric recompute of all three gates) → **approved same state, no edits.** The pilot **BLOCKS on all three gates**; nothing frozen.
- **My S17 chat turn is the physical tail** of the active Phase-2 chat (posted 2026-07-21 18:02 PDT; file now 1140 lines; my header at line 1108, appears once, is last). Verified via the transcript hard-gate (pre-write 1104 lines, Codex's handoff at 1052, my header once at 1108, physically last).
- **MONITORING DUTY IS STANDING:** if I ever see a reply land anywhere but the physical end of a transcript, flag it in `Claude-Codex-Human`. (Append by matching the unique multi-line physical tail, never a bare signature; re-verify after. Did so S17 — clean.)
- **Codex's likely next turn (check for a NEW loop it may open in its S17):** the **bounded task/contact/controller redesign** the matched-pilot block requires — a stabilized finite task trajectory that keeps every scenario inside the unchanged A1 limits over onset+5 s, applied identically across each matched C1/S CRN pair — and/or the **reference-lifecycle fix** (make ONE held scheduled decision, OR phase/time-condition the reference, OR train a temporal model over the full post-probe trajectory; do NOT reuse the single-decision prototype continuously). If Codex opened a new loop → review it (reproduce independently, per my pattern).

## What I did this session (Session 17 — pure cross-review; no new build in my lane)

- **First-reviewed + approved Codex's matched contact-enabled C1/S pilot.** Independent reproduction (`s17_matched_contact_reproduce.py`) of all three BLOCK gates with my own logic, comparing to the committed CSVs:
  1. **Onset+5 s contact/safety audit (the decisive gate) — bit-for-bit.** Drove `CablePlant` myself at z ∈ {0.050, 0.100} × {healthy, structure, actuator} over 3000 steps; recomputed episodes/active-steps/peak/first-last-contact (all match <1e-6) and the joint-1 angle flag independently as `|q_true[:,1]|>π` == plant's `safety_flag[:,1]` exactly (**1111/1658/1651** steps at z=0.100; **311/334/0** at z=0.050). **The block is the joint-angle flag alone** — the 7th (tip-contact-force) flag is 0 everywhere (peaks 3.21–4.46 N < 5 N).
  2. **Exact-window info gate — reproduced.** Collected coefficient vectors at `end_index=1135` (newest sample owned before decision step 1136) for all 32 cal + 48 held-out seeds myself; independently recomputed max-LOO threshold + false alarms + per-class detection. **S: 8.3 % (4/48)** held-out healthy false alarms, 100 % min detection, 100 % attribution → fails ≤5 % screen. **C1: 4.2 % (2/48)** but structurally blind (20.8 % min detection). Verified the causal window never peeks past 1135.
  3. **Short causal seam — drift confirmed.** Re-derived `short_online_pass` from committed rows (all 8 arms end on **actuator** → `final==source` fails, healthy/sensor carry nonzero changed commands) → False; re-ran healthy/S + structure/S through the real seam → exact changed-command counts (**36**, **52**) + actuator drift. `decide()` → `BLOCK_MATCHED_CONTACT_PILOT_AND_CONTACT_PROFILE_CONFIG_FREEZE`.
- **Byte-determinism:** regenerated the whole artifact (default command) to scratch → all 5 files SHA-256 identical to committed. **Full packet 148 green** on my machine. **No NaN/Infinity** in the JSON. Packet runbook Steps 1–14 consistent.
- **Harness-bug honesty (my side, not Codex's):** my first repro pass diverged on the info gate (looser threshold, S 1/48) because I used `cal`/`eval` in the reproduction's `pair_id` where the pipeline uses `calibration`/`evaluation` — the sensor RNG is keyed on `(sensor_seed, pair_id, channel, stream)`, so that reseeded the noise. Fixed → exact match. (The safety audit was immune because it uses no sensor RNG, hence it matched on the first pass.)
- **Two non-blocking forward notes raised (chat §4)** for the bounded redesign:
  1. **No layer of this pilot lets closed-loop recovery affect the safety outcome.** Short-seam first contact (~1.97–2.04 s) precedes the first online decision (2.272 s) → the single episode is pre-recovery; the onset+5 s audit is pure open-loop (no controller). So the redesign must place the diagnosis decision *before* — with the controller having authority over — the safety-relevant window, or the C1-vs-S control/safety comparison is structurally unanswerable.
  2. **The joint-angle violation is an open-loop-task property, orthogonal to the contact plane.** The angle flag fires at z=0.050 for healthy/structure (311/334) where contact is minimal, but the z=0.050 actuator case *with* contact has 0 angle steps → the ~1100–1650-step violations at z=0.100 are the open-loop `commanded_torque` task swinging joint-1 past π over 6 s, not the contact. First-order fix is a **stabilized/bounded task trajectory**, not a lower plane.
- Live-Run README heartbeat → left unchanged (Codex already logged the matched-pilot BLOCK in its S16 public entry; a confirming internal review is not a new public event — same lean-log call as my S15/S16). No new references (review/reproduction session, no external sources read).

## The single most important things to do next session (Session 18)

1. **Check Codex's S17 turn** in the active Phase-2 chat for a NEW review loop (bounded task/contact/controller redesign and/or reference-lifecycle fix). If Codex handed something for review → review it, reproduce independently, approve same-state or edit-and-hand-back.
2. **No progress report due S18** (next regular = S24; only a phase close or approved amendment would add one).
3. **When I next touch `estimator.py`** (the learned head): fold in the carried `null_std` floor consistency nit (`CoefficientReferenceDetector.update` floors at `_EPS` → make it `1e-3` to match `WindowNoveltyDetector`). Forward fix, not a reopen.
4. **Deferred, still blocked (do NOT build early):**
   - The learned attribution rungs (`TemporalAttributionNet` headline + `RMALatentEncoder`) — need PyTorch CUDA build, GPU-verified sm_120, installed *at that point*; + frozen config + confirmatory data.
   - The §D deployable-loader leakage test + whole-trajectory/fault-setting split audit — need real multi-run storage.
   - The eval **driver** ([t_c, t_c+5 s] slicing → paired C1-vs-S control comparison) — needs the frozen data layout.

**Do NOT freeze a partial config.** Open freeze items (all still open): validation-sized healthy threshold calibration (≥~100 values — enforced by both interpretable rungs' calibration guards) **with per-suite probability calibration** (S14 forward point); severity/onset grids; joint sanity-check of non-load-bearing sensor constants; validation-frozen class/abstention/selective/OOD thresholds; **the bounded task/contact/controller profile** (the matched pilot BLOCKED z=0.100 m over onset+5 s — neither the height nor a contact-enabled scenario grid is frozen; needs a stabilized trajectory redesign); **the reference-lifecycle choice** (single held decision / phase-conditioned / temporal model — the matched pilot's prototype drift is the live evidence); `W`/stride (**768/16** pilot-advanced, still not frozen).

## My lanes — current state (reference for next builds)

`Reproducibility Packet/scripts/utils/estimator.py` (**stable, jointly approved through S13; UNCHANGED in S14–S17**):
- **Window front-end `WindowFeatureExtractor(window_steps=W=768, probe_frequency_hz=0.8)`**: fixed `[W,D]` left-padded tensor + per-column summary `[last, mean, std, slope, sync_cos, sync_sin, sync_amplitude, valid_fraction]`, suite-agnostic over the 18-col registry. Constants: `N_FEATURE_STATS=4`, `N_EXTRA_FEATURES=4`, `SYNC_COS_FEATURE_COL=4`, `SYNC_SIN_FEATURE_COL=5`, `SYNC_AMPLITUDE_FEATURE_COL=6`, `VALID_FRACTION_COL=7`, `DIAGNOSTIC_PROBE_HZ=0.8`, `MIN_SYNC_SAMPLES=8`.
- **`WindowNoveltyDetector`**: interpretable detect + calibrated-abstention; consumes cos/sin as per-feature z-scores; score std floored at `1e-3`. Latches `detection_time_s` after `persistence` consecutive over-threshold windows.
- **`CoefficientReferenceDetector`** (my S12 build; Codex's S13-approved corrections in force): second interpretable detection rung; scores joint healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` (module-level `synchronous_coefficient_vector` + `coefficient_reference_distance`, the canonical statistic the pilots import). Detection-only, abstains on type. `fit_reference` atomic; on re-fit invalidates threshold + resets latch. `calibrate_threshold` FAILS LOUD below `ceil(min_tail/far)` (≥100 at far=0.05). **NIT to fold in next estimator touch:** `update`'s `unknown_score=z` floors `null_std` at `_EPS` → float to `1e-3`.
- **`OracleInterface(onset_time_s)`**: allowlisted privileged ceiling, causal. **`EstimatorCommandPolicy`**: the `run_online_rollout` `CommandPolicy` adapter (diagnosis→control socket; recovery command injected as `(EstimatorOutput, step, time)->command`; runs estimator every `stride`, ZOH between; calls `estimator.reset()` at construction). **The matched-contact pilot's `PilotPrototypeEstimator` subclasses `DiagnosisEstimator` and plugs into this socket** (S16, reviewed S17) — its location/severity are a fixed lookup on the *predicted* class, one-hot confidence is a mechanism instrument only, gated healthy before step 1136.
- **`RECOMMENDED_WINDOW` = (W=768, stride=16)** — pilot-advanced, still a pilot proposal (validation owns the frozen value).
- **Learned rungs (specified, not built):** `TemporalAttributionNet` (headline) + `RMALatentEncoder`. Post-freeze; need torch + confirmatory data. Do NOT ship untrained shells.

`Reproducibility Packet/tests/test_recovery_seam.py` (**built my S14; Codex's S14 detection-time-latch fix; jointly approved S15; UNCHANGED S16–S17**): shared end-to-end mechanism regression driving `CablePlant → OnlineSensorSession → EstimatorCommandPolicy → GainScheduledRecoveryController` over multiple steps via `run_online_rollout`. 4 tests. Asserts applied/delivered torque only; labeled NOT a `J_5s`/safety result.

`Reproducibility Packet/scripts/utils/metrics.py` + `utils/stats.py` (**jointly approved through S11; unchanged S12–S17**): two-layer success-bar metrics (macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept, `coverage_at_risk`; control `j_5s` + `tracking_reduction_pct`; safety `safety_incident_rate`/`safety_flag_rates`/`safety_regression_delta` w/ matched-`[T,7]` guard — the 7th flag `tip_contact_force_exceeded` becomes live once contact scenarios exist; the matched pilot confirmed the 7th flag stays 0 at z=0.100 over 6 s because peaks < 5 N — it's the joint-angle flag that trips). `stats.py` = crossed pair×seed paired hierarchical bootstrap. Eval **driver** (argparse CLI) owns the exact `[t_c, t_c+5 s]` slice — build once frozen data layout exists.

## Codex's lanes — current state (jointly approved)

`Reproducibility Packet/scripts/utils/recovery_control.py` — `GainScheduledRecoveryController` + `RecoveryControlConfig` (S12; my approval S13). Plugs into my `EstimatorCommandPolicy` as `recovery_command`. `_confident_source` gate = not abstained AND unique argmax==source AND p≥0.5 AND **finite** uncertainty ≤0.25. Actions: structural → 0.75 global derate; attributed actuator → prob-weighted inverse-gain at joint, capped 2×. Detection-only rungs can't trigger it.

`Reproducibility Packet/scripts/utils/residual_baseline.py` — `LinearResidualAttributionEstimator` + `LinearResidualConfig` (Codex S13; my approval S14). Claim Sheet Slot-5 interpretable residual/linear-sysID floor. Detection-oriented (location=-1, severity_uncertainty=inf), can't trigger active recovery. Three role-separated stages.

`Reproducibility Packet/scripts/utils/cable_mechanics.py` + `utils/cable_plant.py` + `make_mujoco_plant_trace.py` — the plant lane, **+ endpoint-contact extraction (Codex S14; my approval S15).** `CableModelConfig` has `endpoint_contact_enabled` (default **False**) + `endpoint_contact_plane_z_m` (default 0.498 fixture) + `tip_contact_force_limit_n` (5.0). When enabled: adds one plane + explicit `<pair>` between distal geom `L2_G{point_count-2}` and the plane; `_contact_state()` filters to that exact pair (fails loud otherwise), sums `‖mj_contactForce‖` → `tip_contact_force_n`, sets `tip_contact_active` (1.0/0.0), drives the 7th safety flag. **A1 safety flags** = `|q|>π` (angle), `|qd|>10` (speed), 3-D tip radius > 0.82 from base `[0,0,0.5]` (workspace), `|gauge|>500 µε`, `force>5 N` (contact) — all elementwise from privileged truth.

`Reproducibility Packet/scripts/screen_optional_contact_profile.py` + tests + results (**Codex S15; my approval S16**). Open-loop ascending-plane screen; advanced **z=0.100 m** as a *dev candidate* over the 2.274 s horizon (one bounded episode, no A1 flag). Its `run_physical_scenario` / `summarize_record` / `ScreenSpec` / `physical_fault` / `count_contact_episodes` are **reused by the matched pilot**.

`Reproducibility Packet/scripts/run_noisy_reference_pilot.py` (**Codex S11; jointly approved**). Provides `fit_reference` (healthy mean/scale + max-LOO threshold + fault centroids), `evaluate_reference` (false-alarm/detection/attribution), `predict_fault` (nearest standardized centroid), `causal_window` (past-only, availability-masked), `project_observed_suite` (S→C1 by masking the gauge role; CRN-equivalent), `generate_plant_records`, `fault_specs`, `CoefficientReference`. **All reused by the matched pilot.**

`Reproducibility Packet/scripts/run_matched_contact_pilot.py` + `tests/test_matched_contact_pilot.py` + `results/matched_contact_enabled_pilot/` (**Codex S16; my first-review approval S17 → jointly approved**). The first matched contact-enabled C1/S pilot; applies z=0.100 m identically to matched CRN pairs. **Three gates, all BLOCK:**
- **Exact-window info gate:** vectors collected at `offline_decision_index=1135` (first online decision step 1136); 32 cal seeds (9000–9031) + 48 held-out (9032–9079). S **8.3 % (4/48)** healthy false alarms, 100 % detection/attribution; C1 4.2 % (2/48), 20.8 % structural detection. Threshold still the calibration max → can't freeze.
- **Short causal seam (2.6 s):** `PilotPrototypeEstimator` (pilot-only, honest fences) → `EstimatorCommandPolicy` → `GainScheduledRecoveryController` at representative seed 9032. First contact precedes first decision; all 8 arms drift to **actuator** by the final decision (incl. healthy/sensor → inappropriate compensation). Reference-lifecycle BLOCK.
- **Onset+5 s audit (6.0 s, open-loop):** z=0.100 m → 3 contact episodes + joint-angle violations (1111/1658/1651 steps); z=0.050 former control also contacts (~4.32 s) + violates angle for healthy/structure. Contact-profile BLOCK.
- Overall `BLOCK_MATCHED_CONTACT_PILOT_AND_CONTACT_PROFILE_CONFIG_FREEZE`. **Development evidence only, byte-deterministic; nothing frozen; no confirmatory data; central question open.**

## Coherence worth remembering

`utils/synchronous.py` (Codex, S9) is the **single shared harmonic statistic** — used by Codex's mechanics safe-probe screen, the detector-floor analysis, my estimator's synchronous feature, and my `CoefficientReferenceDetector`. The noisy-reference + matched-contact pilots import `synchronous_coefficient_vector` + `coefficient_reference_distance` from `utils.estimator` (one canonical definition). The rung shares the pilots' **score statistic**, NOT their margin/decision rates (threshold/reference/persistence own those — settled S13).

The **recovery seam** has both ends jointly exercised: my `EstimatorCommandPolicy` socket + Codex's `GainScheduledRecoveryController`, control boundary pinned end-to-end by `test_recovery_seam.py`. Detection-only floors (residual_baseline, CoefficientReferenceDetector, WindowNoveltyDetector) correctly stay nominal through the seam. **`detection_time_s` semantics are consistent everywhere** (latch-once at first detection; NaN before). The matched pilot's `PilotPrototypeEstimator` is the first thing to drive *active* recovery through the seam — and it revealed the reference-lifecycle drift (a diagnosis conditioned on one probe phase, reused continuously, drifts to actuator).

**Contact + the 7th safety flag:** contact reaches deployable suites only through motion/strain (contact_state is privileged-only, confirmed S15). In closed loop it is NOT suite-invariant (S vs C1 issue different recovery → different endpoint trajectory → different contact). **Design constraint (my S15 note, encoded in Codex's screen + honored by the matched pilot): apply the contact profile identically across each matched C1/S CRN pair**, so any 7th-flag diff is attributable to recovery. In the matched pilot the 7th flag stayed 0 (forces < 5 N); the block was the joint-angle flag + repeated contact.

## The excitation/detection thread — current status (the project's dominant thread)

- **Settled chain:** S9 reframing (synchronous detector NES ~0.11 µε, ~90× below the 10 µε mechanics-gate floor) → Codex's **0.05 N, 0.8 Hz, one-cycle raised-cosine probe at 50 % task torque** → S11 noisy-deployable pilot with healthy calibration reference → advanced **task 0.50 / probe 0.05 N / W=768 / stride=16** → S12 built `CoefficientReferenceDetector` + recovery-controller floor → S13 residual/linear-sysID baseline → S14 detect→(attribute)→compensate loop pinned end-to-end in prototype + endpoint-contact extraction → S15 both S14 loops approved + optional-contact profile screen (z=0.100 m dev candidate) → S16 I approved that screen; **Codex built + ran the matched contact-enabled pilot → BLOCK on all three gates → S17 I reproduced all three independently + approved (jointly approved).** The *attribution* middle (trained head) stays deliberately post-freeze.
- **The deployable S advantage IS the structural fault** (S12, re-confirmed by the matched pilot): S detects link-stiffness loss at ~100 % where C1 is structurally blind (~21 %); C1 handles actuator/sensor fine. The gauges add the structural-deformation observability C1 lacks. Carry into the confirmatory S-vs-C1 framing.
- **Why the matched pilot blocked (the honest state):** the structural *signal* is strong, but (1) the healthy false-alarm rate (8.3 %) exceeds the screen with only a max-LOO threshold on 32 seeds — needs validation-sized calibration; (2) a single-scheduled-decision prototype can't be reused continuously — reference lifecycle must be redesigned; (3) the static z=0.100 m contact profile isn't safe over onset+5 s under the open-loop task — needs a stabilized bounded trajectory. **None is evidence against the hypothesis** — they are method/config problems that scope the next redesign.
- **Honesty bounds (keep loud):** (1) the deployable floors are **detection**, NOT attribution; (2) thresholds are max-LOO on too-few seeds — validation needs ≥~100 healthy cal values, calibrated per-suite; (3) reference conditioning assumes a scheduled, phase-reset probe — unscheduled phase drift + probe-band thermal remain open; (4) no layer of the matched pilot lets closed-loop recovery affect the safety outcome (S17 forward note) — the redesign must fix that for the control/safety comparison to be answerable.

## The headline experiment design (both seam ends pinned in prototype — carry this)

The "does attribution improve control" comparison is wired at the diagnosis→control seam, its control semantics pinned by `test_recovery_seam.py`. Post-freeze arms: **detection-only rungs → nominal** (control floor); **learned attribution head → active inverse-gain / derate** (headline arm); **RMA latent → blind adaptation** (adapt-without-attribution arm); **oracle → ceiling**. Scored by `J_5s` reduction + no-safety-regression on frozen confirmatory data. **S17 sharpening: the matched pilot showed the comparison design must place the diagnosis decision before — and give the controller authority over — the safety-relevant window, or the safety-axis result is structurally unanswerable.** The trained head lands into a socket whose control semantics are already fixed.

## Schema v1.0 + A1 mental model (in force)

File: `Reproducibility Packet/schema/schema-v1.0.md` (rendered in `utils/schema_types.py` §B/§0/§C/§E + `utils/estimator.py` §D). **Amendment A1 (in force):** `contact_state[2] = {tip_contact_force_n, tip_contact_active}`; `safety_flag[7]` order = joint_angle_0, joint_angle_1, joint_speed_0, joint_speed_1, tip_workspace, gauge_abs, tip_contact_force. Safety computed from **privileged truth**. Dev thresholds (config values, not frozen): `|q|≤π`, `|qd|≤10 rad/s`, tip radius ≤0.82 m, `|gauge_true|≤500 µε`, tip contact force ≤5 N. **`CablePlant`'s optional endpoint-contact profile (disabled by default → `[0,0]`, fails loud if `ncon≠0`) populates `contact_state` from real `mj_contactForce` truth + drives the 7th flag when enabled; z=0.100 m is a screened dev candidate now BLOCKED by the matched pilot over onset+5 s.**
- **§A** identity/pairing/splits (splits by whole trajectory AND whole fault setting; suite never a split input; CRN within a pair via `utils/rng.py`). **§B** privileged `PlantStepState` (lossless); `observable_sources()` expose only the measurable subset (**contact_state NOT in it — privileged-only**). **§C** observed record (fixed 18-col registry, unavailable=NaN; `OnlineSensorSession` authoritative stateful path; `available_record` past-only/masked/bounded). **§D** labels/outputs/causality/leakage (`detection_time_s` = time change first flagged, NaN before; automated leakage test — build post-freeze). **§E** storage. **§F** frozen constants (values in `config.json`, frozen before confirmatory gen). **§G** tracking metric `J_5s`.
- **Sensor RNG is keyed on `(sensor_seed, pair_id, channel, stream)`** — the `pair_id` string is load-bearing for reproducibility (my S17 harness bug proved it: a differing `pair_id` reseeds all sensor noise). Match pair_ids exactly when reproducing.

## The agreed contract's load-bearing specifics (carry this)

- **Sensor suites (controlled variable):** **C0** = joint encoders + commanded actuation · **C1** = C0 + noisy motor-current nominal-Kt torque estimate + one distal 6-axis IMU · **S** = C1 + **four fixed strain/curvature gauge stations** · **O** = privileged oracle. External pose/vision + true delivered torque excluded from deployable suites.
- **Two settled correctness points:** (1) actuator-gain fault acts *downstream* of the current proxy → C1 not handed true delivered torque; (2) encoder (sensor) fault has a *relational* signature — a lying sensor doesn't deform the structure; identified by disagreement between corrupted encoder and independently-evolved gauge/physical history.
- **Success bar (pre-declared, BOTH layers required):** S improves held-out four-way **macro-F1 over C1 by ≥0.05 absolute** (paired 95 % excludes zero; every source-class recall diff lower-95 % above **−0.02**) **and** reduces the **5-s post-change integral of absolute tracking error by ≥10 %** (paired excludes zero; **no safety regression** — `safety_regression_delta` upper-95 % ≤ 0), **under realistic confounds**. Known-class abstention scored as error in headline macro-F1; calibration/selective/OOD reported separately.
- **Failure shapes:** *hypothesis failure* (C1+temporal adaptation matches S — clean negative) vs *method failure*. **Inconclusive shapes:** diagnostic-only · fault-specific/bounded · confound-fragile · **excitation-dependent** (the synchronous finding + safe-probe + noisy-reference pilot are the way out, pending validation-sized calibration + the contact/lifecycle redesign).
- **Pre-specification:** bars fixed before the pilot; freeze gauge placement, model/hyperparams, thresholds, analysis window, seeds/scenarios before confirmatory generation. Leakage-free splits. ≥5 training seeds. Paired hierarchical bootstrap.

## Labor split (agreed, unchanged)

- **Codex:** feasibility spike + physics (MuJoCo cable/rod + online loop; bounded-excitation envelope; safe-probe co-design + A1; noisy-reference pilot S11), virtual-gauge extraction, excitation design, **interpretable residual/linear-sysID baseline (S13)**, **recovery controller (S12)**, **contact/safety + real endpoint-contact extraction (S14)**, **optional-contact profile screen (S15)**, **matched contact-enabled C1/S pilot (S16; my approval S17)**, **the bounded task/contact/controller redesign + reference-lifecycle fix (next)**, evaluation-sized closed-loop controller comparison (pending).
- **Claude (me):** fault-injection + sensor-realism model (S6), two-layer evaluation harness + metrics + stats (S7, safety gate S10, pairing guard S11), diagnosis-estimator front + window contract + oracle + seam adapter (S8; synchronous feature S10; cos/sin/amplitude S11; coefficient-distance rung + 768/16 window S12), synchronous-detection floor analysis (S9–S10), **shared recovery-seam regression `test_recovery_seam.py` (S14; approved S15)**, matched temporal-attribution net + RMA latent (next, needs torch + frozen data), Slot-8 verification demo, default-writer artifacts (Technical Report, Accessible Piece, Study Guide Pass 2 — Codex reviews).
- **Shared:** the plant→signals→estimator schema (in force, +A1), fault library, Reproducibility Packet, references reconciliation (Phase 2), **`utils/synchronous.py`**. The **"does attribution improve control" headline experiment is shared** at the diagnosis→control seam (both ends pinned end-to-end in prototype by the seam test).

## Constraints / environment (unchanged, load-bearing)

- **Simulation-only, one desktop:** Windows 11, Ryzen 7 8700F (8C/16T), RTX 5060 Ti **16 GB VRAM** (Blackwell/sm_120), 32 GB RAM, Python **3.12.10** in `./venv` (use `.\venv\Scripts\python.exe` / `pip.exe`, never bare). Free/OSS, commercial-use-friendly only.
- **venv has:** numpy 2.5.1, scipy 1.18.0, scikit-learn 1.9.0, matplotlib 3.11.0, mujoco 3.10.0, pandas 3.0.3, control 0.10.2, gymnasium 1.3.0, pytest 9.1.1. **PyTorch NOT installed** — install CUDA build when building the learned rungs (verify sm_120 kernel actually runs), pin immediately.
- **Running packet tests:** from `Reproducibility Packet/`, `..\venv\Scripts\python.exe -m pytest tests/` (each test self-inserts `scripts/` on `sys.path`; no conftest). Running a script: from `Reproducibility Packet/scripts/`. **Full packet: 148 tests green (S17).** Console note: set `PYTHONIOENCODING=utf-8` when a script prints unicode math symbols (Windows cp1252 default errors otherwise). When running my scratchpad reproduction scripts, run them **from `Reproducibility Packet/`** (they anchor the packet root via cwd).
- **Packet `.gitignore` ignores `*.npz`** (+ caches/logs); small JSON/CSV/MD result artifacts are intentionally tracked (incl. `results/optional_contact_profile_screen/`, **`results/matched_contact_enabled_pilot/`**).
- **Software-engineering standard for every script:** `argparse` with `required=True` for input roots (or defaulted project-relative outputs), no hard-coded paths, one clear purpose, shared logic in `utils/`, docstrings, prints progress, fails loud. (`utils/` library modules + `tests/` are the import-not-CLI exception.)
- **Licensing:** code MIT, prose CC BY 4.0. No relaxed standard requested.

## Pointers

- Claim Sheet (in-force contract): `Claim Sheet.md` · plain-language: `Accessible Claim Sheet.md` · Study Guide Pass 1: `Study Guide/Pass 1 - Conceptual Foundation.tex`
- **Shared schema (in force, +A1):** `Reproducibility Packet/schema/schema-v1.0.md` — rendered in `utils/schema_types.py` + `utils/estimator.py`
- **My lanes:** sensor `utils/{schema_types,sensor_model,rng,synthetic_plant}.py`; eval `utils/{metrics,stats}.py`; estimator `utils/estimator.py`; synchronous floor `scripts/analyze_synchronous_detection_floor.py` + results; **shared seam test `tests/test_recovery_seam.py`**. Tests: `tests/test_{sensor_model,metrics,stats,estimator,synchronous_detection_floor,recovery_seam}.py`.
- **Codex's plant/control lane:** `utils/{cable_mechanics,cable_plant,online_loop,recovery_control,residual_baseline}.py`, `scripts/{make_mujoco_plant_trace,run_feasibility_spike,run_bounded_burst_sensitivity,screen_synchronous_safe_probe,run_noisy_reference_pilot,screen_optional_contact_profile,run_matched_contact_pilot}.py`, `tests/{test_cable_plant,test_feasibility_spike,test_online_loop,test_safe_probe_screen,test_noisy_reference_pilot,test_recovery_control,test_residual_baseline,test_optional_contact_profile,test_matched_contact_pilot}.py`; artifacts `results/{bounded_burst_sensitivity,synchronous_safe_probe,noisy_reference_pilot,noisy_reference_pilot_threshold_followup,optional_contact_profile_screen,matched_contact_enabled_pilot}/`.
- **Shared:** `utils/synchronous.py` (Codex S9) + `tests/test_synchronous*.py`.
- **Active Phase-2 chat:** `chats/Claude-Codex/Phase 2 Integration and Config Freeze/…- Active.md` (my S17 turn = tail, 1140 lines; **no open review loops between agents**). Concluded chats have `Summary.md`s.
- Director requests: `director_requests.md` (root) — entry 1 (Claim Sheet review) non-blocking, awaiting director reply.
- My foundation `agents/Claude/Literature Foundation.md` · ledger `agents/Claude/references.md` (**no S17 entry — review/reproduction session, no new external sources read**) · progress reports `agents/Claude/Progress Reports/` (Phase 0/1 Close, Session 8, Session 16). **Next regular: Session 24.**
- Live-Run README (co-maintained): root `README.md` — **Phase 2 / In Progress**; last log entry 2026-07-21 (Codex's matched-pilot BLOCK). Untouched S17 (heartbeat check → the BLOCK is already logged by Codex; a confirming review is not a new public event).
- Scratchpad (not committed, outside repo): `s17_matched_contact_reproduce.py` (independent three-gate reproduction; note the `pair_id` split-label fix vs the RNG key).
