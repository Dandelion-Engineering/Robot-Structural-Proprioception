# Claude — Workspace

This is Claude's personal workspace for the **Robot Structural Proprioception** project. It holds my continuity documents, my Phase-0 field survey, my running source ledger, and my per-session and progress reports. Everything here is visible to the human director and to Codex.

> **How this file is maintained (added Session 104, after it stopped being maintainable).**
> `AgentPrompt.md` asks this README to make the workspace understandable *at a glance* and to
> describe **purpose, not detailed content**. It had drifted into a session-by-session journal —
> two bullets alone had grown past 30 KB each — because every session appended its own paragraph
> to the front of the relevant bullet and deleted nothing. **The rule from here: a session
> updates a bullet's current-state lead in place; it does not append another session's paragraph
> to it.** Per-session history belongs in `Session Summaries/`, review history belongs in the
> chat transcripts, and both are preserved in Git regardless. Session 104 rewrote the two worst
> bullets to demonstrate the rule (−60 KB) and left the rest; the remaining bullets should be
> pruned the same way as each is next touched, not in one sweep.

## Folder tree

```
agents/Claude/
├── README.md                          ← you are here: guide to this workspace
├── Summary of Only Necessary Context.md  ← rewritten every session; restores my working context
├── Literature Foundation.md           ← Phase-0 six-section field survey (authoritative)
├── references.md                      ← running, verified source ledger (authoritative, grows all project)
├── Session Summaries/                 ← per-session human-readable reports (HumanReport1.md, 2, …)
└── Progress Reports/                  ← director-facing progress reports (every 8th session, phase/amendment events)
```

## What each file is for (purpose, not contents)

- **`Summary of Only Necessary Context.md`** — the first thing I read at the start of a session after the framework docs. It is **completely rewritten each session** and contains only what I need to resume smoothly: current phase, what I was doing, decisions and why, open questions, next steps. Authoritative for continuity, but always current-only — it is not a history.
- **`Literature Foundation.md`** — my independent Phase-0 reading of the field (six sections: domain/methods, benchmarks, datasets/resources, failure modes, open questions, references). **Authoritative** and stable; it grounds the Claim Sheet and feeds Study Guide Pass 1. Codex maintains a separate, independent one — the divergence is intentional.
- **`references.md`** — my running source ledger (verified links/DOIs, summary + how-it-informed per entry), clustered by subfield. **Authoritative** and append-mostly; grows across the whole project and reconciles into the Technical Report bibliography at Phase 2.
- **`Session Summaries/`** — thorough, timestamped, human-readable reports, one per session (`HumanReport1.md`, `HumanReport2.md`, …). The director's audit trail of how the work evolved.
- **`Progress Reports/`** — director-facing progress reports at the Accessible-Piece bar, written on my sessions 8/16/24/… and on any session that closes a phase transition or an approved Claim-Sheet amendment. **My next regular is Session 112.** Contents: `Progress Report Phase 0 Close.md` (Session 2), `Progress Report Phase 1 Close.md` (Session 5), and the regular reports at Sessions 8, 16, 24, 32, 40, 48, 56, 64, 72, 80, 88, 96 and 104. Each report is its own record of what it covers and of the arc it reports; the only thing worth carrying here is **review status**, because that is the part a reader cannot get from the file itself. **Jointly approved, loop closed — do not reopen:** 48 (`f01aa7d7`), 56 (`83c527c`), 64 (`b0ff7496`), 88 (`58276bb4`), 96 (`c824173c`). **Read in Codex's general recent-work review with no correction to carry, so no explicit cycle ever opened:** 72. **Written and still unreviewed by Codex:** 80 and 104 — if Codex opens a loop on either, closing it is mine.

There are no scratch/temporary files tracked here at present; transient working files go to the session scratchpad outside the repo, not into this folder.

## Files I own or co-own outside this workspace

- **Root [`README.md`](../../README.md) (the Live-Run README)** — the public-facing status page. I created it in Session 1; it is **co-maintained** by both agents and updated per `Playbooks/live-run-readme.md` when a session closes a phase, finishes an outward-facing artifact, or produces something genuinely noteworthy. Its running log is append-only and lean by design, **so the log is its own history and I do not restate it here**. **Current state: Phase 2 — Execution, 🟡 In Progress, banner 2026-08-09.** My Sessions 105 and 106 added nothing and changed nothing, including the banner date, and in each I re-read the playbook rather than reasoning from memory: an internal documentation review round is none of the three triggers, and "Last updated" tracks the **public state**, not the session count. My Session 104 wrote the last entry. **The rule I hold myself to on this file:** an entry earns its place only if a stranger would care; an entry that leaves an earlier entry's forward-looking sentence standing after it has gone stale is a defect, corrected by appending a dated successor and never by editing the entry that went stale. **And the open debt:** the entry reporting the capacity read's result does not tell the reader that the reader-script as first written could not have read the finished sweep at all. Dated entries are never edited, so that obligation now belongs to the Technical Report.
- **[`director_requests.md`](../../director_requests.md) (project root)** — the project's single append-only record of director-only work. I created it in Session 5 at Phase-1 close; first entry is the non-blocking *Claim Sheet ready for director review*. Co-owned (either agent appends; the director replies in place).
- **[`Claim Sheet.md`](../../Claim%20Sheet.md)** — the project contract. I drafted it (Session 2, default writer); Codex reviewed and edited it (its Session 2); I re-reviewed and approved the same state (Session 3). It is now **agreed by both agents** — the authoritative contract. Changes from here run through the amendment protocol. **It now carries an `# Amendments` section after Slot 15, and the first amendment to it is A2** (A1 amended the schema, not this sheet, and is recorded in place there).
- **[`Accessible Claim Sheet.md`](../../Accessible%20Claim%20Sheet.md)** — plain-language companion to the contract. I wrote it Session 3 (default writer); Codex reviewed/edited it (its Session 3); I re-reviewed and approved the same state (Session 4). **Agreed by both agents** — review loop closed. Kept in exact sync with the Claim Sheet, **including its amendments.**
- **AMENDMENT A2 — IN FORCE AS OF 2026-08-05.** Direction **Option C**, jointly settled by both agents in our Session-74 turns; Codex assigned me the draft under the default writer convention (my Session 75), reviewed and edited it (its Session 75), I re-reviewed and made one `+1/-1` technical correction (my Session 76), and **Codex approved those exact bytes unchanged in its Session 76 — the two-file loop is closed.** The in-force state is `Claim Sheet.md` blob **`baa8fd53…`** and `Accessible Claim Sheet.md` blob **`203aab77…`**; both remain **pure insertions** versus the pre-A2 contract (`+146/-0` and `+103/-0`), so nothing above Slot 15 in either document was rewritten. *(Superseded, do not review: `d4c2fea2…`/`5bd4a93d…`, the `d67d22c4…`/`203aab77…` pair, `ad4e42cd…`/`365fe2df…`, and `7206bb77…`/`75fedf70…`.)* The approval fired both amendment duties in **Codex's** session — its progress report and the public README milestone — because they belong to the agent writing the approving turn. What it does: keeps both ladders, changes **no** bar in Slot 11, adds a scope bound on the sentence a success licenses, draws a new boundary in Slot 12 (a structural null is a *hypothesis* failure only where the screen found signal — which makes our clean negative harder to claim, not easier), adds the payload-bounded non-transfer shape to Slot 13, and adds payload-stratified reporting to Slot 7 (a **reporting** rule only: no per-stratum bars, no post-hoc stratum selection, pooled bars unchanged). Two things in it are mine rather than transcriptions and I flagged both to Codex to push on: the **0/0/0/0 recount** (val's and test's apparent single-severity coverage was established at 0.000/0.050 kg, which the assignment reserves for *dev*, and collapses at the masses those splits actually carry — sourced from the assignment file, not from either result artifact), and the **no-regeneration correction** (Option C inserts no severity, so no seed ordinal shifts and the delivered development set is not invalidated by A2 — which retires the "slated for full regeneration after A2" expectation both agents have carried since Session 33). Two things in it are mine rather than transcriptions, and Codex independently confirmed both rather than accepting them: the **0/0/0/0 recount** and the **no-regeneration correction**. **Do not reopen either, and do not edit A2's status lines** — the amendment is deliberately self-resolving so that being in force never requires a post-approval edit that would create bytes nobody approved.
- **[`Study Guide/`](../../Study%20Guide)** — director-facing study guide. `Pass 1 - Conceptual Foundation.tex` (+ compiled PDF) written Session 3; Codex reviewed/edited (its Session 3); I re-reviewed, independently rebuilt the PDF, and approved the same state (Session 4). **Agreed** — review loop closed. Pass 2 is produced at Phase 3.
- **[`Reproducibility Packet/schema/schema-v1.0.md`](../../Reproducibility%20Packet/schema/schema-v1.0.md)** — the shared `plant → signals → estimator → controller` data contract both Phase-2 lanes build against. Co-owned: my v0.1 sketch → Codex's v0.2 plant-side revision → my v1.0 handoff (S4) → Codex's v1.0 edited review state (S4) → my **same-state approval (Session 5)**. **Jointly approved and in force**; changes now run through the amendment protocol. I built the sensor lane against it in Session 6. **Amendment A1** (contact/safety role widths: `contact_state[2]`, `safety_flag[7]`) was proposed/implemented by Codex (S9) and reached my **same-state approval in Session 10 — A1 is now jointly in force**.
- **[`Reproducibility Packet/README.md`](../../Reproducibility%20Packet/README.md) — the packet runbook (co-owned).** The numbered, step-by-step account of how an outside reader reproduces every result the packet holds; it is the document the Phase-3 fresh-environment validation is run against. Both agents write the steps covering their own executables and review the other's. Current state: Steps 1–29, ending with the Stage-1 capacity sweep and its pre-registered read. **Steps 28–29 are jointly approved at blob `a985108e` (Codex Session 105 reviewer-edited, me Session 106) — that loop is closed.** Codex's review found that the execute command I displayed could not do what its prose said (execute mode takes the run label from the authenticated plan, never from the argument) and that a fresh clone cannot substitute rebuilt anchors for the ten the executable authenticates by digest; both corrections stand. Two of the three Phase-3 assembly obligations recorded against this file are discharged (the sweep runbook; the analyzer boundary-block scope rule); the third — a clean-machine recovery path for the 55 git-ignored checkpoints — is **disclosed as a limitation rather than closed**, because the reader authenticates checkpoints by digest and cross-machine bitwise reproduction is not established. **The packet's own [`.gitignore`](../../Reproducibility%20Packet/.gitignore) is part of this lane:** rules for runbook-generated scratch output live there rather than in the repository root, so they travel with a packet-only copy; my Session 106 completed the enumeration (nine rules, each verified to fire, with six tracked results trees verified not to) and it is open on Codex at blob `fd106b95`.
- **[`Reproducibility Packet/scripts/`](../../Reproducibility%20Packet/scripts) — sensor lane + evaluation core (mine):**
  - **Sensor lane (Session 6):** the sensor-realism + fault-injection model — `utils/schema_types.py`, `utils/sensor_model.py`, `utils/rng.py`, `utils/synthetic_plant.py`, `scripts/make_synthetic_plant_trace.py`, `scripts/run_sensor_model.py`, `tests/test_sensor_model.py`. Built against schema v1.0. The shared interface structs in `schema_types.py` (`PlantStepState`/`PrivilegedRecord`/`observable_sources`/`FaultSpec`) went through the review cycle with Codex in **Session 7 and are now jointly approved (loop closed)** — Codex edited them (lossless `PlantStepState`, `qd_obs` validity fix, shared `FaultSpec`) and I re-reviewed, verified end-to-end, and approved the same state.
  - **Evaluation harness core (Session 7):** `utils/metrics.py` (two-layer success-bar metrics: macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept; control `J_5s` + tracking-reduction %), `utils/stats.py` (paired hierarchical bootstrap), `tests/test_metrics.py`, `tests/test_stats.py`. In **Session 8** Codex's cross-review corrected four defects here (truncated-`J_5s` guard, tie-safe risk–coverage, OOD operating point = 95% unknown-detection *sensitivity*, crossed pair×seed bootstrap); I re-reviewed against the contract, reproduced the evidence, and **approved the same state (loop closed)**. I also added the missing `coverage_at_risk` metric (Claim Sheet line 118). In **Session 10** I wired the Slot-7 **"no safety regression"** gate to the now-in-force `safety_flag` (Amendment A1): `safety_incident_rate`, `safety_flag_rates`, `safety_regression_delta`, scored from privileged truth and composing with the crossed pair×seed bootstrap (+4 tests). In **Session 11** Codex reviewed this and added one fail-loud pairing guard (`safety_regression_delta` requires matched `[T,7]` C1/S shape before differencing); I re-reviewed and **approved the same state (loop closed)**.
  - **Diagnosis-estimator lane (Session 8; review loop closed Session 9):** `utils/estimator.py` — the §D `EstimatorOutput` contract, the past-only windowed front-end (`WindowFeatureExtractor`; the concrete `W`/stride realization), the interpretable **detection + calibrated-abstention** rung (`WindowNoveltyDetector`), the allowlisted **oracle** interface, and the `run_online_rollout` seam adapter (`EstimatorCommandPolicy`). Tests `tests/test_estimator.py`. In **Session 9** Codex's cross-review corrected three contract issues (fixed-shape `[W,D]` startup padding, per-channel measurement-time slopes, causal oracle onset) + output/trace validation hardening; I re-reviewed against the schema, reproduced the evidence with an independent 27-check script, and **approved the same state (loop closed)**. The learned attribution net + RMA latent are specified (shared front-end + `W`) and trained post-config-freeze. In **Session 10** I added the **synchronous (lock-in) feature** to `WindowFeatureExtractor` — a per-channel harmonic amplitude at the 0.8 Hz probe (shared `utils/synchronous`, per-channel grid, gated on ≥1 full probe cycle), the interpretable realization of the ~100× detector headroom; this moved the recommended window from `W=512` to **`W=640`** (a 512-sample window can't span a full 0.8 Hz cycle). `stride=8`; both still pilot-sweep proposals, not frozen. In **Session 11** Codex's cross-review corrected the feature from **amplitude-only** to **cosine + sine + amplitude** (amplitude alone discards phase, and the mechanics screen measures the coefficient-vector distance `‖coeff(fault)−coeff(reference)‖`, which amplitude cannot reconstruct); I re-reviewed, reproduced the math, the real feature path (bit-identical to an independent solve), and Codex's real-MuJoCo numbers (2.22× coefficient distance vs 1.77× amplitude-only), and **approved the same state (loop closed)**. In **Session 12** I built the second interpretable rung, **`CoefficientReferenceDetector`**, which consumes the retained `[cos,sin]` *jointly*: it scores the dimension-normalized, healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` of a live window to a healthy calibration reference — the deployable analog of Codex's mechanics safe-probe screen. Shared module-level `synchronous_coefficient_vector` / `coefficient_reference_distance` express the pilot's exact statistic (pinned bit-equal by a coherence test), so the deployed rung's margin *is* the pilot's margin. It is detection-only (abstains on fault type, like the novelty detector); its threshold is frozen on healthy calibration with a **fail-loud guard** that refuses a calibration set too small to resolve the requested false-alarm tail (≥`ceil(min_tail/far)` values) — the pilot's undersized-calibration BLOCK turned into a hard precondition. Session 12 also moved `RECOMMENDED_WINDOW` from `W=640`/`stride=8` to the pilot-advanced **`W=768`/`stride=16`** (the only suite-S cell clearing the ≤5% worst-alignment false-alarm screen in Codex's follow-up; still a pilot proposal, not frozen). Handed to Codex for review. In **Session 13** I closed that owner loop: Codex's cross-review made two corrections — it narrowed my "deployed margin = pilot margin" overclaim to a shared *score statistic* only (the threshold/reference/persistence still own the margin), and added a re-fit guard that atomically replaces the reference and invalidates the threshold + detection latch so a stale operating point can't survive a changed reference — and I re-reviewed both (reproduced independently with a 26-check script) and **approved the same edited state (loop closed)**. In **Session 14** I built the shared **end-to-end recovery-seam regression** `tests/test_recovery_seam.py` (a mechanism test on my `EstimatorCommandPolicy` socket: a localizing attribution stand-in sustains active inverse-gain compensation that restores delivered torque over a real multi-step rollout, while a detection-only/unlocalized stand-in stays nominal — the "does attribution improve control" property in mechanism form, explicitly not a `J_5s`/tracking result). Handed to Codex for first review. In **Session 15** I closed that loop: Codex's S14 review found a genuine fixture defect — the `FixedDiagnosisStandIn` re-stamped `detection_time_s` with the *current* decision time every step instead of latching the first-detection time (schema §D: "the time change was *first* flagged"), and it passed the validator only because `detection==now` is a valid special case of `detection≤now`, so the fixture-level correction was the only possible guard — added a reset-able first-detection latch (the same idiom the three real detectors use) plus a regression, and handed it back; I **owner-re-reviewed the diagnosis and the implementation, reproduced both independently (9-check script), and approved the same state (loop closed)**.
  - **Synchronous-detection noise-floor analysis (Session 9):** `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/` + `tests/test_synchronous_detection_floor.py`. Drives the real gauge pathology stack to show the detector-referred noise floor at the 0.8 Hz probe is ~0.1 microstrain — ~100× below the per-sample mechanics-gate floor — so the bounded-burst differentials Codex's gate marked BLOCK are detectable with wide margin. A detector-side reframing of the excitation blocker; proposes a synchronous feature for `WindowFeatureExtractor` and "W ≥ one probe cycle". Detection floor only (not attribution); nothing frozen. In **Session 9** Codex reviewed and corrected it (phase-invariant joint regression in the new shared `utils/synchronous.py`, full-cycle W=640, honest thermal/surrogate wording); I re-reviewed independently (18-check script, reproduced the old-method phase spread + the floor bit-for-bit) and **approved the same state in Session 10 — loop closed**. The proposed synchronous feature is now **built** (see the estimator lane above). **Session-36 correction to how this artifact is *used*, not to the artifact:** it reproduces exactly (`0.4053` against the stored `0.4052568`), but the number it publishes is `nes_mean + 5*nes_std` — a five-sigma **detection threshold**, not the noise floor it has been called since Session 9 — and it is measured **per gauge** at **`W=640`**. Session-35's Protocol P compared it against a four-gauge statistic at `W=768`, which is three configuration mismatches at once. The errors ran in opposite directions and nearly cancelled (the coherent bar is 8% higher), but the naming is what propagated, so any future use must re-derive the threshold in the configuration it will judge. **Session-37 correction on top of that (Finding F):** re-deriving it was still not enough, because the re-derived number described the *wrong operation*. This script's null — at any window or aggregation — is the distribution of a **single** window's statistic, while Protocol P's `D` is a **difference between two** windows. Measured at `W=768`: single-window five-sigma `0.4388`, difference five-sigma `0.6526`, ratio of means `1.424` against a theoretical `sqrt(2)`. And the correct value cannot simply be substituted, because Stage A/B differences are **matched on `(sensor_seed, pair_id)`**, so common random numbers cancel the sensor term and no sensor-only bar bounds them at all. Protocol P v2 therefore **retires this threshold as a gate entirely** and commits a separate packet artifact — a sensor-only *difference* null (`scripts/analyze_synchronous_difference_null.py` → `results/protocol_p/sensor_only_difference_null.json`). **Superseded description, corrected in Session 46:** the "validity tripwire on Stage C" role above is the v2 reading. In the in-force v2.3.3 this is **Stage 0**, and §8 gives it *no* gating role at all — it sets no threshold and gates nothing, its only corroboration is that its Q95 falls inside the real-plant fixed-trace per-cell range, and the protocol's operative null is Stage C's `Q95_c`. I **built** the script in Session 46 to that specification; it has not been run. A second Session-37 measurement (Finding G) shows the difference statistic is **thermally self-cancelling**: its null is unchanged (p95 `0.391`/`0.398`/`0.396`) across per-window excursions of 0 °C, the realized 0.5113 °C, and the committed 3 °C, because a shared deterministic temperature profile cancels in a difference.
  - **Severity read-out + severity-quality screen (Session 22):** `SeverityRidgeHead` in `utils/estimator.py` — the first *deployable* severity read-out in the project (standardized closed-form ridge on the existing `WindowFeatureExtractor` summary, suite-agnostic because a channel the suite lacks has exactly zero training variance and standardizes away). Every recovery number recorded before this came from an oracle severity or a pinned stand-in constant, so severity-estimation quality was an unmeasured term sitting under every control result. `scripts/screen_severity_estimation_quality.py` + `tests/test_severity_estimation_quality.py` + `results/severity_estimation_quality/` measure it in two parts: an analytic map of where the controller's actuator multiplier is *severity-sensitive* at all (it is flat below `1/cap`, and the `minimum_gain_remaining` floor bounds that interval from below), and a matched C1-vs-S read-out accuracy measurement whose held-out estimates are then pushed back through the real multiplier. The same session folded in the carried `null_std` floor nit (`CoefficientReferenceDetector` now shares `WindowNoveltyDetector`'s `_SCORE_STD_FLOOR`; provably a no-op on every recorded artifact). Codex's S22 review found a real defect — the predeclared severity grid used 0.55 and 0.40 *because* they bracket the cap-2 kink, and never included the kink itself at 0.50, which is a setting the deficit screen records, is above the bar, and is where the multiplier is one-sidedly sensitive. Codex added it, separated the boundary from the strictly capped interior throughout, and made the JSON strict; I re-reviewed with a 60-check independent audit and **approved the same state in Session 23 (loop closed)**.
  - **Cap-boundary action screen (Session 23):** `scripts/screen_severity_action_boundary.py` + `tests/test_severity_action_boundary.py` + `results/severity_action_boundary/`, plus `leave_one_group_out_residuals` in `utils/estimator.py`. Answers the question the corrected severity screen leaves open — what the suites' boundary disagreement is actually worth in tracking. Three parts: a **held-out severity uncertainty** (the in-sample dispersion the head reports understates the true one by 1.59x for C1 and **5.72x** for S, which *inverts* the suites' apparent reliability and must never reach the controller's confidence gate); the **paired quantity measured** with each suite's recorded held-out estimate driving the real controller (mean **−0.1177%**, worst 0.5154%, against a 10% bar); and a **fixed-multiplier sweep** that converts multiplier units into contract units and bounds the whole route (3.81 pp across the entire swept range, so no read-out difference reaches the bar at that boundary). The construction's validity rests on common-random-number reuse of the Step-15 trajectories, which is checked rather than assumed: 8/8 reference arms reproduce the committed `J_5s` at exactly 0.000e+00. Also recorded: exact restoration of the gain realizes only 93.2% of the analytic `deficit -> reduction` ceiling, so the deficit screen's gate is optimistic by about that margin. Codex's S23 review made three corrections — my leave-one-seed-out figure is a *calibration-role* diagnostic rather than a disjoint held-out one (the ridge penalty was selected on the same groups), so the absolute suite ranking it implied is not warranted; the multiplier sweep is an **envelope over the swept range**, not a bound on any read-out (my own arms contain the counterexample: extend the range to no-action and the span is 10.81 pp, above the bar); and the seven audit booleans were computed but only one of them gated. I reproduced all three from the upstream artifacts, agreed, and **approved the same state in Session 24 (loop closed)**.
  - **Class-probability channel screen (Session 24):** `scripts/screen_actuator_probability_channel.py` + `tests/test_actuator_probability_channel.py` + `results/actuator_probability_channel/`. Measures the graded class-probability channel (the recovery action's response to how confident the diagnosis is) at the condition the deficit screen actually **selected** (`actuator_gain_remaining_0p25`), not at the cap boundary. Two structural facts make it exact rather than sampled: the severity channel is **flat** there (the cap binds for every estimate ≤ 0.50, ~25x the recorded error scale from the true 0.25), and the probability channel is closed at both ends by recorded controller constants (`source_probability_threshold` below, `maximum_gain_compensation` above), so `m = 1 + p` and the sweep covers the channel's **entire reachable set** — a reachable-set span rather than a chosen-range envelope, which is the distinction Codex's S23 review taught. Result: the widest paired difference two gate-clearing suites can produce is **5.07 pp** against a 10-pp bar, reported in the contract's own `100·(J_C1−J_S)/J_C1` units; the gate crossing is reported separately at 10.82 pp because that is an authorization difference, not a probability-precision one. Also answers Codex's open §5 question: the realization at 0.25 is **57.5%** of the analytic ceiling, not 93.2%, because the action is cap-saturated (restoration needs m=4, cap allows 2). **Codex reviewed it (its S24) and I approved the corrected state same-state in Session 25 — loop CLOSED.** Two overclaims in the framing above were corrected: the six-point sweep is a *sampled empirical envelope*, not an exact reachable-set span (constants fence the input multiplier set, but the nonlinear tracking response between samples is only sampled); and the screen does **not** close the actuator class, because holding class/severity/uncertainty/abstention fixed leaves calibrated *authorization* unmeasured (the 10.82 pp gate-crossing is exactly that channel). The 5.07 pp number is unchanged; only the claims narrowed.
  - **Structural separability screen (Session 34):** `scripts/screen_structural_separability.py` + `results/structural_separability/` (two runs: `pooled_trajectories/` and `diagnostic_trajectory_only/`). The stop/go instrument for `BLOCK_GATE4_LADDER_PENDING_STRUCTURAL_SEPARABILITY_CHECK`. It refuses any split but `dev` **in code**, so it cannot spend validation or test while answering a development question. Its design exploits a property of the delivered assignment: each fault setting's eight development runs occupy the *same* eight background-condition cells run-for-run, so every contrast is context-matched, cross-validation folds hold out a whole cell (both of its runs), and the per-cell difference supports an exact 2^8 paired permutation null. Two rungs on one window set — the interpretable `CoefficientReferenceDetector` with its reference fitted on the seven training cells, and a small L2 logistic probe on the raw `[W,D]` tensor reduced to 16 mean-pooled time bins plus per-column std and valid fraction, reported as the *maximum over a regularisation grid* (an optimistic bound, with the null applying the same maximisation) — plus a per-registry-column paired attribution that answers *which* channel carries any signal, and an actuator positive control so an empty table can be told apart from a broken pipeline. **Result: neither suite separates structure at either development severity; the positive control reaches 0.891/0.859; no gauge column reaches significance in any arm; the one consistent structural signature is `imu_obs[2]`, a C1 channel.** This is a development screen, not a confirmatory result: it fits nothing carried forward and selects no hyperparameter entering the confirmatory protocol. **Corrected in Session 35 (forward, per Codex's catch):** the per-channel report hard-coded the exact 8-cell sign-test floor into both rendered reports; at `n_cells = 4` the floor is p = 0.1250, so the `p <= 0.05` listing filter can never admit a column and the diagnostic report's empty table was arithmetically forced rather than an empirical null. The floor is now derived from `n_cells` and the forced-empty case says so in the report. **Also superseded in Session 35:** the accompanying severity ladder's conclusion that the reserved grid is "2x to 40x too mild" was measured at the delivered (unscreened, weaker) probe amplitude and is withdrawn; the separability result itself stands, since both suites saw the identical excitation.
  - **Gate-4 learned attribution rung (Session 77):** `utils/attribution_net.py` + `tests/test_attribution_net.py` (64 tests). Slot 9's **rung 1** of the model-capacity ladder — the compact causal dilated temporal-convolutional attribution network, 39,594 parameters, 1,023-sample receptive field — built behind the same `DiagnosisEstimator` interface and `[W, D]` front-end the interpretable rungs use, so the recovery controller and the evaluation driver stay agnostic to which rung is installed. It has **no training loop and no fitted weights** by design. Four properties are built in and *measured* rather than asserted: identical shape and parameter count across C0/C1/S (so an S-over-C1 advantage cannot be capacity); strict causality including the normalization (a `GroupNorm` in the same slot would mix a window's later samples into its earlier features with no shape change and no error — the test measures causality by perturbation and a companion test shows the ordinary normalization failing it); refusal to report an attribution before trained weights with a recorded provenance are attached; and validation-owned thresholds that default to always-abstain / never-flag. `severity_uncertainty` stays `+inf` even when fitted — the raw head scale is exposed only as `raw_severity_scale`, because S24 measured an in-sample scale understating true predictive error by 5.72× for S. **Review cycle CLOSED at `c4fa3c63` / `5a401ca1`** — Codex re-opened my returned Session-78 state in its Session 78 and explicitly approved the same bytes I had approved, so both approvals name one state. Do not reopen. The round history is kept because it is the point: Codex reviewed it in its Session 77, approved every design choice, and blocked one real defect: PyTorch's `load_state_dict` is not transactional, so a failed replacement left *mixed weights carrying the previous run's provenance*. I reproduced that against my own blob with my own instrument (both states driven in one process from a common driver) and accepted the diagnosis and the repair in full. **My Session-78 owner re-review then found one new silent defect in the repair itself:** installing the validated weights as `self.net = candidate` replaces the estimator's network *object*, so anything that captured `estimator.net` earlier — an optimizer built over `estimator.net.parameters()` before a checkpoint is resumed, which is the order the trainer will use — is left training an orphan while the estimator answers from a different module, with no error and a falling loss. Fixed by keeping every line of Codex's candidate-copy validation and writing the validated tensors *into* the live network instead of rebinding it; three tests added, of which the optimizer test is the evidence. **Both of Codex's Session-77 rulings are accepted and acted on:** development-only fitting on the delivered `dev` partition is authorized under five narrow bounds, and the Config Freeze Readiness Review governs the ordering — model implementation and dev fitting come *before* the final freeze. The stale docstring was corrected forward in **two** places, not one; the second instance was in `scripts/utils/__init__.py` and was found by looking rather than assumed absent.
  - **The development-only fitting contract (Session 78):** `utils/dev_fit_contract.py` + `tests/test_dev_fit_contract.py` (93 tests). The executable form of Codex's ruling: which rows a fit may read (`dev` only, of the matched suites, re-checked at the point of *consumption* because a caller-assembled list is the path no filter guards, refused rather than returned empty, and always accompanied by a census that states its own denominator); what the fit plan is (five explicitly named seeds crossed with two matched suites, with an unbalanced set refused because a paired C1-vs-S comparison is only paired if both arms ran the same seeds); and what every development checkpoint must be able to say about itself (the exact development-only authority string, the data root's **bare name** rather than a path, the manifest/config/assignment digests, the suite, the seed, the training-protocol code identity, and the checkpoint digest — with every rendering validating before it is produced). It imports neither MuJoCo nor PyTorch, checked in a fresh interpreter. **Deliberately not the trainer**, which is the fourth thing Codex's ruling names: refusals have to be reviewable on their own, and a role check inside a training loop is only exercised by running the loop. **Review cycle OPEN at my Session-80 state `9d6ecfea` / `d4202c8e` (92 tests), and Codex owns the turn.** *(Round 3, Session 80: Codex blocked my Session-79 state on **two more real defects** and repaired them, and I reproduced both against my own blob before touching anything. The better of the two is a genuine Python trap — `$` may match immediately **before** a final newline, so four fields whose own error messages promise an exact 64-character digest were accepting 65-character, two-line values; those are the identities by which a later reader decides which manifest, configuration, checkpoint and training code a development artifact names. The second turned four malformed record types into foreign `TypeError`/`AttributeError` escapes below the module's own exception boundary. I kept every predicate and every message literal of both repairs, and I checked the thing its fix made load-bearing: dropping the `^…$` anchors is safe **only** because every call site uses `fullmatch`, and I swept `fullmatch → match` and `→ search` at each site to confirm Codex's own new cases guard the choice. **My owner re-review then found one more, and its shape is different from the previous five rounds and worth stating: Codex's repair is COMPLETE for the thing it repaired** — measured over 110 hostile-input cells, `DevFitProvenance.validate()` now has zero foreign escapes and zero silent accepts. What I found is one function over, not one layer below. Asking what Codex's *family* of four fields was standing in for gave a property of the **module** rather than of four fields, so I enumerated the module: every public entry point crossed with a hostile-value battery, 140 cells, reporting **both** failure modes rather than only the one I went looking for — which is the finding's whole provenance, because my first version of that probe printed foreign exceptions only and could not have seen what it found. **One cell was silently ACCEPTED: `code_identity({})` returns `{}` without a word, and the record refuses exactly that mapping one step later.** The routine that *builds* bound 4's code identity hands back the value the routine that *audits* it rejects — the second time in two sessions this module has had two guards disagreeing about one quantity, and both found the same way, by calling both guards on one value. Repaired not by copying the record's check into the producer (that closes the instance and leaves two copies of one rule, which is how they drifted apart) but by stating the rule **once** as `require_code_identity`, called by both, with the producer ending by asserting it as a post-condition. Two smaller in-domain refusals travelled with it, including one worth its sentence: `Path(None)` raises `TypeError` one line **before** the `is_file` guard that exists precisely to refuse a bad path, so the guard written for the case could never see its most likely instance. **The sweep then caught something in my own repair** — the post-condition made the in-loop label check redundant and therefore undeletable-without-notice, so the label rule now lives in one place and is swept there. Sixth consecutive round with a defect one layer below the repair that had just landed; this time below mine. Final state: **42/42 mutation cases caught, zero survivors, zero bad anchors, two identical passes, restore byte-identical**, and the module's silent-accept count is 0. **Forty foreign escapes remain and I deliberately did not close them**, all caller-type errors in `require_dev_only` / `select_dev_rows` / `require_complete_matched_plan`: every one is a loud crash and **no bound anywhere in the module is permeable**, so converting loud-in-the-wrong-domain into loud-in-the-right-domain is decoration unless something depends on the domain. That choice favours me, so it was measured, stated, and handed to Codex to rule on rather than taken.)* **Superseded, never review: `73e5e743`/`3959ff28` (mine S78), `6541cebc`/`9df7d7f7` (Codex S78), `2448ad4d`/`2aa5f762` (mine S79), `872c6b12`/`3125a618` (Codex S79).** Prior round-2 record: Codex reviewed my submitted `73e5e743` / `3959ff28` in its Session 78, **blocked it on four real defects and repaired them** — a duplicate `(suite, seed)` pair passed the completeness check because the input was collapsed to a set; the point-of-consumption guard accepted both an empty batch and a `dev/C0` batch, and could not express "these rows belong to *this* suite"; the census disclosed a withheld unmatched-suite `dev` row as "non-dev"; and a data root name containing a newline turned the promised one-line provenance record into two lines. **My Session-79 owner re-review reproduced all four against my own blob, kept every line of the repair (`+57/−0` and `+141/−0`, zero deletions), and found two behavioural defects one layer below two of them.** First, the control-character rule does not deliver the property it was written for: enumerating **every codepoint** (1,112,064) found three values accepted as bare names that still split the record in two — `U+0085`, `U+2028`, `U+2029` — none of them an ASCII control character and all of them legal in a directory name. Closed by making the promise itself the post-condition (the value must be a single line by Python's own reckoning), with the control rule kept because neither rule subsumes the other and a test drives each direction. Second, two guards in the module disagreed about what a training seed is: `("C1", True) == ("C1", 1)` with an equal hash, so the set arithmetic certified as a *complete matched plan* a plan containing a bool the module's own seed check refuses outright, and an unhashable entry died with a foreign `TypeError`. Closed by checking each entry's shape before the set arithmetic — shape only, not membership, so the branch Codex's own test drives stays reachable. The Session-79 mutation sweep also found **four guards, two of Codex's and two of mine, that no test could make fail**, of which the sharpest is my own authority check: weakening it from equality to containment survived the whole suite, and would admit a checkpoint that wraps the mandated authority string in text contradicting it. Nine tests added; the final state sweeps **52/52 with zero survivors across two identical passes**. **Codex's four rulings on the flagged design choices are all accepted and in force** (the authority stays a local literal pinned by equality against two outside sources; the `dev-` config identity is required; the assignment digest is equality-bound; the data root is a bare name plus a manifest digest), as are both forward choices — the trainer and evaluation driver share `deterministic_conv_precision()` for forward *and* backward, checkpoints get raw digests and tracked source gets canonical-text ones. **LOOP CLOSED AT ROUND FOUR (Session 81): both agents approved the exact same bytes (mine S81, Codex S80), blobs bd2c0d08 / fbd941b5, 93 tests. Do not reopen.**
  - **The development-only trainer (Session 81, reviewed and revised through Session 82):** `utils/dev_fit_trainer.py` + `tests/test_dev_fit_trainer.py` (49 tests). The executable that turns the closed contract into fitted rung-1 weights: the ten arms come from `matched_fit_plan()` rather than a loop it writes, the role bound is re-checked at the point of consumption, `deterministic_conv_precision()` wraps forward **and** backward, every checkpoint carries a validated `DevFitProvenance` whose code identity is built by `code_identity()`, and a comparison cannot be reported over an incomplete plan. A refusal message is never persisted; the artifact records only which check refused. Every terminal exit is named, writes an artifact, and has a test that drives that exit and reads back what it wrote. **Codex blocked my Session-81 state (blobs `275a7a50`/`80d9722f`) on six executable defects and one missing design decision, and I preserved all six corrections in Session 82 after reproducing the central one against my own blob** — the largest being that the persisted record deliberately retains measurements delivered *after* an earlier decision, and my window slice did not reapply the online availability cutoff, so a sample arriving a second late was visible to the network. **The missing decision was the training window**, and Session 82 supplies it: the delivered development role spans an ordinary and a diagnostic trajectory, so there is no single global origin, and the origin is now **derived per trajectory from the approved assignment** rather than supplied on the command line — `origin = onset + lead`, where the lead is that split's own diagnostic-probe offset from onset. For `dev` that reproduces Protocol P's already pre-registered `[1000, 1768)` exactly and gives the probe-free trajectory `[900, 1668)`, so both open at the same elapsed time after onset and only the excitation differs. One window per persisted run, recorded as such. Session 82 also found one defect in the reviewer's own state — `DevFitContractError` and `DevFitDataError` are both `RuntimeError` subclasses, so the handler added to name torch's runtime failures was filing bound violations under the missing-data exit and erasing the module's own diagnoses from the only channel that carries them. **Session 83 closed the scientific half and re-opened the code half.** Codex's Session 82 **approved the window policy** — checked against the approved assignment and Protocol P rather than against my arithmetic — and narrowed one overstatement of mine that I accepted in full: equal post-onset lead removes an avoidable timing difference but does **not** make excitation the only difference between the two trajectories, because the assignment also changes target joints and task timing. It then blocked `10054696`/`9e76923c` on four executable findings (matched identity, the persisted-label onset binding, malformed schedule controls, and mixed checkpoint generations) and returned `788fc240`/`c95bd8fb`. My Session 83 drove all four against my own blob — every one reproduced, and one case is worse than reported: a control period of 0.004 s instead of 0.002 was *accepted*, silently halving every derived step count — and kept all four implementations unchanged. **Two defects sit one layer below that repair.** The first is the serious one: the new guard refusing to fit into a directory holding an earlier attempt took an exit that writes `dev_fit_result.json`, which is the *only* record binding each checkpoint to its data root, digests, suite, seed and code identity (`torch.save` embeds nothing) — so the refusal deleted the provenance of the checkpoints it was refusing to mix with and left them orphaned beside a document reporting `fits_run: 0`, and the module's first exit destroyed the same record above the guard's position without it running at all. Both measured by running. The guard now runs before any write and refuses through a sixth named exit, `X_OUTPUT_DIRTY`, writing a filename deliberately outside the protected set. The second: matchedness was upgraded from equal counts to equal `pair_id` **sets**, and a set discards multiplicity — `C1 = [a, a, b]` against `S = [a, b, b]` has equal counts, equal sets, and two rows with no partner; now compared as sorted lists. The 23-case two-pass sweep also exposed five coverage gaps sharing one cause — the reviewer's fixture derives the label's onset *time* from its onset *index*, so its test moves both at once and either half of the binding alone catches it — all closed, and one unreachable guard removed with the reasoning recorded and the decision handed to Codex. State returned at `b9d7bb6f`/`3a81eecc`. **Session 84 CLOSED the loop and RAN the fits.** Codex’s Session 83 found two more against that state and both reproduced against my own blob: the new refusal artifact sat outside the cleanliness guard as well as outside the protected namespace, so a directory holding only it was accepted as clean and ended with two contradictory terminal records; and one docstring still carried the superseded “excitation is the only thing that differs” sentence. I kept both repairs, looked for a Finding-S recurrence in the new trigger/target collision and **measured that there is none** (the refusal document’s content is invariant, so re-writing it loses nothing), and found one real thing instead — **Finding W**: because the refusal reports through the name whose occupancy triggers it, an *unwritable* occupant of that one name turns a named terminal exit into an uncaught `PermissionError`, while an unwritable `dev_fit_result.json` refuses cleanly every time. Nothing is destroyed, it is unreachable from the authorized invocation, and it is a member of the class Codex ruled on in its Session 80, so I **disclosed rather than blocked** and handed Codex the ruling — saying out loud that the choice favours me. Approved `caa00418`/`cbc4064f`; **both agents now approve the same bytes and the executable gate is open.** **THE TEN DEVELOPMENT FITS THEN RAN** — `X_FIT_OK`, 152 examples per arm, **0 rollouts, 0 generation**, into `results/dev_fit/`. See the entry below.
  - **THE FIRST DEVELOPMENT FIT RESULT (Session 84):** `results/dev_fit/dev_fit_result.json` (tracked) plus ten `dev_fit_<suite>_seed<n>.pt` checkpoints (git-ignored by the packet’s own rule, so the tracked document is their **sole** provenance record). Ten arms — C1 and S at seeds 0–4 — 152 examples each, 76 per trajectory per suite, on CPU at every default setting, **zero rollouts and zero generation**. What it establishes and what it does not: the implementation **learns** (in-sample class cross-entropy 0.434 for C1 and 0.557 for S against the empirical prior’s 1.010; accuracy 0.870 / 0.817 against a 0.632 majority-class baseline), and everything past that is a caution rather than a result. **The persisted `final_loss` is not a learning signal** — the composite carries a Gaussian NLL with an unbounded-below `+ log_scale` term, which is what makes seven of ten arms report a negative loss and is also the term that varies most between arms, so ranking arms by it ranks them by severity-head confidence. **The seed spread swamps the effect the study is designed to detect**: paired S−C1 macro-F1 across five seeds is −0.032 with sd **0.150**, against a pre-declared bar of ≥0.05 — in-sample, at one capacity rung, but it arrives before any reserved data has been touched and belongs in the Gate-6 sample-size decision. **The direction is against the hypothesis and rung 1 cannot settle it**: per-class F1 paired S−C1 is healthy +0.100, structure −0.069, actuator −0.108, sensor −0.052 — but S is C1 plus four gauge channels at a *fixed* 39,594 parameters, so this is a statement about optimization at one rung, not about information, and Slot 9’s capacity ladder is the instrument for it. I verified the gauge channels actually reach the network before reading any direction into that. The decomposition, accuracies and per-class F1 are **probe output, not committed artifacts** — the tracked document carries only the census, provenance and loss history.
  - **The in-sample fit readback (Codex-created Session 84, reviewed and returned by me Session 85):** `scripts/analyze_dev_fit.py` + `tests/test_dev_fit_analysis.py` + `results/dev_fit/dev_fit_analysis.json`. **Codex owns these; I am the reviewer.** A zero-fit, zero-rollout read that verifies the fit ledger and every checkpoint digest, then persists the four post-fit loss terms separately (their sum is not a ranking statistic, because the Gaussian severity term carries an unbounded log-scale and goes negative), the in-sample classification metrics, and the paired five-seed spread. My Session-85 review kept every published number and changed four things: the analyzer's re-implemented loss is now bound to the trainer's own `arm_loss` as a post-condition rather than trusted as a copy; the plan cardinality and seed set are derived from the dev-fit contract instead of typed as literals; two docstring claims that measurement did not support were corrected; and the missing binding requiring the tracked artifact to name the *current* analyzer was added, which is what forced the regeneration. Tests 10 -> 30. **Session-86 correction to my own Session-85 claim:** I wrote that the derivation path "cannot be covered by the packet's own suite" and that closing the six survivors would require extracting pure functions from Codex's module. **Both halves were wrong, and Codex rejected the premise in its Session 85 rather than accepting it.** `load_authorized_examples` and `evaluate_arm` are already seams; replacing them with synthetic fixtures drives the real derivation code with no production change at all. Codex added five tests on that basis (30 -> 35) and, correctly, declined to quote a mutation score it had not measured. **My Session 86 supplied the measurement: 14 derivation-path cases, 10 caught, 4 survivors** — all five guards Codex targeted were genuinely caught, and the four survivors traced to three *degenerate fixtures* rather than to any defect in the code. A uniform class census makes `max` and `min` the same answer in both baselines; a constant paired difference makes the mean and the sample SD blind to the table's cardinality; and a loader stub returning its own count leaves the production per-suite row filter unexercised. I repaired all three fixtures — **no production code, no published number, no regeneration** — and re-swept to **14 caught, 0 survivors**, with a negative control (two semantically inert edits, both correctly surviving) confirming the harness discriminates rather than reporting everything as caught. **Test count stayed at 35: I added no test, I made three existing ones able to fail.** Returned at blob `c7b0a093…`; Codex owes the same-state review. One of the four survivors was my own gap, not Codex's — my Session-85 repair derived the paired loop's seed set from the contract and never pinned its cardinality, and the existing test that asserts the seed list reads a static tracked file, which cannot move when the code that wrote it moves. **Session 87: Codex reviewed `c7b0a093`, accepted all three fixture repairs, and corrected one false comment of mine — `sensor` IS the last key of the count mapping, and my comment said it was not — approving its own state `4481ba32`. I CONTESTED that state, because its replacement comment carries the same defect one notch smaller: it claims the fixture pins the selector against “an iteration-order or tie accident”, and a last-key selector is exactly such an accident.** Measured, two agreeing passes with both negative controls surviving: against `4481ba32`, replacing `max(...)` with `list(counts)[-1]` **SURVIVED**, while first-key, `min` and `min(proportions)` were all caught. The cause is structural — the census is built in `SOURCE_CLASS_ORDER`, so ascending counts put the majority at the *last* key — and the delivered 8/16/32/96 census is peaked on `sensor` too, so neither this fixture nor the real data separates the selector from the ordering unless the majority is deliberately interior. Repaired by reordering the counts to `(1, 2, 4, 3)`, whose majority is `actuator`: all four mutants now die, the proportions are the same multiset so every published number is unchanged, and the test count stays at 35. Returned at blob `6f29bf05`; Codex owes the same-state review. **Eighth consecutive round with the defect one layer below the repair, and the second in a row inside a repair the other agent made to my own work.**
  - **THE STAGE-1 CAPACITY SWEEP — CURRENT STATE AFTER MY SESSION 104. THE MEASUREMENT IS COMPLETE AS SCOPED: THE SWEEP RAN, C7 (THE READ-ONLY ANALYSIS SCRIPT) RAN ONCE, AND ITS TERMINAL ARTIFACT IS JOINTLY APPROVED.** Design: `Reproducibility Packet/protocol/capacity-escalation-v0.1.md` (frozen v0.1, canonical `05109d97…`). Executable: `scripts/utils/capacity_sweep.py` + `tests/test_capacity_sweep.py`. Reader: `scripts/analyze_capacity_sweep.py` (blob `b9043fa2`, canonical `7eca4016…`) + `tests/test_capacity_sweep_analysis.py` (blob `a81d35c9`, canonical `bd8c3631…`); 241 focused tests, 1,792 packet-wide. Run roots `results/capacity_sweep/stage1-run-1` (the preserved failed run — **do not delete, do not reuse the label**) and `results/capacity_sweep/stage1-run-2` (the completed run, 42 fits / 42 checkpoints / 0 rollouts). Terminal artifact: `results/capacity_sweep_analysis/stage1-run-2/capacity_sweep_analysis.json`, 89,150 B, blob `3c963059…`, canonical == raw `e381d12e…` — **approved by Codex (its S103) and by me (my S104) at the same bytes; the result loop is CLOSED.** My S104 audit was 73 checks that imported neither the producer nor the executable, re-deriving every §5.2 quantity from the design's own prose, plus a twelve-mutant whole-probe negative control in which every mutant was caught by the check that names it. **The applied §5.4 reading:** exactly one of the six frozen rows matches — *the paired curve does not have a readable shape at five points and five seeds* — and **no trend statement is permitted**. Codex's matching §5.4 half is still owed. **Nothing here selects a capacity or a threshold, licenses Stage 2, or authorizes a pilot/validation/test read.** Per-session construction and review history for this artifact lives in `Session Summaries/` (HumanReport87–104) and in the Phase-2 chat transcript; it is deliberately not restated here.
  - Codex owns the plant lane in the same folder (`utils/cable_mechanics.py`, `utils/cable_plant.py`, `utils/online_loop.py`, `scripts/make_mujoco_plant_trace.py`, `scripts/run_feasibility_spike.py`, `scripts/run_bounded_burst_sensitivity.py`, `scripts/screen_synchronous_safe_probe.py`, `tests/test_cable_plant.py`, `tests/test_feasibility_spike.py`, `tests/test_online_loop.py`, `tests/test_safe_probe_screen.py`) and the online-interface edits I re-reviewed and approved in S8. The shared harmonic statistic `utils/synchronous.py` (Codex, S9) is used by the mechanics safe-probe screen, the detector-floor analysis, and my estimator's synchronous feature — one statistic across all three. Both lanes share `utils/` and the Reproducibility Packet. **Full packet suite: 1,466 tests green (S80, 128.41 s; `tests/test_dev_fit_contract.py` collects 92 after the fifteen cases my S80 owner re-review added to Codex's 77, and Codex's S79 count of 1,451 + 15 = 1,466 exactly). Prior count 1,451 (Codex S79, 130.87 s; that file collected 77). Prior count 1,441 (S79, 126.39 s; `tests/test_dev_fit_contract.py` collects 67 after the nine coverage cases my S79 owner re-review added to Codex's 58, and Codex's S78 count of 1,432 + 9 = 1,441 exactly). Prior count 1,432 (Codex S78, 130.27 s). Prior count 1,430 (S78, 125.33 s). Prior count 1,370 (S77, 132.81 s, `tests/test_attribution_net.py` collecting 64 for the first time). Prior count 1,306 (S71, 126.03 s; `tests/test_payload_boundary_extension.py` collects 170 after the eighteen coverage cases my S71 review added to Codex's 152). Prior count 1,288 (Codex S70, 123.18 s; that file collected 152). Prior count 1,277 (S70, 127.33 s; that file collected 141 after the thirty-five tests my S70 review added to Codex's 106). Prior count 1,242 (S69, 117.26 s; `tests/test_payload_boundary_extension.py` collects 106 after the twenty-three tests my S69 review added to Codex's 83). Prior count 1,217 (S68, 119.02 s; that file collected 81 after the five tests my S68 review added to Codex's 76). Prior count 1,207 (S67, 117.49 s; that file collected 71 after the thirteen tests my S67 review added to Codex's 58). Prior count 1,189 (S66, 117.25 s; that file collected 53 after the six tests my S66 review added to Codex's 47). Prior count 1,126 (S61, 137.87 s; `tests/test_protocol_p_payload_conditioning.py` collects 105 after the eight tests the S61 mutation sweep forced and the three that close a three-way message collision, and 1,021 + 105 = 1,126 exactly). Prior count 1,107 (S60, 150.54 s; my new `tests/test_protocol_p_payload_conditioning.py` collects 86 and 1,021 + 86 = 1,107 exactly). Prior count 1,021 (S59, 143.00 s; `tests/test_protocol_p_role_coverage.py` collects 46 after the twelve tests the S59 mutation sweep forced and the two that replaced a tautological one, and 975 + 46 = 1,021 exactly). Prior count 999 (S58, 142.13 s; my new `tests/test_protocol_p_role_coverage.py` collects 24 and 975 + 24 = 999 exactly. An earlier S58 run reported 997 at 140.51 s, before the two constructed tests that kill the sweep's two surviving mutations were added; the 999 figure is measured, not extrapolated from it). Prior count 975 (re-run S57, 122.71 s; unchanged from S56, when `tests/test_protocol_p_driver.py` grew to 148 collected and 938 + 37 = 975 exactly. Session 57 changed no code, so the count is a regression confirmation rather than a new total. Prior count 906 (S54, 57.63 s; my S54 `tests/test_protocol_p_results.py` collects 71 and `tests/test_protocol_p_driver.py` collects 85, so 156 is that session's two-file total and 750 + 156 = 906 exactly. Prior count 750 (S52, 13.60 s; after Codex's S51 review edits and my S52 additions `tests/test_protocol_p_conditions.py` collects 135 and `tests/test_protocol_p_shared.py` collects 20, so 155 is the two-file focused total. Prior count 736 (Codex S51); prior count 725 (S51, 13.22 s; my S51 `tests/test_protocol_p_conditions.py` collected 111 and `tests/test_protocol_p_shared.py` 19, so 130 was that session's two-file focused total). Prior count 595 (S48, 12.32 s; `tests/test_synchronous_difference_null.py` collects **99** and `tests/test_gauge_windows.py` collects **18**, so 117 is the two-file focused total, not a single-file count — my S47 description of that file as "99 → 117" conflated the two and Codex corrected it in its S47. Prior count 565 (S46, 12.33 s): my S46 `tests/test_synchronous_difference_null.py` added 72 and `tests/test_gauge_windows.py` added 15, and Codex's S45 review took the replay-gate file from 30 to 36. Prior count 472 (S45, 11.21 s): Codex's Gate-1/2/3 contract layers plus the S32 assignment-binding and generator suites added the tests above the S26 count of 329, each reviewed and reproduced; my S43 `tests/test_cable_plant_softening_boundary.py` added 6, my S44 `tests/test_assignment_generator_screen_overrides.py` added 37, and my S45 `tests/test_protocol_p_replay_gate.py` added the last 30).**
  - **Reviewer loop in Codex's lane — CLOSED (Session 22).** My S21 edits to `scripts/screen_fault_tracking_deficit.py` (the headroom gate converted the Claim Sheet's *reduction* target into deficit units, which under-delivered it) were owner-re-reviewed by Codex in its S21 turn: it accepted the units diagnosis and the selection change, and made one narrowing edit to a generated interpretation claim (performance above the exact-restoration ceiling is not automatically generic command authority). I re-reviewed that edited state in Session 22 and **approved it same-state — the loop is closed.** My S20 edits to `screen_structural_recovery_action.py` closed the same way in Codex's S20 turn.
- **Chats with Codex** (co-owned, append-only): [`Phase 0 Coordination/`](../../chats/Claude-Codex/Phase%200%20Coordination) — **Concluded** Session 2; [`Claim Sheet Review and Division of Labor/`](../../chats/Claude-Codex/Claim%20Sheet%20Review%20and%20Division%20of%20Labor) — **Concluded Session 6** (all Phase-1 loops closed; see its `Summary.md`), concluded at the director's instruction; and [`Phase 2 Integration and Config Freeze/`](../../chats/Claude-Codex/Phase%202%20Integration%20and%20Config%20Freeze) — **Active**, opened Session 6, carries the plant↔sensor integration + `config.json` freeze coordination.

  *Its purpose, not its contents:* this is where every Phase-2 review cycle between the two agents happens — each artifact is handed off with an explicit approval, first-reviewed by the other agent, and either approved same-state or edited and handed back. The transcript is the authoritative record of which loops are open and what each agent approved; my per-session detail lives in `Session Summaries/`. **The turn-by-turn history is deliberately not repeated here** — it is fully carried by the transcript and by `HumanReport7.md` onward, and restating it in a navigation guide is what made this file hard to read.

  **Current state (end of my Session 76).** **THE MEASUREMENT IS SPENT, ITS RESULT IS JOINTLY APPROVED, AND THE ONLY OPEN STATE IS AMENDMENT A2 — NOW AT MY SESSION-76 STATE, AND CODEX OWNS THE NEXT TURN.** Codex reviewed my Session-75 A2 draft, made six bounded edits, and handed back its own explicitly approved state (`d67d22c4…` / `203aab77…`). **I accepted all six and verified the three factual ones against the artifacts rather than against Codex's report; all three are exact.** The central one is a correction I should have made myself: I had repeatedly written that payload determines whether the structural signal *exists*, when the evidence only supports that it *clears the detection threshold* — the distances at the two heaviest masses are 0.731179 and 0.642285 against thresholds of 0.762767 and 0.829649, which is a signal we cannot certify rather than one that is gone. Codex's status-line catch is the better of the two of us: both my status sentences instructed a future agent to replace the line with the approvals *after* approval, which guarantees the in-force file is one edit past the bytes both agents approved. **I then found one place the correction had not reached and made a single `+1/−1` edit:** Codex fixed the accessible sheet's noise-floor wording and left the technical sheet — the contract — saying the noise floor "does not move." It moves (mean Q95 0.416546 → 0.399002, ratio 0.958). I also declined the natural repair, "the difference is inside the cell-to-cell spread," because it is only half true — that spread is 0.0573 at 50 g and 0.0103 at 0 g against a between-level gap of 0.0175, so it holds against one level and fails against the other. The property that carries the argument is the *contrast of ratios*: signal 0.487–0.537, null 0.958 — the attenuation is in the numerator. Handed back at `baa8fd53146bb838b673946b34fe435c77d8ec06` / `203aab77f1f244f0a11943955a6f8ec123944030` (Codex's accessible blob preserved byte-for-byte) with my explicit approval. **Zero rollouts this session; lifetime total unchanged at 278;** no script, test, protocol, assignment, config, or result file touched; `config/config.json` still absent; test split untouched at zero. **Fourth consecutive session whose output is a limit on our own claim rather than a defect in an artifact — correct work, but the loop needs to close, and if my next re-review produces another wording edit rather than an approval, that is the point to say the loop costs more than it buys.**

  **Session 75 (superseded, pointer only).** **THE MEASUREMENT IS SPENT, ITS RESULT IS JOINTLY APPROVED, AND THE ONLY OPEN STATE IS AMENDMENT A2 — WHICH IS NOW DRAFTED AND WITH CODEX.** Step 5 ran once in Codex's Session 73 (127 physical rollouts, `X_CASE_EMPTY`, `mass_coverage COMPLETE`, anchor `X_ANCHOR_PASS`); Codex approved the result bytes in its Session 73 and I approved the same bytes in my Session 74 after 130 independent checks, so that loop is closed and the authorization is spent — **a second invocation of any kind needs a new joint authorization, not a retry.** This session I wrote **Amendment A2** into `Claim Sheet.md` and `Accessible Claim Sheet.md` (see the entry above for exact blobs and content) and handed the two-file state to Codex with my explicit owner approval. **Zero rollouts this session; lifetime total unchanged at 278.** No plan mode, no replay, no execute mode; the protocol file, extension document, assignment, draft config, seam, driver, every script, every test, and every result artifact are untouched (`payload_boundary.json` re-hashed after all edits, canonical `7746372f…9aa04`, unchanged); `config/config.json` still absent; test split untouched at zero. The session's substantive addition beyond the drafting is a **second-order limit on my own Session-74 disclosure**: that analysis measured what a single well-shaped flip does to *the outcome*, which is a different question from what it does to the *role-retention sentence*, and three of the seven role losses (0.050 kg dev at −5.0%, 0.100 kg val at −5.7%, 0.150 kg test at −4.1%) sit inside the same 10% band the protocol had declared untrustworthy before the measurement ran. Licensing does not move — Option B still breaks at the first mass, 18.2% out — but A2 therefore writes the aggregate sentence rather than the universal one.

  **Prior state (end of my Session 73).** **STEP 3 IS COMPLETE AND HALF OF THE STEP-4 AUTHORIZATION IS ON THE RECORD — MINE.** Codex's Session 72 gave the second independent read §13 Step 3 requires and approved the same bytes I approved, so both agents now approve canonical `15298da4…030be3` (blob `04f2bccd…`, 5,386 B) and the plan is jointly settled. This session I issued **my half** of the Step-4 execution authorization: one invocation of execute mode bound to that exact digest, with the §3.3 replay gate's one physical rollout authorized by name. **It is not in force.** §13 says the authorization is issued "by both agents"; Codex owes the other half, and nothing may run until it exists. **I also pinned the authorization's scope, because both of us had been writing a shorthand — "authorizing exactly the one §3.3 replay rollout" — that read alone would license one rollout and make Step 5, which costs up to 127, unrunnable.** §13 Step 4 calls the thing an *execution authorization* and says it *also* authorizes the replay rollout; the `also` is additive. My half therefore licenses one invocation of Step 5, once, in the §8 `X0E/XR/XA/XM-C/XL/XM-B/XZ` order, spending 0–127 rollouts per §12's exit table — and explicitly does **not** license a second invocation, A2, config materialization, or any use of the result before both agents audit it. **Before authorizing I audited the entire pre-rollout surface at zero cost** (14/14 pass in 0.39 s against the real retained dataset: all four I1 pins, `resolve_context`, `resolve_replay_source`, the 20/38 reference payload shapes, and the 3,203-file inventory floor), **including the two checks the gate itself only reaches *after* its rollout is spent** — the retained manifest row lookup and `compare_manifest_row` — which retires one whole class of `X_DEFAULT_PATH_UNVERIFIED` exit before the rollout instead of in exchange for it. **The session's finding is operational and belongs to Step 5:** `run_replay_gate` brackets its rollout with an inventory of `[data_root, PACKET_ROOT]` plus the repository root's direct file children and fails on *any* change, at a point where `rollout_spent` is already 1 — and nothing filters `__pycache__`, `.pytest_cache` or `MUJOCO_LOG.TXT` out of that watch list. Measured: a warm plan run changes 0 of 3,203 watched files, and the bytecode mechanism is closed by construction (the only function-level imports on this path re-import a module `estimator.py:69` already loaded). The residual is a **concurrent writer**, which no measurement can close — running the packet test suite alone would write `.pytest_cache` inside the packet and burn the rollout. Hence: while Step 5 runs, nothing else may touch the packet or the repository root. I also drove `require_authorized_plan` directly (14/14) to confirm that naming a digest actually binds, and recorded the second layer that makes its one gap harmless: a content-edited plan named by its own digest passes the naming gate and then dies at X0E for **0 rollouts**, before the replay gate is called. Zero rollouts this session; lifetime total unchanged at **151**; `payload_boundary.json` and `config.json` both absent; focused 170 (and 170 under `-O`), full packet **1,306 green in 121.80 s**, compileall clean.

  **Session 72 (superseded, pointer only).** **STEP 2 IS CLOSED AND STEP 3 HAS RUN.** Codex explicitly approved my Session-71 blobs (`95040d93…` / `0d7b68fc…`) in its Session 71, so both agents approve the same bytes and the eight-round measurement-executable review loop is closed. That approval authorized exactly one thing under §13: **Step 3 — plan mode only.** I ran it this session. It wrote `Reproducibility Packet/results/payload_boundary_extension/plan.json`, `plan_valid=true`, **zero rollouts**, canonical sha256 `15298da4c7a903bf4b62a79eb384abe1f53182972dff41c6e1387dc0ce030be3`, git blob `04f2bccd…`, 5,386 bytes. Plan mode compiles eight MuJoCo bodies and steps none of them; the evidence is the clock — 0.38 / 0.36 / 0.37 s over three runs, against 25.1–27.5 s for one rollout. **I read the artifact as a stranger who does not trust the program would read it, and re-derived its load-bearing numbers from its own published fields with code that does not import the executable:** all 126 physical-key reports rebuilt from `plan.masses` / `plan.ladder` / `plan.identities` / `inputs.probe_*` reproduce the published digest `9889afa6…` exactly; every census figure, exit cost, anchor rung and identity-band rule re-derives; the anchor's stability interval comes out (0.0211, 0.1962) against §9.3's (0.021, 0.196); and an independently written absolute-path matcher finds **0 offenders and zero backslashes** across all 285 strings. Three runs from two different packet roots give **byte-identical** files, so the digest identifies the document rather than the copy — and because canonical JSON emits **no newline of either kind**, limitation 80 cannot bite this artifact (index blob verified byte-identical to the worktree). I flagged two fields present beyond §11.1's literal list — `preflight.per_mass_realized_delta` (named by §11.2) and `plan.shape_audit` (named nowhere, and the field that discharges X9) — and one documentation slip in §12 that I deliberately did **not** escalate into a version bump of a frozen document. **I approved the artifact and handed the second independent read to Codex, explicitly declining to write anything that could be read as half of the Step-4 authorization.** Step 4 is a separate joint authorization naming that digest and authorizing the §3.3 replay rollout; neither agent may issue it alone. Nothing else is authorized: no replay, no measurement, no A2, no config materialization. Zero rollouts this session; lifetime total unchanged at **151**; `config.json` absent; test split untouched at zero; full packet suite **1,306 green in 122.77 s** with the new artifact present.

  **Session 71 (superseded, pointer only).** **The extension document loop is CLOSED — both agents approved canonical `538ae06b…` (blob `d9f6e188…`) in Session 63; it may not be edited and it authorizes Step 2 only.** Two of Step 2's three prerequisites remain **jointly approved and closed** (`b7b2430a…` / `c23e61d3…`, and `2f7c33b2…` / `ad6b32fe…`). **The third — the measurement executable — is still OPEN, now at my Session-71 state, and Codex owns the next turn.** **Codex's Session 70 found a real defect in my Session-70 repair and I accepted every line of it.** My scheme whitelist was compiled as one bare lookbehind per name, which asks whether the text *ends in* `https:` rather than whether `https` is the complete scheme token — so `reasonhttps://host/PRIVATE/row.npz` was protected and the complete rooted path published, one token boundary further out than the shape my own S70 turn closed. Codex made each guard token-exact, named `git+ssh` explicitly rather than letting it be shielded by its own `ssh` suffix, and approved `c850a4b6…` / `150870f4…`. **My Session-71 return review reproduced all of that through a different instrument and changed NO OPERATIONAL EXPRESSION** — executable AST with docstrings stripped: mine == Codex's `c850a4b6`, verified mechanically; 3,256-cell grid and 576-cell two-path grid both give **0 outputs differing** from its state. **What I found instead is a different kind of finding from the previous seven rounds: three COVERAGE gaps around code that behaves correctly.** (1) **`_URI_SCHEMES` is adopted by its own tests, never checked** — both scheme tests are parametrized over the constant, so they follow it; measured, dropping `ftps` or `sftp`, or ADDING an unlisted name, each survives the entire focused suite, and adding one immediately republishes a complete rooted path. That is requirement (r) — equality, never adoption — applied to a constant, and it is my own S70 lesson written down and not implemented. (2) **The whitelist's accept side is only ever tested at a space or at start-of-string**; Codex's 312-cell boundary matrix was a probe, not a committed test, so widening the guard's longer-token character class to any non-space survives the suite while publishing `(https://example.org/spec)` as `(https:spec)`. I enumerated all 100 printable characters and the mangling set is **exactly** `[A-Za-z0-9+.-]` plus `/`, so the test asserts the measurement. (3) **A disclosure of mine misstates its own limitation** — the docstring says an unlisted scheme's URL becomes `x` when measured it becomes `myscheme:x`, and the test that exists to keep the two together already asserted the right value, so nothing could see the disagreement. **All three were demonstrated by MUTATION, not by a red-check: every one of my 18 new cases is GREEN against the state under review, and I said so at the top of the turn rather than letting the count imply otherwise.** Handed back at `95040d9305e08da22d23d6b827c8d14cd0e5603c` and `0d7b68fc02295c9611b80a5e9c9b58ed71123eb6`. Verified: focused suite **170**, full suite **1,306** green in 126.03 s, `python -O` clean (its warning checked against the reviewed state rather than reported as new), compileall clean, 19-case two-pass mutation sweep at **18 caught / 1 survivor / 0 bad anchors, both passes agreeing** — before my additions the same 19 cases gave five survivors, four of which were the gaps above; the remaining one is the deliberate, documented discard branch. Accept side re-measured at **0 offenders** across the plan document, execute skeleton and failed-plan document. **Step 2 is incomplete; nothing authorizes plan mode, replay, execution, A2, or the config freeze.** Zero rollouts this session; lifetime total unchanged at 151; `config.json` absent; test split untouched at zero.

  **Session 70 (superseded, pointer only).** **The extension document loop is CLOSED — both agents approved canonical `538ae06b…` (blob `d9f6e188…`) in Session 63; it may not be edited and it authorizes Step 2 only.** Two of Step 2's three prerequisites remain **jointly approved and closed** (`b7b2430a…` / `c23e61d3…`, and `2f7c33b2…` / `ad6b32fe…`). **The third — the measurement executable — is still OPEN, now at my Session-70 state, and Codex owns the next turn.** My Session 69 blocked Codex's state on four instances of one class (a UNC path glued to prose, a path containing a SPACE, a mixed-separator reduction that kept the parent directory, and — disclosed rather than fixed — the single-slash POSIX form) and returned `9fd723b0…` / `191d9b4d…`. **Codex's Session 69 changed no operational expression at all:** it accepted those four, agreed with the single-slash judgment, and found that my written disclosure was NARROWER than the executable's measured behaviour — a space-containing forward-drive, forward-UNC or mixed-separator path also leaves a relative private suffix. It corrected the disclosure and its test and approved `f2d9f3b1…` / `eb10bb23…`. **My Session-70 return review kept every word of that and verified its AST-equality claim independently** (executable AST, docstrings stripped: `9fd723b0` == `f2d9f3b1` True), **then found a new defect one layer out and blocked the state.** The instrument was the cross-product again, widened where it was thin: twenty-two renderings × eleven preceding contexts × four suffixes = **968 cells**, each classified by what survives. **Four published a COMPLETE ROOTED PATH — `reason://host/PRIVATE/row.npz` came back byte-identical** — because the forward-UNC lookbehind said "any alphanumeric followed by a colon is a URL scheme", which is a much wider claim than "this is a URL", and the writer's guard shares that pattern on purpose, so it declined too: **X6 satisfied, X7 violated.** Eight more kept the DRIVE DESIGNATOR (`opaque-prefixC:My Data\PRIVATE\row.npz`), inside Codex's disclosed whitespace family but wider than its sentence, which says the absolute root is removed. **Repaired by naming the protected schemes** — a URL host and a UNC host are lexically identical, so a name-based decision is unavoidable and the only honest version is an explicit list (`http, https, ftp, ftps, sftp, ssh, git`; **`file` deliberately absent**, because `file://host/share` IS a path) — **and by having the forward-slash drive form refuse a second slash (`[A-Za-z]:/(?!/)`) instead of demanding a token boundary.** The converse cost is disclosed and pinned: an unlisted scheme's `//` form is reduced like a path. Measured prose cost of the pair: **zero over a 30-sentence battery** including this project's vocabulary and http/https/ftp/ftps/sftp/ssh/git URLs in three cases. Handed back at `c745106872da27f8370144cec601de9e50bd092e` and `485dcc3dc8654f1bb1a2fa21bafdc57236ff45ee`. Verified: focused suite **141** (Codex handed off 106), full suite **1,277** green in 127.33 s, `python -O` clean, compileall clean, **14 contracts red against Codex's exact blob and 21 already true — the 21 reported as COVERAGE, not as findings**, and driven directly because the reviewed blob cannot even COLLECT the test file (a harness result, not a red-check). Ten-case two-pass mutation sweep at **10 caught / 0 survivors / 0 bad anchors, both passes agreeing**; one cosmetic fault in my own sweep's detail field is reported beside the numbers. **The sweep also caught a regression in MY OWN repair:** once the drive form dropped its boundary, the only committed case exercising `_final_component`'s both-separator split was consumed by the Windows rule first, so deleting that split SURVIVED the whole focused suite; closed with a case only the POSIX rule can match. **The accept side was measured too** — a stricter predicate is the obvious way to make X7 fire while X6 writes — with **0 offenders in the plan document, the execute skeleton and the failed-plan document under both the old and the new predicate**. **Step 2 is incomplete; nothing authorizes plan mode, replay, execution, A2, or the config freeze.** Zero rollouts this session; lifetime total unchanged at 151; `config.json` absent; test split untouched at zero.

  **Session 68 (superseded, pointer only).** **The extension document loop is CLOSED — both agents approved canonical `538ae06b…` (blob `d9f6e188…`) in Session 63; it may not be edited and it authorizes Step 2 only.** Two of Step 2's three prerequisites remain **jointly approved and closed** (`b7b2430a…` / `c23e61d3…`, and `2f7c33b2…` / `ad6b32fe…`). **The third — the measurement executable — is still OPEN, now at my Session-68 state, and Codex owns the next turn.** Codex's Session 67 accepted my three Session-67 corrections, ruled that the authorization-gate refusal does **not** reopen the accepted "embed approved content verbatim" scope (I take that ruling; the question is closed), then found three further execute-exit shapes and approved its own state `25386e27…` / `ab4ddfc0…`. **My Session-68 re-review accepted all three corrections and kept every line of them, attached a scope correction to two of the three, and blocked that state on one defect the repair itself created.** The scope corrections: the unserializable-value defect is real but only when the value sits under `inputs` / `protocol` / `plan`, which are the only three members the artifact copies — in the position Codex demonstrated it, my reviewed blob wrote a perfectly good artifact — and the "990 nested arrays" figure is a property of how deep the *caller's* stack already is, not of this file (swept: at zero extra frames my blob survived depth 960; at 300 extra frames it lost the artifact at 800). **The new defect: the corrected cleaner can discard a persisted failure reason in its entirety.** Dropping the token boundary from the backslash drive form was right, and it made a state reachable that was not before — the POSIX rule reduces `/plant/\row.npz` to `\row.npz` and re-emits it after the boundary character, which *builds* `C:\row.npz` inside prose the Windows rule had already declined, and the only exit left replaces the whole message with `<path>`. Measured on three realistic sentences and on six of the 37,448 strings the committed enumeration test already generates. It leaks nothing and loses no artifact; it loses the **reason**, which on a failure exit is the entire record. Fixed by running the substitutions to a fixpoint (termination is arithmetic: each productive pass strictly reduces the separator count) — prose preserved, discard branch now reached **0** times. I also moved Codex's three new test payloads into the member that matters and re-aimed its depth case at the gate's own boundary. Handed back at `04ec936e1fd0a8b3cbe4d17c0a942f9b5632187f` and `4979af073747dc1fd9906877709caa8bca4fa3e1`. Verified: focused suite **81** (Codex handed off 76), full suite **1,217** green in 119.02 s, `python -O` clean, compileall clean, Codex's five additions **9 red** against the state it reviewed, three of my four additions red against Codex's exact state for the right reason (the fourth needs an extracted function, so it is coverage and I do not count it), and an 8-case two-pass mutation sweep at **7 caught / 1 survivor / 0 bad anchors** including the Lesson-63 double removal — the survivor is the discard branch, unreachable by construction, which is exactly what one of the new tests asserts. **Step 2 is incomplete; nothing authorizes plan mode, replay, execution, A2, or the config freeze.** Zero rollouts this session; lifetime total unchanged at 151; `config.json` absent; test split untouched at zero.

  **Session 67 (superseded, pointer only).** **The extension document loop is CLOSED — both agents approved canonical `538ae06b…` (blob `d9f6e188…`) in Session 63; it may not be edited and it authorizes Step 2 only.** Two of Step 2's three prerequisites remain **jointly approved and closed**: the assignment-generator seam (`b7b2430a…` / `c23e61d3…`) and the results layer (`2f7c33b2…` / `ad6b32fe…`). **The third — the measurement executable — is still OPEN, now at my Session-67 state, and Codex owns the next turn.** Codex's Session 66 accepted all four of my Session-66 diagnoses and implementations, then found two real further defects of its own — a foreign plan whose `inputs` field is text, a list, or empty crashed the program while it assembled the failure record, and an absolute path used as a JSON *field name* was published intact because both the cleaner and the final safety check inspected values only — fixed both, and approved its own state `86fc3fdb…` / `e081a26d…`. **My Session-67 re-review reproduced both findings against my own handed-off blob, accepted both implementations unchanged, and then blocked that state on a third family of the same defect class.** The program's path cleaner recognises a *list of spellings*; the safety check that refuses to publish a path asks Python a *question*. Enumerating every string over `/ \ C : x space . 1` up to five characters — 37,448 of them — measured **1,358 that are still absolute after cleaning**, and each one destroys the record the cleaning exists to protect. Two families: a bare root (`/`, `//`, `/ x`), which no pattern can match because no component follows the separator; and a drive letter Python accepts and `[A-Za-z]` does not (`1:\dir\row.npz`), which is a real path with a real directory portion. Nine such shapes, driven through the actual program, returned no exit code, crashed, and left the output directory empty. The fix stops enumerating and states the contract: the cleaner now ends by asking the safety check's own question and reducing to a fixpoint — **0 of 37,448** afterwards, with URLs, ratios and arrows untouched. I also closed the one path both agents had agreed to leave alone, **without reversing that agreement**: approved plan content still reaches the artifact verbatim, but a plan that records an absolute path can no longer *be* approved, because the gate now checks the premise the agreement rested on instead of assuming it — a refusal, not a rewrite, routed through the exit that cleans and writes. After it, **no reachable execute-mode failure leaves the run unrecorded.** Handed back at `5a5b056200bfb219ef7966ecc17987e477b782ab` and `f2f5031dfb856c938634963c4dd6ea119689939a`. Verified: focused suite **71** (Codex handed off 58), full suite **1,207** green in 117.49 s, `python -O` clean, **12 of 13 new tests red against Codex's reviewed blob** with all 58 of its own green (the thirteenth covers *Codex's* collision guard, which works, so it is coverage rather than a red-check and is not counted as one), and a 16-case two-pass mutation sweep at **15 caught / 1 survivor / 0 bad anchors** including both Lesson-63 double removals — the survivor is a length comparison that cannot fail arithmetically and the code says so. That sweep is also what found the two guards in Codex's repair that no test could exercise. **Step 2 is incomplete; nothing authorizes plan mode, replay, execution, A2, or the config freeze.** Zero rollouts this session; lifetime total unchanged at 151; `config.json` absent; test split untouched at zero.

  **Session 66 (superseded, pointer only).** **The extension document loop is CLOSED — both agents approved canonical `538ae06b…` (blob `d9f6e188…`) in Session 63; it may not be edited and it authorizes Step 2 only.** Two of Step 2's three prerequisites remain **jointly approved and closed**: the assignment-generator seam (`b7b2430a…` / `c23e61d3…`) and the results layer (`2f7c33b2…` / `ad6b32fe…`). **The third — the measurement executable — is still OPEN, now at my Session-66 state, and Codex owns the next turn.** Codex's Session 65 accepted all five of my Session-65 corrections including the `resolve_replay_source` extraction, found two real further defects of its own (both missing-argument execute exits still returned without an artifact, and the path scrubber deliberately ignored POSIX paths although the packet must run on other machines), fixed both, and approved its own state `eb94afb2…` / `5d8dd369…`. **My Session-66 re-review accepted both of those findings and both implementations, and then blocked that state on four more defects — every one reproduced by RUNNING the program, and three of them the same class Codex was closing.** A named plan file carrying an absolute path in its own fields makes the artifact writer's path refusal fire *while writing the failure record*, so the program dies with a traceback and persists nothing — on the one exit whose entire purpose is "you named the wrong plan"; the exit Codex added this session can itself fail that way, because `--approved-plan-sha256` reaches the artifact before anything checks it looks like a digest; a `//host/share` path survives both scrubbers and is absolute to both `PurePath` flavours, so it either crashes the writer or is published; and the Windows drive-letter form eats every URL (`https://` reads as drive `s`), which is **my** defect from Session 65, measured with each regex in isolation so the attribution is not a guess. One further finding: two execute exits returned `1` with nothing printed at all — including the one that has already spent the replay rollout — and the plan-mismatch branch had no test of any kind. I corrected all five and handed back an explicitly approved state: `431d9c08af0df645f8ddb6849d6ce3265e9fd699` and `4d194a672801e56e5e03a25c625728e5914a9300`. Verified: focused suite **53** (Codex handed off 47), full suite **1,189** green in 117.25 s, `python -O` clean, **6 of 6 new tests red against Codex's reviewed blob** in an isolated packet copy with all 47 of its own still green, and a 10-case two-pass mutation sweep at **0 survivors / 0 bad anchors** including both Lesson-63 double removals. **I deliberately did not scrub the plan content on the *authorized* path** — a plan whose digest matches the authorized digest is one both agents read, and plan mode's own writer already refuses absolute paths — and I named that as a scope for Codex to rule on rather than leaving it to be discovered. **Step 2 is incomplete; nothing authorizes plan mode, replay, execution, A2, or the config freeze.** Zero rollouts this session; lifetime total unchanged at 151; `config.json` absent; test split untouched at zero. **My Session-64 progress report loop is CLOSED at `b0ff7496…`.**

  **Session 65 (superseded, pointer only).** **The extension document loop is CLOSED — both agents approved canonical `538ae06b…` (blob `d9f6e188…`) in Session 63, and it may not be edited; a change needs a version bump and a `git mv`. It authorizes Step 2 only.** Two of Step 2's three prerequisites are **jointly approved**: `scripts/utils/assignment_generator.py` (`b7b2430a…`) with `tests/test_assignment_generator_screen_overrides.py` (`c23e61d3…`), and `scripts/utils/protocol_p_results.py` (`2f7c33b2…`) with `tests/test_protocol_p_results.py` (`ad6b32fe…`). **The third — the measurement executable — is OPEN and Codex owns it.** Codex built it in its Session 64 (`scripts/run_payload_boundary_extension.py` blob `62e4c9e1…`, `tests/test_payload_boundary_extension.py` blob `96906aab…`) and approved its own state. **My Session-65 first review blocked it: that state could not have completed an execute run at all, and I reproduced every defect by running the program rather than by reading it.** Its self-verification gate selected the *wrong* delivered simulation — the approved gate exports `SCENARIO_SPEC_ID = "scenario_dev_t01_f000_r00"` and derives the file name the extension already imports, and the scenario id had been re-typed by hand as the neighbouring `t00` run, so every execute attempt fails that gate before spending its rollout. When it fails, the handler read two variable names bound only inside a *sibling* exception handler, so it raised `UnboundLocalError` and wrote no artifact, discarding exactly the evidence the custom `ReplayGateFailure` class had been built to carry. And the measurement loop caught one exception type while the payload-override construction raises `AssignmentGenerationError`, a `ValueError` outside that hierarchy — an escape mid-run would have discarded up to 126 already-spent rollouts. Two smaller findings: the artifact's absolute-path guard asks whether a string *is* a path, which no real refusal sentence is, so the first realistic failure publishes this machine's paths; and `TAU_ANCHOR` appeared exactly twice, its definition and the field the plan publishes, while the partition it supposedly governs was typed separately beside it. I corrected all five in both files, flagged the one structural change (lifting the gate's pre-rollout half into `resolve_replay_source` so it is testable at zero cost) as a deviation for Codex to rule on, and handed back an explicitly approved state: `ff0cdbe63bf527cb21fe499b84e45a86e1dee0af` and `ebdfdf837035e3a0049a2a68e78c102717d8ac92`. Verified: focused suite 45 (from 36), full suite **1,181** green in 117.19 s, `python -O` clean, seven of nine new tests red against Codex's blob in an isolated packet copy, and a 17-case two-pass mutation sweep at 13 caught / 4 survivors / 0 bad anchors — the sweep found a gap in my own repair, and a fault in my own harness that had made the two-pass agreement check vacuous. **Step 2 is incomplete; nothing authorizes plan mode, replay, execution, A2, or the config freeze.** Zero rollouts this session; lifetime total unchanged at 151; `config.json` absent; test split untouched at zero.

  **Session 63 (superseded, pointer only).** **One state was open and Codex owned it: the payload-boundary extension v0.2, at my Session-63 state.** Codex's Session 62 reviewed the v0.2 I handed off, made five direct edits, and explicitly approved its own edited state at canonical `e5192eaa…` / blob `3d72e1f4…`, leaving the loop open for my owner re-review. **I accepted all five corrections — diagnosis and implementation — and verified the three that rest on facts about the codebase rather than on reading.** All three hold: `ScreenOverrides` really has five fields with `provenance_hash` among them, so my §11.3 had asked the provenance record to contain its own digest; `PhysicalKey` really carries no payload field; and the anchor's source reservation `scenario_dev_t01_f000_r02` really already carries 0.050 kg, **so the anchor cannot establish that the new payload override is live** — a dead override would still hand it the requested body. That last one is Lesson 88 recurring inside the fix for Lesson 88, and it is now recorded as such. Codex's plan/execute split (X0P/X0E bound to the approved plan digest) also closed a hole of mine: under my ordering a failed preflight had no artifact to land in at all. **I found one new defect in Codex's own new text and fixed it.** Codex had correctly tightened Option B under `X_CASE_ROLE_LOST` — the cap must come from an ascending prefix in which every mass retains its *own* reserved role — but left the neighbouring `X_CASE_EMPTY` branch stating the older, weaker rule, and the two branches are mutually exclusive, so a run gets one or the other. Enumerating all **19,448** reachable classifier states (the same space Codex's exhaustiveness check ran over, which is the point — the classifier was checked for completeness, and what each branch *licenses* was not checked with it) measured the disagreement: **4,106 of 8,008** `X_CASE_EMPTY` states licensed a band containing a mass that had lost its role, and in **3,185** states **deleting the heaviest mass's result raised the licensed cap** — worst case from 0.025 kg to 0.150 kg, into a band where four of the seven masses fail the requirement Option B exists to guarantee. Strictly worse evidence must not license a bolder choice. Fixed by stating the rule once and having both branches use it (an empty set necessarily breaks the prefix), which leaves **0 of 3,185**; the counterexample and counts are in §9.5 so a future reader does not "simplify" the duplication away. Document: `Reproducibility Packet/protocol/payload-boundary-extension-v0.2.md`, canonical sha256 `538ae06b…`, blob `d9f6e188…`, `+30/−2` confined to the licensing block. Explicitly approved by me and handed back; **Codex owns the next turn.** Zero rollouts; suite **1,126** green in 121.30 s with no code changed; `config.json` absent; test split untouched at zero; A2 still undrafted and blocked.

  **Prior state (end of my Session 62).** **One state was open and Codex owned it: the payload-boundary extension, then at v0.2.** Codex's Session 61 approved my payload-conditioning analyzer and tests at blobs `39048d26` / `b9e81f63`, closing that loop, and **blocked the extension pre-registration on four findings. I checked all four against primary sources and all four are real; none is contested.** The serious one is a design error of mine: v0.1 gave every payload mass its own sensor identity, so a difference between two masses would have been part payload and part identity with no way to separate them afterwards — and the invariant meant to catch a payload override that never reached the plant would have passed regardless, because different identities produce different numbers whether or not the body changed. **v0.2 uses common random numbers across masses** — eight identities keyed on the replicate index only, reused at every mass — which makes the tripwire a real discriminator (a dead override would now produce *identical* healthy vectors) and grows it from 21 comparisons to 168. That fix forced a **second prerequisite nobody had named**: `PhysicalKey` in Codex's approved results layer carries no payload field, so under common random numbers two masses collapse to one key and one simulation could be silently reused for another — carried requirement (x) exactly. v0.2 also replaces the prose outcome cases with **one ordered, exhaustive classifier** (the shape Codex constructed was reachable by no rule in v0.1), pins the role-severity map as literals verified against the assignment, removes the non-monotonicity tolerance entirely in favour of set inclusion, names both artifact paths and the minimum fields persisted on every exit, pins the per-rollout identity payload field by field, adopts the parent protocol's one-rollout replay gate as a precondition, and **runs the control mass first and alone** so a failed instrument costs 19 rollouts rather than 127. **Three changes came from my own measurements, not the review:** the plant is compiled with **zero gravity**, so payload is tip inertia and not a hanging weight — the arm does not deform at all at any of the eight masses, and my Session-61 "hung at the tip" phrasing is withdrawn; a linearized modal estimate puts the lowest elastic mode at **77.34 Hz against a 0.8 Hz probe**, which **rules out resonance** as the mechanism for the payload attenuation and leaves it honestly unidentified; and v0.1's control test demanded reproduction of a margin sitting **2.1% from the line**, which is asking a measurement to reproduce noise, so v0.2 constrains the nine rungs whose margins exceed 10% of threshold and shows that any cut in `(0.021, 0.196)` gives the identical rule. Document: `Reproducibility Packet/protocol/payload-boundary-extension-v0.2.md`, canonical sha256 `e734c498…`, blob `c7facc13`, superseding v0.1 by `git mv`. **Zero rollouts** (two mechanics probes, reported as such); suite **1,126** green with no code changed; `config.json` absent; test split untouched at zero; A2 still undrafted and blocked.

  **Sessions 45–61 (superseded, pointer only).** Each session's open/closed loop state used to be restated here in full, which is the turn-by-turn history this section says it does not keep. It is carried completely by the Phase-2 transcript and by `Session Summaries/HumanReport45.md` – `HumanReport61.md`, and the arcs are summarised for the director in `Progress Reports/Progress Report Session 48.md` and `Progress Report Session 56.md`. Removed in Session 61; the Session-60 paragraph folded in here in Session 62, and the Session-61 paragraph in Session 63.

  **Files I own or co-own outside this workspace:** `Reproducibility Packet/protocol/protocol-p-v2.3.3.md` (created Session 41 as v2.3.1, corrected and renamed in Sessions 42 and 43; mine, Codex reviews — **jointly approved and closed in Codex's Session 43**); `Reproducibility Packet/tests/test_cable_plant_softening_boundary.py` (written by me in Session 43, but it tests **Codex's** plant contract and Codex approved both its location and its exact state — treat it as co-owned, and Codex's call if the two ever conflict); **`Reproducibility Packet/tests/test_assignment_generator_screen_overrides.py` (written by me in Session 44; it tests the seam I added to Codex's generator, so it is co-owned on the same terms — **approved at exact state by Codex in its Session 44**)**; **the §3 seam inside `Reproducibility Packet/scripts/utils/assignment_generator.py` (Codex's file; I own the seam implementation by Codex's explicit assignment, Codex reviews and owns the file — **approved at exact state, Codex Session 44**)**; **`Reproducibility Packet/scripts/protocol_p_replay_gate.py` and `Reproducibility Packet/tests/test_protocol_p_replay_gate.py` (both mine, written in Session 45 — the executable §7 gate and its 30 permanent comparison-layer tests; awaiting Codex's review. The script imports two private names from Codex's generator: `_generate_reservation`, which §4 pins explicitly, and `_plant_payload`, which I import so the comparison *shares* production's serialization rather than agreeing with a second copy — that coupling is Codex's call and is an open question in my S45 handoff)**; my estimator/evaluation lane and screens in `Reproducibility Packet/scripts/` as described above; and the root `.gitattributes` byte pins (shared hygiene, extended Session 41; its `protocol/*.md` wildcard already covers each renamed file, verified via `git check-attr` in Sessions 42 and 43). **Session 44 raised whether the seam files should also be LF-pinned there; Codex answered no in its Session 44 — Protocol P hashes no source file, so the git blob hashes are the checkout-EOL-stable identifiers and adding a byte contract would expand policy without protecting an identity. The two Session-45 files are in the same state for the same reason.** **`Reproducibility Packet/scripts/analyze_synchronous_difference_null.py`, `Reproducibility Packet/scripts/utils/gauge_windows.py`, `Reproducibility Packet/tests/test_synchronous_difference_null.py` and `Reproducibility Packet/tests/test_gauge_windows.py` (all mine, written in Session 46 — Protocol P Stage 0, the pre-registered shared gauge-window helper, and 117 permanent tests across the two files; **RUN ONCE in my Session 48**. **All five files are now jointly approved at exact blobs and the loop is CLOSED**: three at Codex's S46 blobs, approved by me in S47 (`gauge_windows.py` `7f7c09da…`, the detection-floor screen `b99fe333…`, `test_gauge_windows.py` `925b0bd8…`); the Stage-0 script at my blob `8435c764…`, approved unchanged by Codex in its S47; and its test file at Codex's reviewer-edited blob `9591c91b…`, approved by me in S48. The stage's output artifact `Reproducibility Packet/results/protocol_p/sensor_only_difference_null.json` is mine and is tracked by design (the packet `.gitignore` excludes `*.npz`, not `results/*.json`). The Stage-0 script imports `ProtocolPError`, `canonical_text_sha256` and the two text-domain digest pins from the replay-gate module rather than re-declaring them, so the two-domain hashing rule has one implementation and each pinned digest one copy; that coupling is flagged for Codex, with a recommendation to extract `utils/protocol_p.py` when the Stage-A/B/C driver becomes the third consumer.)** I also maintain **Steps 22, 23 and 24 of the packet README** (my structural separability screen, the replay gate, and the Stage-0 runbook step); Codex documented `scripts/embed_approved_assignment.py` at Step 2B in its Session 45, so no packet script is undocumented. **Step 24 was written in my Session 49**, after three deliberate deferrals — a runbook step should describe a step that has been executed *and reviewed*, and Codex's review of the result landed in its Session 48. It states the pre-registered invocation, the recorded result, the four boundaries the artifact itself carries (no threshold, upper-tail containment rather than agreement, permanent `dev-` ineligibility, and an identity that binds inputs and output shape rather than measured values), `first-run elapsed time: not captured`, and the two reader traps (`len(samples)` is 6 not 100; `std` is the population one). It leads with the contrast against Step 23 because the two Protocol-P steps have **opposite reader-reproducibility status**: Step 23 cannot be run from the distributed packet, while Step 24 needs no dataset, performs no MuJoCo simulation, and is the first Protocol-P step an outside reader can execute end to end. **Do not write that Step 24 needs no MuJoCo** — my first state said exactly that and Codex correctly blocked it: importing the Stage-0 script pulls in the `mujoco` package transitively through `protocol_p_replay_gate -> assignment_generator -> cable_plant`. Measured in my Session 50, that is **one of the script's eight project imports** and the surface crossed is four constants, one exception class and one text-hashing helper, so the dependency is incidental rather than intrinsic and should disappear when `utils/protocol_p.py` is extracted; Step 1 pins and installs `mujoco==3.10.0` either way, so the step remains clean-checkout runnable. **Step 24 reached joint approval at Codex's reviewer-edited blob `9363e144…` (Codex S49 reviewer, me S50 owner), and I reopened it in my Session 51**: its last sentence said the MuJoCo package “is still imported transitively by shared input-binding code,” which the Session-51 extraction made false. It now states that the stage's script imports no MuJoCo at all and names the test that pins the property; that `+1/−1` edit is handed to Codex for review. **Two new modules of mine, both written in Session 51 and awaiting Codex's exact-state review:** `Reproducibility Packet/scripts/utils/protocol_p.py` (blob `8d900525…`) — the shared Protocol-P primitives extracted out of the replay gate, standard library only, with the two `.npz` pins deliberately left in the gate because a pin belongs with the check that reads it; and `Reproducibility Packet/scripts/utils/protocol_p_conditions.py` (blob `9e7cc76f…`) — the Stage-A/B/C construction layer enforcing I3/I4/I5/I6/I7/I8/I13a as preconditions, with `tests/test_protocol_p_shared.py` (blob `231eda23…`, 19 tests) and `tests/test_protocol_p_conditions.py` (blob `24a906a9…`, 111 tests). The gate and the Stage-0 script were edited to import from the shared module (blobs `c6b16749…` and `f104971d…`); both edits are inside closed loops and therefore go back through exact-state review. **All of that reached joint approval across the S51/S52/S53 rounds and those loops are now CLOSED — do not re-review and do not edit:** `scripts/utils/protocol_p.py` (`8d900525…`), `scripts/protocol_p_replay_gate.py` (`c6b16749…`), `scripts/analyze_synchronous_difference_null.py` (`f104971d…`) and packet README Step 24 (`ba9c067a…`), all four approved unchanged by Codex in its S51; `scripts/utils/protocol_p_conditions.py` (`7fdddf0e…`) and `tests/test_protocol_p_shared.py` (`f505877f…`, 20 tests), both reviewer-edited by Codex in S51 and approved unchanged by me in S52; and `tests/test_protocol_p_conditions.py` (`1874773e…`, 135 collected), edited and returned by me in S52 and approved unchanged by Codex in its S52. **Four further files of mine, built in Session 54, blocked by Codex in its Session 54, corrected in my Session 55, and JOINTLY APPROVED at those exact blobs in Codex's Session 55 — that loop is CLOSED:** `Reproducibility Packet/scripts/utils/protocol_p_results.py` (`e84e5f9f…`), `Reproducibility Packet/scripts/run_protocol_p_screen.py` (`99e2d447…`), `Reproducibility Packet/tests/test_protocol_p_results.py` (`cbac30ed…`) and `Reproducibility Packet/tests/test_protocol_p_driver.py` (`3f1a8106…`). **My Session 56 then changed two of those four** — the driver and its test file — to add Correction 1's pre-registered `screen_physical_faults` (plus `screen_onset_index`, `require_preregistered_faults` and `packet_relative_input_path`), so both are back under exact-state review; the results layer and its test file are untouched and remain at Codex's approved blobs. **I also own packet README Step 25**, written in Session 56 once Codex's approval removed the standing objection that a runbook step must describe something a reader can rely on. Steps 22, 23, 24 and 25 are mine to maintain. **NEW in Session 87: `Reproducibility Packet/protocol/capacity-escalation-v0.1.md`** (mine, Codex reviews) — a DRAFT pre-registration for the Slot-9 within-suite capacity sweep that carried limitation 127 says has to be run before any C1-vs-S reading is drawn from rung 1. It authorizes nothing; it is a document handed over for review, on the same three-step sequencing as the payload extension (document approved, then executable reviewed, then execution separately authorized). It needs no Claim Sheet amendment — Slot 9's escalation condition (b) and Slot 14's required “within-suite capacity sweep” already contract it — and it resolves the dev-fit contract's bound 5 explicitly rather than around it: the sweep may expose whether the rung-1 deficit is capacity-bound and may **not** select the headline capacity, which stays validation-owned at Gate 5/6. Its proposed capacity points all sit inside Slot 9's rung-1 parameter band, so Stage 1 does not climb the ladder at all. It is LF-pinned by the existing `protocol/*.md` rule and inherits Protocol P's version-bump discipline. **REVISED in Session 88 at blob `ccd12ef4…` (canonical `283de293…`) and explicitly approved by me; Codex owes the second read.** Codex blocked the Session-87 state on five findings and issued three rulings, all accepted without contest: the `CAPACITY_BOUND` verdict is gone (a fixed-epoch width sweep cannot separate capacity from trainability, and a rising paired difference can also mean the *baseline* got worse, so the absolute per-suite curves are now first-class outputs); the executable now emits exactly-defined descriptive fields and **no verdict at all**, with the interpretation pre-registered as prose and applied jointly, and **no observation licensing any action**; the same seed number is no longer called cross-width common random numbers (verified by measurement — same shape and seed gives bit-identical weights, different widths cannot); the grid gained `channels = 40` at an independently reproduced 61,010 parameters; and a whole new section adds a zero-fit plan mode and a run-level partial-failure contract. Two defects of my own went in on top, both below the layer either review had reached: **the approved trainer cannot fit any width but 32** — the size is hard-coded, absent from the CLI and absent from the file — and my own comparability invariant would have refused the only edit that fixes it, which is now answered by a mandatory equivalence gate that re-fits one 32-channel arm and requires bit-identical weights rather than asserting the code paths agree; and **the saturation criterion was on the wrong quantity**, since the read is over macro-F1 while the criterion was over accuracy, and under this split's 8/16/32/96 census a point can be "saturated" at accuracy 0.98 with a paired difference of 0.06 still arithmetically available — replaced by an exact algebraic bound tied to the success bar the approved artifact already publishes, which removes the last invented constant from the read. The route decision for the first of those is deliberately handed to Codex. **REVIEWER-EDITED in Codex's Session 88 at blob `e1c8f77c…` and OWNER RE-REVIEWED in my Session 89, returned at blob `51c86f68…` (canonical `19c6edb3…`, 55,534 bytes / 873 lines / LF).** All five of Codex's edits and all five of its rulings were kept uncontested — including its correction that the approved trainer stays inside the sweep's code identity under the chosen route, because the new module imports the loss from it. Each ruling was checked against something outside the document rather than against the document's own logic: the five-width constructor map was rebuilt independently, `code_identity()`/`require_code_identity()` were read to confirm a ninth identity entry is expressible without touching the closed contract, and the approved ledger was read to confirm the equivalence gate's two named arms exist with the loss histories and checkpoints its bit-identity comparison requires. Three further defects were then repaired: the exact call site of the copied fit loop was written down (it calls a *private* batching function, which the module now imports rather than retyping, since a hand-copied batcher is precisely the divergence the equivalence gate exists to catch); a run-time-sourced constant was named to its field rather than left beside a hard-codeable literal; and — the load-bearing one — **removing the machine path from the plan document, a correct repair, also removed the only thing making two executions two different documents, while the same review's new retry rule licensed re-running all forty fits, so a second full spend would have passed every gate without a second joint authorization.** Repaired with a `run_label` field that restores single-use authorization without restoring the contradiction. **REVIEWER-EDITED in Codex's Session 89 at blob `618d9ada…` and OWNER RE-REVIEWED in my Session 90, returned at blob `b2f650e1…`; REVIEWER-EDITED again in Codex's Session 90 at blob `b359ba0b…`; OWNER RE-REVIEWED in my Session 91 and returned at blob `b45efa47…` (canonical/raw `05109d97…`, 72,630 bytes / 1,084 lines / LF, owner delta `+86/−15`).** The last three rounds are one continuous argument about **where the program is allowed to write**, and each round's finding sat one write location below the previous repair. Codex's Session-89 corrections (the §4.4 table is the complete *project-defined* call surface; a `run_label` cannot make an approved plan digest non-replayable) were kept uncontested in my Session 90, which then found that the document named a `run_label` and a packet-relative namespace and never bound either to the directory the executable writes into — leaving its own "repeated use is recorded" claim with no mechanism. Codex's Session 90 accepted that binding and found the guard I wrote for it too weak in three ways (an empty leftover directory passes; a check followed by a separate create admits two concurrent invocations; a pre-existing *file* at the path was not covered at all), and added the atomic absent-root claim plus a sibling refusal sink outside the occupied resource. My Session 91 kept both halves unedited — checking the cited precedent at source (`require_clean_fit_output` is a name-based staleness check followed by later writes, and permits plan artifacts by design, so it cannot carry an atomicity claim) and checking the two reserved sink names against the label space (`^[a-z0-9][a-z0-9-]{2,31}$` admits no underscore, so no conforming label can ever collide with them) — and then found the one write location the repair did not reach: **C9's scratch output root, named three times in the document and placed none of them.** Two claims written during this loop depend on that unstated answer — §7.3's "the failed root is preserved as evidence" fails if a retry's equivalence fits can overwrite or be blocked by it, and "claimed before any other run write" is not exhaustive while two of the forty-two budgeted checkpoints sit outside the claim. Repaired by making it a reserved `_equivalence/` subtree of the claimed run root, packet-relative and label-leading. Codex owes the same-state reviewer re-review; the document is still `v0.1` because it has never been jointly approved. **The closing condition is now written into the document and the chat: if the next round finds only wording, the loop closes.** **THE LOOP CLOSED IN CODEX'S SESSION 91: it explicitly approved `b45efa47…` — the same bytes I had approved — so **v0.1 IS FROZEN**, five rounds, and the freeze authorizes exactly one thing, the writing of the executable and its tests. That is what my Session 92 did (see the sweep-executable entry above). **The document is now pinned by a digest the executable checks**, so editing v0.1 in place turns plan mode red — which retires my own Session-91 measurement that no test pinned it, and makes the bump-and-`git mv` rule executable rather than remembered.**

  Historical detail for sessions 7–44 (each loop, what was found, what was approved) is in the transcript and in `Session Summaries/HumanReport7.md` … `HumanReport44.md`. That range includes the Protocol-P specification loop closing at v2.3.3 (S43), the permanent I13b guard, and the §3 seam implementation (S44).
- **Chats with the director** (co-owned, append-only): [`Chat Appends/`](../../chats/Claude-Codex-Human/Chat%20Appends) — **Concluded** (Session 6): the director flagged a chat-misplacement occurrence; both agents acknowledged, and I carry a **standing monitoring duty** — if a reply ever lands anywhere but the physical end of a transcript, I flag it. [`Transcript Order Monitoring/`](../../chats/Claude-Codex-Human/Transcript%20Order%20Monitoring) — **Active**, opened my Session 21 to discharge that duty. It records the occurrences, their append-only repairs, and one clean check (Session 23) so the thread shows the rule working rather than only its failures. **The duty is to flag recurrences, so a clean check adds no note** — I verify every session at the git level regardless. **The streak figure below is STALE and I am deliberately not extending it from memory** — it was last swept at Session 65, ten sessions ago, and this project has already had one count be wrong five times running because each correction trusted the previous one. Anyone who wants the current number should sweep the transcript's commit history for it rather than increment this line. My **Session 76 append was clean** and its assertions are in `HumanReport76.md`: pre-write prefix of 1,293,688 bytes retained byte-for-byte under an identical SHA-256, header occurring exactly once at line 20,130 and after the boundary, Claude physically last, pre-existing CR count unchanged at 19,329, `+142/−0`. **I also checked Codex's two Session-75 appends at the git level: both landed after the recorded 20,000-line tail as a single `+126/−0` hunk at line 20,001, with nothing inserted before the boundary and nothing moved or rewritten — no order violation, so no note was added to the thread.** My **Session 75 append was clean** and its assertions are in `HumanReport75.md`: pre-write prefix of 1,273,919 bytes retained byte-for-byte under an identical SHA-256, header occurring exactly once and after the boundary, Claude physically last, pre-existing CR count unchanged at 19,329, `+201/−0`. Last swept streak: **thirty-two clean appends** (through my Session 65, whose append passed all four gates at the git level: pre-write prefix retained byte-for-byte with an identical SHA-256, header occurring exactly once, physically last, `+214/−0`; my Session 64's was `+184/−0`). Prior wording: **twenty clean appends** (Codex S35 through Codex S53, plus Claude S36 — the last verified at the git level in my S54: Codex's S53 commit changed the transcript by `+108/−0`, its header occurs exactly once at line 12,734, after its recorded 12,730-line boundary, and Codex was physically last); the last recurrence was Codex's S34 turn, self-reported and repaired before commit. [`Better Suited Task/`](../../chats/Claude-Codex-Human/Better%20Suited%20Task) — **Concluded** (Session 26): the director's task-redesign directive, which he then withdrew in favour of a separate follow-on project. The joint-space screens remain on the record as honest development evidence; see its `Summary.md`.
- Future co-owned deliverables (per the default writer convention): I remain default writer for the Technical Report, Accessible Piece, and Study Guide Pass 2; Codex is the required reviewer. Both agents contribute to the Reproducibility Packet. These do not exist yet (produced in Phase 3).

## How to navigate this folder without prior context

1. Read **`Summary of Only Necessary Context.md`** first — it tells you where I am right now and what's next.
2. Read the latest report in **`Session Summaries/`** for the most recent session's full detail.
3. Read **`Literature Foundation.md`** for my grounding on the problem and **`references.md`** for the evidence behind any claim.
4. For project-wide context (premise, standards, phases), see the root [`Project Details/Project Details.md`](../../Project%20Details/Project%20Details.md) and [`AgentPrompt.md`](../../AgentPrompt.md) — I re-read those every session; they are not duplicated here.
