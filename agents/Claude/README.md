# Claude — Workspace

This is Claude's personal workspace for the **Robot Structural Proprioception** project. It holds my continuity documents, my Phase-0 field survey, my running source ledger, and my per-session and progress reports. Everything here is visible to the human director and to Codex.

## Folder tree

```
agents/Claude/
├── README.md                          ← you are here: guide to this workspace
├── Summary of Only Necessary Context.md  ← rewritten every session; restores my working context
├── Literature Foundation.md           ← Phase-0 six-section field survey (authoritative)
├── references.md                      ← running, verified source ledger (authoritative, grows all project)
├── Session Summaries/                 ← per-session human-readable reports (HumanReport1.md, 2, …)
└── Progress Reports/                  ← director-facing progress reports (every 8th session, phase/amendment events)
```

## What each file is for (purpose, not contents)

- **`Summary of Only Necessary Context.md`** — the first thing I read at the start of a session after the framework docs. It is **completely rewritten each session** and contains only what I need to resume smoothly: current phase, what I was doing, decisions and why, open questions, next steps. Authoritative for continuity, but always current-only — it is not a history.
- **`Literature Foundation.md`** — my independent Phase-0 reading of the field (six sections: domain/methods, benchmarks, datasets/resources, failure modes, open questions, references). **Authoritative** and stable; it grounds the Claim Sheet and feeds Study Guide Pass 1. Codex maintains a separate, independent one — the divergence is intentional.
- **`references.md`** — my running source ledger (verified links/DOIs, summary + how-it-informed per entry), clustered by subfield. **Authoritative** and append-mostly; grows across the whole project and reconciles into the Technical Report bibliography at Phase 2.
- **`Session Summaries/`** — thorough, timestamped, human-readable reports, one per session (`HumanReport1.md`, `HumanReport2.md`, …). The director's audit trail of how the work evolved.
- **`Progress Reports/`** — director-facing progress reports at the Accessible-Piece bar, written on my sessions 8/16/24…, and on any session that closes a phase transition or an approved Claim-Sheet amendment. Contains `Progress Report Phase 0 Close.md` (Session 2, Phase 0 → Phase 1), `Progress Report Phase 1 Close.md` (Session 5, Phase 1 → Phase 2), `Progress Report Session 8.md` (Session 8), and `Progress Report Session 16.md` (Session 16, regular cadence — Phase 2 mid-execution). Next regular report: my Session 24 (Sessions 17–19 closed no phase and wrote no approved Claim-Sheet amendment, so none was triggered).

There are no scratch/temporary files tracked here at present; transient working files go to the session scratchpad outside the repo, not into this folder.

## Files I own or co-own outside this workspace

- **Root [`README.md`](../../README.md) (the Live-Run README)** — I created it in Session 1 (State A). It is the public-facing status page; it is **co-maintained** by both agents and updated per `Playbooks/live-run-readme.md` at phase transitions / finished artifacts / noteworthy events. Updated Session 2 (Phase 0 → Phase 1), Session 3 (Claim Sheet converged), Session 4 (companions approved; schema drafted), Session 5 (banner flipped to Phase 2 — Execution; still In Progress), Session 6 (running-log entry for the chat-coordination occurrence), Session 7 (running-log entry: plant↔sensor lanes connected and mutually verified + evaluation-harness core built), Session 8 (running-log entry: the diagnosis estimator's detection/calibrated-abstention front built on the seam + evaluation-core review caught/fixed three defects), and **Session 9 (running-log entry + banner date: a detector-side analysis reframed the excitation blocker — a synchronous detector's noise floor is ~100× below the per-sample mechanics-gate floor; still Phase 2 / In Progress, nothing frozen)**.
- **[`director_requests.md`](../../director_requests.md) (project root)** — the project's single append-only record of director-only work. I created it in Session 5 at Phase-1 close; first entry is the non-blocking *Claim Sheet ready for director review*. Co-owned (either agent appends; the director replies in place).
- **[`Claim Sheet.md`](../../Claim%20Sheet.md)** — the project contract. I drafted it (Session 2, default writer); Codex reviewed and edited it (its Session 2); I re-reviewed and approved the same state (Session 3). It is now **agreed by both agents** — the authoritative contract. Changes from here run through the amendment protocol.
- **[`Accessible Claim Sheet.md`](../../Accessible%20Claim%20Sheet.md)** — plain-language companion to the contract. I wrote it Session 3 (default writer); Codex reviewed/edited it (its Session 3); I re-reviewed and approved the same state (Session 4). **Agreed by both agents** — review loop closed. Kept in exact sync with the Claim Sheet.
- **[`Study Guide/`](../../Study%20Guide)** — director-facing study guide. `Pass 1 - Conceptual Foundation.tex` (+ compiled PDF) written Session 3; Codex reviewed/edited (its Session 3); I re-reviewed, independently rebuilt the PDF, and approved the same state (Session 4). **Agreed** — review loop closed. Pass 2 is produced at Phase 3.
- **[`Reproducibility Packet/schema/schema-v1.0.md`](../../Reproducibility%20Packet/schema/schema-v1.0.md)** — the shared `plant → signals → estimator → controller` data contract both Phase-2 lanes build against. Co-owned: my v0.1 sketch → Codex's v0.2 plant-side revision → my v1.0 handoff (S4) → Codex's v1.0 edited review state (S4) → my **same-state approval (Session 5)**. **Jointly approved and in force**; changes now run through the amendment protocol. I built the sensor lane against it in Session 6. **Amendment A1** (contact/safety role widths: `contact_state[2]`, `safety_flag[7]`) was proposed/implemented by Codex (S9) and reached my **same-state approval in Session 10 — A1 is now jointly in force**.
- **[`Reproducibility Packet/scripts/`](../../Reproducibility%20Packet/scripts) — sensor lane + evaluation core (mine):**
  - **Sensor lane (Session 6):** the sensor-realism + fault-injection model — `utils/schema_types.py`, `utils/sensor_model.py`, `utils/rng.py`, `utils/synthetic_plant.py`, `scripts/make_synthetic_plant_trace.py`, `scripts/run_sensor_model.py`, `tests/test_sensor_model.py`. Built against schema v1.0. The shared interface structs in `schema_types.py` (`PlantStepState`/`PrivilegedRecord`/`observable_sources`/`FaultSpec`) went through the review cycle with Codex in **Session 7 and are now jointly approved (loop closed)** — Codex edited them (lossless `PlantStepState`, `qd_obs` validity fix, shared `FaultSpec`) and I re-reviewed, verified end-to-end, and approved the same state.
  - **Evaluation harness core (Session 7):** `utils/metrics.py` (two-layer success-bar metrics: macro-F1 w/ abstention-as-error, per-class recall, Brier/NLL/ECE, risk–coverage, false-abstention, OOD AUROC/AUPRC/false-accept; control `J_5s` + tracking-reduction %), `utils/stats.py` (paired hierarchical bootstrap), `tests/test_metrics.py`, `tests/test_stats.py`. In **Session 8** Codex's cross-review corrected four defects here (truncated-`J_5s` guard, tie-safe risk–coverage, OOD operating point = 95% unknown-detection *sensitivity*, crossed pair×seed bootstrap); I re-reviewed against the contract, reproduced the evidence, and **approved the same state (loop closed)**. I also added the missing `coverage_at_risk` metric (Claim Sheet line 118). In **Session 10** I wired the Slot-7 **"no safety regression"** gate to the now-in-force `safety_flag` (Amendment A1): `safety_incident_rate`, `safety_flag_rates`, `safety_regression_delta`, scored from privileged truth and composing with the crossed pair×seed bootstrap (+4 tests). In **Session 11** Codex reviewed this and added one fail-loud pairing guard (`safety_regression_delta` requires matched `[T,7]` C1/S shape before differencing); I re-reviewed and **approved the same state (loop closed)**.
  - **Diagnosis-estimator lane (Session 8; review loop closed Session 9):** `utils/estimator.py` — the §D `EstimatorOutput` contract, the past-only windowed front-end (`WindowFeatureExtractor`; the concrete `W`/stride realization), the interpretable **detection + calibrated-abstention** rung (`WindowNoveltyDetector`), the allowlisted **oracle** interface, and the `run_online_rollout` seam adapter (`EstimatorCommandPolicy`). Tests `tests/test_estimator.py`. In **Session 9** Codex's cross-review corrected three contract issues (fixed-shape `[W,D]` startup padding, per-channel measurement-time slopes, causal oracle onset) + output/trace validation hardening; I re-reviewed against the schema, reproduced the evidence with an independent 27-check script, and **approved the same state (loop closed)**. The learned attribution net + RMA latent are specified (shared front-end + `W`) and trained post-config-freeze. In **Session 10** I added the **synchronous (lock-in) feature** to `WindowFeatureExtractor` — a per-channel harmonic amplitude at the 0.8 Hz probe (shared `utils/synchronous`, per-channel grid, gated on ≥1 full probe cycle), the interpretable realization of the ~100× detector headroom; this moved the recommended window from `W=512` to **`W=640`** (a 512-sample window can't span a full 0.8 Hz cycle). `stride=8`; both still pilot-sweep proposals, not frozen. In **Session 11** Codex's cross-review corrected the feature from **amplitude-only** to **cosine + sine + amplitude** (amplitude alone discards phase, and the mechanics screen measures the coefficient-vector distance `‖coeff(fault)−coeff(reference)‖`, which amplitude cannot reconstruct); I re-reviewed, reproduced the math, the real feature path (bit-identical to an independent solve), and Codex's real-MuJoCo numbers (2.22× coefficient distance vs 1.77× amplitude-only), and **approved the same state (loop closed)**. In **Session 12** I built the second interpretable rung, **`CoefficientReferenceDetector`**, which consumes the retained `[cos,sin]` *jointly*: it scores the dimension-normalized, healthy-standardized coefficient distance `‖(coeff_live−mean)/scale‖/√D` of a live window to a healthy calibration reference — the deployable analog of Codex's mechanics safe-probe screen. Shared module-level `synchronous_coefficient_vector` / `coefficient_reference_distance` express the pilot's exact statistic (pinned bit-equal by a coherence test), so the deployed rung's margin *is* the pilot's margin. It is detection-only (abstains on fault type, like the novelty detector); its threshold is frozen on healthy calibration with a **fail-loud guard** that refuses a calibration set too small to resolve the requested false-alarm tail (≥`ceil(min_tail/far)` values) — the pilot's undersized-calibration BLOCK turned into a hard precondition. Session 12 also moved `RECOMMENDED_WINDOW` from `W=640`/`stride=8` to the pilot-advanced **`W=768`/`stride=16`** (the only suite-S cell clearing the ≤5% worst-alignment false-alarm screen in Codex's follow-up; still a pilot proposal, not frozen). Handed to Codex for review. In **Session 13** I closed that owner loop: Codex's cross-review made two corrections — it narrowed my "deployed margin = pilot margin" overclaim to a shared *score statistic* only (the threshold/reference/persistence still own the margin), and added a re-fit guard that atomically replaces the reference and invalidates the threshold + detection latch so a stale operating point can't survive a changed reference — and I re-reviewed both (reproduced independently with a 26-check script) and **approved the same edited state (loop closed)**. In **Session 14** I built the shared **end-to-end recovery-seam regression** `tests/test_recovery_seam.py` (a mechanism test on my `EstimatorCommandPolicy` socket: a localizing attribution stand-in sustains active inverse-gain compensation that restores delivered torque over a real multi-step rollout, while a detection-only/unlocalized stand-in stays nominal — the "does attribution improve control" property in mechanism form, explicitly not a `J_5s`/tracking result). Handed to Codex for first review. In **Session 15** I closed that loop: Codex's S14 review found a genuine fixture defect — the `FixedDiagnosisStandIn` re-stamped `detection_time_s` with the *current* decision time every step instead of latching the first-detection time (schema §D: "the time change was *first* flagged"), and it passed the validator only because `detection==now` is a valid special case of `detection≤now`, so the fixture-level correction was the only possible guard — added a reset-able first-detection latch (the same idiom the three real detectors use) plus a regression, and handed it back; I **owner-re-reviewed the diagnosis and the implementation, reproduced both independently (9-check script), and approved the same state (loop closed)**.
  - **Synchronous-detection noise-floor analysis (Session 9):** `scripts/analyze_synchronous_detection_floor.py` + `results/synchronous_detection_floor/` + `tests/test_synchronous_detection_floor.py`. Drives the real gauge pathology stack to show the detector-referred noise floor at the 0.8 Hz probe is ~0.1 microstrain — ~100× below the per-sample mechanics-gate floor — so the bounded-burst differentials Codex's gate marked BLOCK are detectable with wide margin. A detector-side reframing of the excitation blocker; proposes a synchronous feature for `WindowFeatureExtractor` and "W ≥ one probe cycle". Detection floor only (not attribution); nothing frozen. In **Session 9** Codex reviewed and corrected it (phase-invariant joint regression in the new shared `utils/synchronous.py`, full-cycle W=640, honest thermal/surrogate wording); I re-reviewed independently (18-check script, reproduced the old-method phase spread + the floor bit-for-bit) and **approved the same state in Session 10 — loop closed**. The proposed synchronous feature is now **built** (see the estimator lane above).
  - Codex owns the plant lane in the same folder (`utils/cable_mechanics.py`, `utils/cable_plant.py`, `utils/online_loop.py`, `scripts/make_mujoco_plant_trace.py`, `scripts/run_feasibility_spike.py`, `scripts/run_bounded_burst_sensitivity.py`, `scripts/screen_synchronous_safe_probe.py`, `tests/test_cable_plant.py`, `tests/test_feasibility_spike.py`, `tests/test_online_loop.py`, `tests/test_safe_probe_screen.py`) and the online-interface edits I re-reviewed and approved in S8. The shared harmonic statistic `utils/synchronous.py` (Codex, S9) is used by the mechanics safe-probe screen, the detector-floor analysis, and my estimator's synchronous feature — one statistic across all three. Both lanes share `utils/` and the Reproducibility Packet. **Full packet suite: 160 tests green (S19; 159 from Codex's S18 bounded noisy held-decision review, +1 from my S19 review split of its gate-separation test).**
  - **Reviewer edits currently open in Codex's lane (my S19, awaiting Codex's owner re-review):** `scripts/run_bounded_noisy_information_review.py` (decision-gate completeness + report interpretation boundary), `tests/test_bounded_noisy_information_review.py` (gate-separation test rewritten to assert its own docstring, then split in two), `results/bounded_noisy_information_review/bounded_noisy_information_report.md` (regenerated deterministically; purely additive diff). The four other artifacts in that folder are byte-unchanged.
- **Chats with Codex** (co-owned, append-only): [`Phase 0 Coordination/`](../../chats/Claude-Codex/Phase%200%20Coordination) — **Concluded** Session 2; [`Claim Sheet Review and Division of Labor/`](../../chats/Claude-Codex/Claim%20Sheet%20Review%20and%20Division%20of%20Labor) — **Concluded Session 6** (all Phase-1 loops closed; see its `Summary.md`), concluded at the director's instruction; and [`Phase 2 Integration and Config Freeze/`](../../chats/Claude-Codex/Phase%202%20Integration%20and%20Config%20Freeze) — **Active**, opened Session 6, carries the plant↔sensor integration + `config.json` freeze coordination. In **Session 7** I closed the review-cycle loop on the shared interface here (same-state approval) and posted the remaining config-freeze items. In **Session 8** I closed **both** open review loops (evaluation core + online interface) with same-state approval after independent verification, delivered the estimator lane, and proposed `W=512`/`stride=8` — filling the estimator column of the config-freeze table. In **Session 9** I closed the estimator review loop (same-state approval of Codex's three corrections), approved Codex's contact/safety role proposal, and delivered the synchronous-detection noise-floor analysis that reframes the bounded-burst excitation blocker. In **Session 10** I closed **both** loops Codex opened (the synchronous-floor artifact and schema Amendment A1 — A1 now jointly in force), built the synchronous feature into the estimator, and wired the "no safety regression" eval gate. In **Session 11** I closed the loop Codex opened on those two increments — genuine owner re-review of Codex's phase-retention correction (amplitude-only → cos/sin/amplitude) and the safety pairing guard, reproduced independently and approved same-state — which unblocks Codex's pilot sweep. In **Session 12** I reviewed and **approved same-state** Codex's noisy healthy-reference pilot (after independent reproduction: bit-for-bit C1 projection, W=512 inert control, S-detects-structural-fault-where-C1-is-blind), then handed back my `CoefficientReferenceDetector` + `W=768/stride=16` estimator increment for Codex's review (loop open), with two non-blocking forward points (advancing-report threshold-resolution caveat; record the base seed in the pilot artifact). In **Session 13** I closed both loops Codex opened in its S12 turn: the genuine owner re-review of Codex's two estimator corrections (approved same state), and the **first review of Codex's new interpretable recovery-controller floor** (`utils/recovery_control.py`, Codex's lane, which plugs into my `EstimatorCommandPolicy` seam) — approved same state after independently reproducing the real-plant actuator compensation (2× command cancels the plant's 0.5 gain loss, no saturation) and confirming that a detection-only rung (my `CoefficientReferenceDetector`) correctly cannot trigger it, so the safety boundary holds end-to-end through the shared seam. All three of my S12 forward points were verified closed in the committed artifacts (pilot dedup by import identity, base seeds in both `summary.json`s, symmetric threshold caveat in the advancing report). In **Session 14** I closed the loop Codex opened in its S13 turn — a genuine first review of Codex's new interpretable **linear residual / system-ID baseline** (`utils/residual_baseline.py`, Codex's lane), reproduced independently with a 22-check script (ARX/residual math bit-match, matched-ablation nesting, structural leakage guard, atomic re-fit lifecycle, and the end-to-end safety boundary), **approved same-state (loop closed)** — then built and handed over the shared seam regression above (new loop open). Two non-blocking forward points raised (per-suite probability calibration fairness for the eval; a benign fit-dynamics re-bind observation). In **Session 15** I closed **both** loops Codex opened in its S14 turn: the **owner re-review** of Codex's detection-time-latch correction to my `test_recovery_seam.py` (accepted diagnosis + implementation, approved same state), and the **first review** of Codex's new development-only **endpoint-contact extraction** (`utils/cable_mechanics.py`, `utils/cable_plant.py`, `make_mujoco_plant_trace.py`, `tests/test_cable_plant.py`, runbook — Codex's lane) — reproduced independently with a 13-check script (final-step contact force bit-matched to <1e-12 across 4 contact points, intended-pair-only filter, the 7th safety flag == force>limit elementwise, disabled-plant genuinely collision-free, and the airtight privileged/deployable boundary: contact reaches the deployable suites only through motion/strain) and **approved same-state (loop closed)**, with one non-blocking forward point (contact scenarios will make the 7th safety flag a live, not-suite-invariant contributor to my `safety_regression_delta` gate; the matched-`[T,7]` guard already covers it; apply the contact profile identically across each matched C1/S pair). In **Session 16** I closed the loop Codex opened in its S15 turn — a genuine **first review** of Codex's new development-only **optional endpoint-contact profile screen** (`scripts/screen_optional_contact_profile.py`, `tests/test_optional_contact_profile.py`, `results/optional_contact_profile_screen/`, runbook — Codex's lane) — reproduced independently by driving the plant myself across the full 5-height × 3-scenario grid (every gated metric bit-matched to <1e-9, the z=0.05 negative control genuinely zero-contact with the collision pair *enabled*, the z=0.15 two-episode disqualifier real, the selection of **z=0.100 m** reproduced from my own selection logic, `summary.json` SHA-256 identical, full packet 143 green) and **approved same-state (loop closed)**, with two non-blocking forward notes (re-confirm the single bounded contact episode once the eval horizon extends past the ~2.25 s probe release; the row's `max_tip_workspace_radius_m` is a 2-D `true_task_output` readout, not the plant's 3-D gated workspace quantity). In **Session 17** I closed the loop Codex opened in its S16 turn — a genuine **first review** of Codex's new development-only **matched contact-enabled C1/S pilot** (`scripts/run_matched_contact_pilot.py`, `tests/test_matched_contact_pilot.py`, `results/matched_contact_enabled_pilot/`, packet runbook Steps 10–14, Live-Run blocker entry — Codex's lane), which **blocks on all three gates**. I reproduced every gate independently (my own plant drive + metric recompute): the onset+5 s safety audit bit-for-bit (3 episodes at z=0.100; my independent `|q₁|>π` recompute equals the plant's flag column: 1111/1658/1651 steps; the block is the joint-angle flag alone, tip-contact-force flag 0 everywhere), the exact-window info gate (S 8.3%=4/48 held-out healthy false alarms with 100% detection/attribution vs C1 4.2%=2/48 but structurally blind at 20.8%), and the closed-loop drift (re-ran two arms → 36/52 changed-command steps, all arms drift to actuator); byte-determinism verified (5/5 SHA-256 match), full packet **148 green**. **Approved same-state (loop closed)**, with two non-blocking forward notes for the bounded redesign (no layer of this pilot lets closed-loop recovery affect the safety outcome — first contact precedes the first decision and the audit is open-loop — so the redesign must place the decision before the safety-relevant window; and the joint-angle violation is an open-loop-task property orthogonal to the contact plane, so a stabilized/bounded task trajectory is the first-order fix, not a lower plane). A transient discrepancy on my first pass traced to a bug in *my* harness (a `cal`/`eval` vs `calibration`/`evaluation` split label in the reproduction's `pair_id`, which reseeds the sensor RNG), not Codex's artifact. In **Session 18** I closed the loop Codex opened in its S17 turn — a genuine **first review** of Codex's new development-only **bounded task/contact/controller redesign** (`scripts/utils/task_control.py`, the `command_from_nominal` extension in `utils/recovery_control.py`, `scripts/screen_bounded_task_contact.py`, `tests/test_bounded_task_contact.py` + two `tests/test_recovery_control.py` tests, `utils/__init__.py`, `results/bounded_task_contact_screen/`, packet runbook Step 11 + renumbered 12–15, root Live-Run entry — Codex's lane), which **advances z = 0.200 m** as the lowest all-source bounded-contact plane. I reproduced the decision independently (my own drive of the negative-control and selected planes across all four sources): causal arithmetic recomputed from scratch (decision step 1136 = 2.272 s on the stride boundary, precedes movement at 2.400 s), the no-contact control genuinely zero-contact, the selected plane one bounded episode per source with peak force < 5 N, **all seven A1 flags zero** (six recomputed directly from raw truth — the joint-angle flag now `|q|max ≈ 0.36 rad ≪ π` where S16 blew past it — the seventh via the plant column), recovery beginning at the decision and preceding contact for the fault arms with changed-step counts bit-matched (1864/1863/0), the selection + plane bracket reproduced from my own logic, all three artifacts SHA-256 identical to committed (equal to Codex's reported hashes), full packet **155 green**. **Approved same-state (loop closed)**, with two non-blocking forward notes for the next matched-noisy-information gate (recovery now precedes contact but the screen doesn't yet let it *change* the outcome — the eval condition must carry enough dynamic range that a correct diagnosis measurably helps; and the "recovery precedes contact" property depends on the held decision being confident *and* correct, which a real noisy estimator won't always be, so the next gate should report the action-gate firing rate, not just classification accuracy). A transient assertion failure on my first pass traced again to a bug in *my* harness (reconstructing the 3-D workspace flag from the 2-D `true_task_output` in the wrong frame), not Codex's artifact — I recompute the other six flags from raw truth and confirm the seventh via the plant column instead. In **Session 19** I first-reviewed Codex's S18 **bounded noisy held-decision information review** (`scripts/run_bounded_noisy_information_review.py`, `tests/test_bounded_noisy_information_review.py`, five `results/bounded_noisy_information_review/` artifacts, packet runbook Step 12 + renumbering, root Live-Run entry — Codex's lane), which splits its verdict: the information/reference lifecycle advances (S macro-F1 0.995 vs C1 0.704, driven entirely by 100% vs 8.3% structural recall) while the recovery-control profile blocks (the one suite-informative action makes tracking 18.6% worse). I reproduced every number independently — my own 592-run collection, my own reference fit, my own distance/classification/action-gate/macro-F1/`J_5s` — **53/53 checks, zero mismatches**, with all five artifacts byte-identical (SHA-256 matching Codex's reported hashes, at a different worker count). **I did not approve same-state: I edited and handed back (loop open).** Two corrections — the advancing verdict string was guarded by one of five gates, so I demonstrated five counterfactual failure states that would still have emitted it (including one gate, `predecision_clean`, that fed nothing at all) and tightened it so only the control-sensitivity verdict may separate the two advancing labels, proving the recorded decision dict key-for-key identical so four of five artifacts stay byte-unchanged; and the report's interpretation boundary gained the bound that most limits its headline number (every case uses one fixed fault setting per class, so the held-out seeds vary sensor noise only) plus the quantified abstention limitation (smallest held-out margin 0.90, so no threshold below it can bind). The pinning test whose docstring claimed the opposite of its assertion was rewritten and split in two. I deliberately did **not** edit the root Live-Run entry — that log is append-only by playbook and the entry is Codex's — and recommended a seven-word qualifier in chat instead. Three forward notes, the first consequential: the structural recovery action (a bounded 0.75 derate) and the Claim Sheet's control bar (≥10% tracking-integral reduction, safety only as non-regression) point in opposite directions by construction, so a correct structural diagnosis's best case on the headline metric is break-even — the project must either re-aim the structural action at tracking or amend the contract to make a graded safety-margin win first-class (the derate cut peak force 2.051 → 0.499 N, which the current threshold-crossing safety metric scores as nothing). **Review loop with Codex OPEN after S19** (my edits await Codex's owner re-review).
- **Chat with the director** (co-owned, append-only): [`chats/Claude-Codex-Human/Chat Appends/`](../../chats/Claude-Codex-Human/Chat%20Appends) — **Concluded** (Session 6): the director flagged Codex's chat-misplacement occurrence; both agents acknowledged and are watching for it. **Monitoring duty is standing:** if I ever see a reply land anywhere but the physical end of a transcript, I flag it in `Claude-Codex-Human`.
- Future co-owned deliverables (per the default writer convention): I remain default writer for the Technical Report, Accessible Piece, and Study Guide Pass 2; Codex is the required reviewer. Both agents contribute to the Reproducibility Packet. These do not exist yet (produced in Phase 3).

## How to navigate this folder without prior context

1. Read **`Summary of Only Necessary Context.md`** first — it tells you where I am right now and what's next.
2. Read the latest report in **`Session Summaries/`** for the most recent session's full detail.
3. Read **`Literature Foundation.md`** for my grounding on the problem and **`references.md`** for the evidence behind any claim.
4. For project-wide context (premise, standards, phases), see the root [`Project Details/Project Details.md`](../../Project%20Details/Project%20Details.md) and [`AgentPrompt.md`](../../AgentPrompt.md) — I re-read those every session; they are not duplicated here.
