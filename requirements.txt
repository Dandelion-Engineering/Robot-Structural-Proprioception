# PyTorch is installed from the CUDA 12.8 wheel index so the +cu128 build (which
# carries sm_120 / Blackwell kernels for the RTX 5060 Ti) resolves. Verified on
# the project GPU in Session 27: real matmul+autograd execute and match CPU.
--extra-index-url https://download.pytorch.org/whl/cu128

control==0.10.2
gymnasium==1.3.0
matplotlib==3.11.0
numpy==2.5.1
pandas==3.0.3
mujoco==3.10.0
pytest==9.1.1
scikit-learn==1.9.0
scipy==1.18.0
torch==2.11.0+cu128
